# Handoff — SR55 broke the throughput ceiling: #96 Survey Mission Mode surveyed 17 cells in 25 minutes at 1.06 decisions per cell

**Date:** 2026-09-01 11:55 local
**Branch:** main
**Prior handoff:** [HANDOFF_2026-09-01_1115.md](HANDOFF_2026-09-01_1115.md)

## TL;DR

**#96 Survey Mission Mode is LIVE VERIFIED.** SR55 (25 minutes) surveyed **17 cells** — the
previous 54 runs managed 20 in total — taking coverage from 20/150 to **37/150** at **1.06 LLM
decisions per cell**, with **zero wedges and zero errors**. The user's verdict: *"Dufus is doing
WAY better."* Everything is committed and pushed. **Next session: run a longer session (an hour
or more) and read the log** — the open question is whether the per-cell checkpoint is worth its
LLM call, and 17 cells of mostly already-named ground is too small a sample to decide on.

## What was done this session

* **Read the full SR55 log** (`Python/worlds/MCP_World/logs/sim_runner.log`, 17,474 lines, plus
  `agent_decisions.log`). All four checks from the previous handoff passed:
  1. **Start placement fired:** `[dufus] Mission start placement: swept cell (8, 5) beside first
     target (9, 4)` — no commute; the first sweep began about 80 s into the run.
  2. **Code drove the travel:** 19 `mission: next survey target` lines, **zero** LLM calls
     between a target and its sweep. 17 `visual saved; breadcrumb dropped`.
  3. **Exactly one checkpoint per cell** — 17 `observe`/`idle` decisions for 17 sweeps
     (18 LLM calls total including the wake), and the model **never ordered travel** at one.
     `_mission_text` is quoted back in his own words: *"the mission moves me on to 9,4"*.
  4. **The #95 patch shrink held** — no A↔B loop, no `WEDGED`, no new self-inflicted seals.
* **Confirmed the skip path works, which is the failure mode every prior run died on:**
  `sweep: cell (11,6) abandoned — 4 travel ticks moved less than 150cm; marking it unreachable`,
  and the very next tick selected (11,7). The mission did not stop, stall or loop.
* **Verified the DB after the run:** 37 cells swept, **all 37 named**; no `measured` no-go patch
  exceeds 300 cm; the shrunk row (id 5) is still 110 cm.
* **Answered a previous handoff open question:** the `mission` key **survived** the full live run
  — `state.json` still reads `"mission": "survey"` afterwards. (The web-UI `save_agent` path is
  still unverified; only the run path is now proven.)
* **Wrote the SR55 result into `plan/backlog.md`** — item #96 marked LIVE VERIFIED, a results
  table, the four passed checks, and a new "Open after SR55" block. Updated the "Now" banner.
* **Committed and pushed everything** to `origin/main` (was 7 commits ahead; now synced).

## In flight

_None._ Working tree is clean and `main` is level with `origin/main`.

## Decisions locked in this session

* **#96 Phase 1 is proven; do not redesign it.** The re-division of authority (code picks cells,
  the LLM gets one checkpoint) is measured, not argued: 1.06 decisions/cell, 17 cells in 25 min.
* **The checkpoint stays as-is for now.** It produced no name and no refusal in 17 tries, but the
  sample ran over ground that was already named. Gather a longer run before changing it — see
  Open questions for the two candidate shapes.
* **A cell that cannot be walked into is skipped, not fatal.** Proven live at (11,6).
* **Naming happens in the sweep/composite pipeline, not at the checkpoint.** This is why the
  checkpoints read as "nothing new to do" — worth remembering before "fixing" the prompt.

## Open questions

* **Is the per-cell checkpoint worth its LLM call?** All 17 read as *"already surveyed, nothing
  new to do here"*. Two shapes, **neither chosen**: **(a)** fire the checkpoint only when the
  sweep found something worth ruling on (unknown footing, a refusal candidate) — cheaper, but
  guesses at what "worth ruling on" means; **(b)** keep it every cell and trim the prompt to
  ~5 sections (#84's contract, Phase 1's deferred item). **I would gather one longer run first.**
* **`survey_priority: true` and `mission: "survey"` still coexist on Dufus.** Carried over
  unanswered. One sets sweep arrival tolerance, the other sets authority; collapse or document.
* **Does the web UI's `save_agent` preserve the `mission` key?** Still **unchecked** — the run
  path is proven, the UI path is not. `mission` is not one of the form fields in
  `web_ui/main.py`. Do not edit Dufus via the UI until this is verified.
* **Cell (11,6) is marked unreachable in Dufus's *own* spatial map** — no other APC can see that
  verdict. That is exactly what #97 (persistent geometry layer) is for.
* **The #95 `measured` clamp is still unexercised live** — zero new seals were written in SR55.
* **Tests.** Speed mode — none written or run. #95 and #96 are both flagged in the backlog
  "Needs tests" ledger with the specific cases listed.
* **`MEASURED_TTL_S = 1800` still untested**; **`wake_to_first_displacement_ms` still misleads**;
  **the three `_RADAR_*` constants are still guesses.** All carried over.

## Next concrete step

**Run a long session (an hour or more) and then read
`Python/worlds/MCP_World/logs/sim_runner.log`.** Three things decide the next build: (1) does
coverage keep climbing at roughly 40 cells/hour once the rings reach *unnamed* ground; (2) does
any checkpoint then actually name or refuse something — which settles the checkpoint question
above; (3) does `SURVEY MISSION COMPLETE` ever fire while reachable unsurveyed ground remains
(it must not). If the checkpoint still produces nothing over 40+ cells, build shape (a).

## Files touched this session

* `plan/backlog.md` — item **96** marked ✅ LIVE VERIFIED SR55 with the results table, the four
  passed checks, the abandoned-cell proof, and an "Open after SR55" block; the "Now" banner
  updated with the SR55 numbers. **Committed this session.**
* `plan/handoffs/HANDOFF_2026-09-01_1115.md` — the previous handoff, written this session and
  now committed.
* `plan/handoffs/HANDOFF_2026-08-29_1818.md` — the *prior* session's handoff, which had never
  been committed. **Committed this session.**
* `plan/handoffs/LATEST.md` — repointed to this handoff.
* `Python/worlds/MCP_World/agents/dufus/state.json` — runtime fields rewritten by the SR55 run
  itself (no hand edit); `"mission": "survey"` intact.
* **No source code changed this session.** SR55 ran on `6c407b4` / `d3c7362` exactly as built.

## Still outstanding from the previous handoff

* **The plugin rebuild for #92 B has NOT been done.** SR55 ran on the eight-sweep radar fallback
  and still hit these numbers; it costs about 3%.

  ```
  1. Close Unreal Editor
  2. Run: MCPGameProject\Build.bat
  3. Check: MCPGameProject\build_results.txt
  ```

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: run a long session (an hour or more), then read
> `Python/worlds/MCP_World/logs/sim_runner.log` for coverage rate on unnamed ground, whether any
> per-cell checkpoint actually names or refuses anything, and whether `SURVEY MISSION COMPLETE`
> ever fires while reachable unsurveyed ground remains.

## Git state at handoff

```text
(clean — committed and pushed to origin/main)
```

Recent commits:

```text
<pushed this session — see `git log --oneline -6`>
d3c7362 feat: #96 dufus opts into the survey mission
6c407b4 feat: #96 survey mission mode - coverage is a code job, one LLM checkpoint per cell
1dc2125 fix: #95 a measured seal must never inherit a stated radius - and the radar now shows memory vetoes
eb7f374 fix: #94 SR53's two-node loop - the goal pointed at the wall and banned the door
965737f fix: #93 refuse must never seal the ground under the body's own feet
```
