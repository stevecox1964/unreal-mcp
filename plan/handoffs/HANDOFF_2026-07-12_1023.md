# Handoff — First deterministic APC-body slice from SR11 evidence

**Date:** 2026-07-12 10:23 local (America/Chicago)
**Branch:** `auto-loop/backlog`
**Prior handoff:** [HANDOFF_2026-07-11_1902.md](HANDOFF_2026-07-11_1902.md)

## TL;DR

Codex groomed the backlog, fixed runner tick reentrancy (#28), diagnosed the user's SR11 live run,
and built the first approved #27 robustness slice. Scheduled roaming is now bounded by the active
place, authored landmarks beat stale learned/wake aliases with conservative typo tolerance, nearby
APCs are deterministic position facts even when vision misses them, the latest structured vision
result is inspectable, and the cockpit can explicitly capture current APC transforms as reset/start
positions. All 42 offline test scripts pass. Nothing is committed; the next session should perform
the SR12 PIE acceptance run before extending the persistent navigation-ticket design.

## What was done this session

- Read the prior handoff and groomed `plan/backlog.md` into an authoritative active view.
- Reconciled stale planning claims: WP1–WP5 and several numbered items were already complete;
  #19 and #26 were folded into the canonical #27 navigation/controller umbrella.
- Added correctness work #28–#30 and completed **#28 runner tick safety**:
  - automatic ticks, manual whole-sim ticks, and per-agent pulses share a non-waiting manager gate;
  - conflicts return HTTP 409 instead of overlapping bridge/LLM work;
  - invalid/non-positive cadence is rejected at runner and manager boundaries;
  - `/status` exposes the active tick entry for cockpit UX.
- Inspected SR11 decisions, episodes, configs, runtime state, screenshots, and `world_places.db`.
  The evidence showed:
  - Dufus repeatedly chose `wander` at the square; raw wander is a 15 m forward step, so he left
    the place and the scheduler routed him back in a square→leave→return loop;
  - Maren's schedule requested `the vegetable truck`, while the authored marker was
    `vegitable truck`; stale wake/community aliases and the stored start transform kept winning;
  - the VLM recorded no characters even when Maren's screenshot visibly contained another APC;
  - no authored `village square` landmark currently anchors Dufus's semantic destination.
- Built the first #27 deterministic-body slice:
  - at-place `wander` and directional `walk_to` become concrete targets clamped inside the
    scheduled community cell or owned-place box;
  - authored owned landmarks outrank runtime community names and wake seeds;
  - one-edit fuzzy matching is allowed only for place names at least eight characters long, so
    `vegitable truck` resolves for `the vegetable truck` without broad short-name guessing;
  - each multi-agent tick attaches deterministic nearby-APC names/distances before LLM work;
  - `last_perception.json` persists the latest structured VLM result per agent;
  - the runner/web cockpit gained **Capture starts**, an explicit stopped-sim operation that adopts
    current bound APC transforms as future reset/start positions;
  - cockpit cadence input now has a minimum of one second.
- Updated #27/#28 and the active queue in the backlog.
- Verification: `python scripts/run_tests.py` = **42/42 passed**, targeted modules compile, and
  `git diff --check` passes. No Unreal rebuild is required for these Python/web changes.

## In flight

- All workflow, grooming, #28, and #27 slice changes are uncommitted in one dirty worktree.
- The #27 slice is built offline but has not been verified in PIE. Expected live checks:
  Dufus roams without leaving the square cell; Maren resolves/moves toward the authored truck;
  nearby APC facts encourage interaction even when the VLM misses the figure; Capture starts
  updates reset positions only when explicitly clicked with the sim stopped.
- Five project-local skills, the revised autonomous-loop contract, the prior handoff, this session's
  planning changes, and all product changes still need review/commit decisions.
- The user wants Codex to begin driving the cockpit through the in-app browser in small supervised
  steps once PIE and the Python servers are running.

## Decisions locked in this session

- The human supplies visual truth through PIE; Codex writes and offline-tests movement/control code.
- The AI is the executive for durable semantic intent; deterministic code owns movement continuity,
  place boundaries, collision/personal-space facts, progress, recovery, arrival, and failure.
- #27 is approved, but continues in small acceptance-tested slices rather than a rewrite.
- Authored landmarks are ground truth and outrank runtime discoveries/wake seeds.
- Start-position replacement must be explicit (`Capture starts`), not silently refreshed on every
  run, because a same-PIE rerun could otherwise capture a wandered position.
- VLM perception remains useful but cannot be the only source of basic APC proximity facts.
- Offline evidence is not live verification; the user runs PIE acceptance checks before the next
  movement slice.

## Open questions

- Where exactly should the user place `Landmark_Community_village_square`? Code cannot infer the
  authoritative square coordinate; the current square is a runtime/wake-derived location.
- Will bounded roaming feel natural in PIE, or should the next slice add a deterministic patrol
  pattern inside the place rather than clamping forward intent?
- Does the nearby-APC prompt fact produce appropriate greetings without causing across-wall or
  excessive interactions? PIE evidence should set the distance/visibility policy.
- After SR12, should the next #27 slice introduce the persistent navigation ticket/headless adapter,
  or first address road preference/dead-end recovery from the observed run?
- Should workflow/planning changes and product code be committed together or as separate reviewable
  commits? Nothing has been staged.

## Relevant backlog items

- **#27 Navigation executive + deterministic movement controller:** approved/in progress; first
  SR11 robustness slice built offline, PIE verification pending.
- **#28 Runner tick safety:** done offline, 42/42; live conflict behavior may be spot-checked through
  the cockpit but needs no Unreal rebuild.
- **#29 Cockpit path containment:** current `Now`, ready and loop-safe after SR12.
- **#20 Movement timing instrumentation:** next diagnostic slice.
- **#30 Direct `httpx` dependency:** small loop-safe packaging correction.
- **#17 routed travel / #19 roads / #26 dead ends:** #17 is the foundation; #19/#26 are now folded
  into #27 rather than independent patches.

## Next concrete step

Run the **SR12 supervised PIE acceptance pass** with the restarted Python servers, true APC start
placements captured from the cockpit, and the world synced after adding the village-square landmark.

## Files touched this session

- `Python/agent_runtime/agent_manager.py` — tick gate, bounded at-place movement, nearby APC facts,
  capture-start operation, perception evidence.
- `Python/agent_runtime/agent.py` — explicit start-transform replacement method.
- `Python/agent_runtime/place_db.py` — source-prioritized, conservative typo-tolerant owned-place
  lookup.
- `Python/agent_runtime/llm_router.py` — nearby-APC prompt surface.
- `Python/agent_runtime/runner_app.py`, `runner_client.py` — tick conflicts/cadence validation and
  capture-start transport.
- `Python/web_ui/main.py`, `templates/sim.html` — Capture starts proxy/control and cadence minimum.
- `Python/scripts/agent_runtime/test_{owned_places,route_planner,prompt_context,pacing_and_reset,
  runner_api,sim_controller}.py` — SR11, #28, nearby-fact, and capture-start regression coverage.
- `.gitignore` — ignore per-agent `last_perception.json` runtime evidence.
- `plan/backlog.md`, `plan/specs/README.md` — grooming, status reconciliation, #27/#28 outcomes.
- `plan/autonomous_loop.md`, `.agents/skills/*`, and `plan/handoffs/HANDOFF_2026-07-11_1902.md`
  were already uncommitted from the prior session and remain so.
- `plan/handoffs/HANDOFF_2026-07-12_1023.md`, `LATEST.md` — this session snapshot.

## How to resume

Open this repository as the Codex project and say:

> Read hand off.

For supervised UI driving, start Unreal in PIE and restart `Python/start_sim.bat`, then tell Codex:

> PIE and the servers are ready. Drive the cockpit for SR12.

## Git state at handoff

Branch: `auto-loop/backlog`, tracking `origin/auto-loop/backlog`. No new commits were created; latest
commit remains `0524b0e` (`handoff 2026-07-09 + backlog #26: Maren stays put; onboarding #13.1-13.3 landed`).

The worktree is intentionally dirty with modified Python runtime/web/test/planning files plus
untracked `.agents/skills/*` packages and handoffs. `git diff --stat HEAD` before writing this handoff
reported 19 tracked files changed, 803 insertions, and 178 deletions; this handoff and its `LATEST.md`
update add two planning-file changes. Nothing is staged, committed, pushed, or merged.
