# Handoff — Map skew diagnosed + calibration built, wake sweep drops community breadcrumb, stale seeds purged, WP6/WP7 specs ready, ALL PUSHED

**Date:** 2026-07-06 12:47 local
**Branch:** auto-loop/backlog (**synced with origin — user said push; the old DO-NOT-PUSH is lifted**)
**Prior handoff:** [HANDOFF_2026-07-05_1704.md](HANDOFF_2026-07-05_1704.md)

## TL;DR

User-feedback session on the /map screenshot + PIE observations. Diagnosed the 3–4-cell map skew
(the capture is a hand-taken editor screenshot; assume-covers-bounds is false) and built the
`image_bounds` calibration + cursor coord readout; the wake 180° sweep now drops the community
breadcrumb (`mark_swept`) so a home district shows on the map at wake; Maren's re-wandering was
**stale wake-seed rows** after the user moved the actors — 6 rows deleted from the live DB. Backlog
gained **#18–#21** and a ranked queue; **WP6 (#15 manifest) and WP7 (#21 sync button) specs are
written and executor-ready**. Suite 35/35, everything committed AND pushed. Next: build WP6 —
**user was asked "executor session or build here?" and has not answered yet.**

## What was done this session

- **Map skew diagnosed** (user: cells render 3–4 cells right of the world features): the overlay
  math is self-consistent (17×12=204 cells; Maren (-8400,150)→(6,6) round-trips) — the fault is
  `web_ui/images/world_map_view.png`, a hand-taken editor viewport screenshot (HUD/toolbar baked
  in) that does **not** frame the world bounds, violating #6c's assume-covers-bounds calibration.
- **`7d942c6` calibration + breadcrumb fix:**
  - `WorldGrid` gained an optional `image_bounds` passthrough (`world_grid.json`) = the world rect
    the capture *actually* frames; `/api/map` carries it; `map.html` maps world→pixel through
    `image_bounds || bounds` (`geo.frame`).
  - `/map` gained a **live cursor readout**: `world (x, y) cm — cell (col, row)` (mousemove) — the
    measuring tool for registration.
  - `AgentManager._ingest_wake_views` (extracted from `_wake_agents`): after a non-empty wake 180°
    sweep, drops `mark_swept` — the community place cell exists from wake. Root cause of "Dufus
    never creates a community cell": wake sweep never dropped a breadcrumb + scheduled "act" ticks
    are sweep-exempt (agent_manager.py `sched_status != "act"` gate), so an agent acting at home
    never swept its own district.
  - Tests: `test_wake_place.py` +breadcrumb test, `test_map_view.py` +image_bounds passthrough/page
    checks. Suite 35/35.
- **User moved the actors in PIE** (facts): Maren + vegetable truck at **(-8950, 160)**, Dufus's
  home at **(-10460, -800)**. Grid math: Maren in cell **(6,6)** (50 cm from its west edge!), Dufus
  in **(5,5)** — **NOT the same cell**; they straddle the corner at world (-9000, 0). The "(2,6)"
  the user read is the ~4-cell skew — placement was done against the mis-registered map.
- **Maren re-wandering root-caused:** stale wake seeds, not a wake-fix regression — her owned
  truck rows anchored ~9 m east at the old spot, so "travel" was geometrically honest. **Live DB
  cleaned (out-of-band):** 6 owned rows deleted (maren: "the vegetable truck", "vegetable truck";
  dufus: "home", "farm road", "village square", "main street" — all at his old day-start spot).
  Both re-seed at the new spawns on next wake.
- **`dd8ea78` backlog:** new items **#18** live registered top-down camera (real-time map, kills
  the manual png), **#19** keep APCs on sidewalks/roads (Maren cuts through corn field/yards; three
  design options a/b/c), **#20** Dufus slow to start moving (instrument first), **#21** "I moved
  things — sync the world" button; status banner with the facts + ranked queue.
- **`b823e00` architect pass (Fable role):** `plan/specs/WP6-authored-places-manifest.md` (#15:
  places.json schema incl. reserved `actor` field, declarative converging loader, authored-wins,
  schedule validation, `source` column authored/runtime/wake-seed) and
  `plan/specs/WP7-sync-world-button.md` (#21 v1: `purge_wake_seeds` + `POST /api/world/sync` +
  /map button; rides WP6's `source` column). README table updated (WP6 ready, WP7 after WP6).
- **`37b3afc`** committed the 2026-07-05 handoff docs that were left uncommitted.
- **Pushed everything** to origin (user: "lets push what we have so far").

## In flight

_None in code — tree clean, suite 35/35, pushed. Conversationally in flight: the user was asked
whether WP6/WP7 should be built by an executor session or in-session; no answer yet._

## Decisions locked in this session

- **The manual /map png is retired as an approach** (user: "probably a bad idea now… my bad") —
  #18's engine-shot ortho capture framed exactly to bounds replaces it; `image_bounds` stays as
  the fallback calibration for hand shots.
- **Wake look-around = the district's community sweep** — `_ingest_wake_views` drops `mark_swept`
  after a non-empty wake sweep (user expects a community cell in every explored district).
- **Ranked next work (user: "ranked for next work items… before moving on"):**
  1. #15 manifest → 2. #21 sync button → 3. #18 live camera → 4. #17 grid-first routing →
  5. #19 sidewalks/roads → 6. #16 click-to-author → 7. #20 pacing.
- **Push is back on** — sync local↔origin directly (per the standing push-no-PRs preference).
- **Nobody but the user authors MCP_World's `places.json`** (WP6 D7) — Claude must not invent
  world coordinates; today's facts are noted in the spec for the user to use.
- **#21 v1 purges only `source='wake-seed'` rows** — authored rows are ground truth, runtime
  LLM-discovered places are agent memories; don't guess at legacy rows.

## Open questions

- **Who builds WP6/WP7** — executor session (Opus/Sonnet, per the Fable=architect workflow) or
  this/next session directly (WP4 precedent exists)? Asked; unanswered.
- **Maren/Dufus cell placement:** they straddle the cell corner at (-9000, 0) — if the user wants
  all three in ONE district they need to nudge positions (Maren is 50 cm from the edge). Best done
  after the map is registered (#18 or an `image_bounds` calibration via the new cursor readout).
- **PIE verify still pending (SR3):** the 2026-07-05 wake fix + today's breadcrumb + purged seeds
  have not been observed live yet. Expected: Maren re-seeds at (-8950,160) and stays; Dufus
  re-seeds "home", his district shows swept at wake.
- **#19 design call:** navmesh area costs (a) vs lizard-brain surface facts (b) vs #17 planner
  weighting (c) — spec leans b+c; user hasn't chosen.
- (Carried) act-tick sweep-exemption veto; Dufus's "home until 08:30" schedule block; #12.2
  stranger re-greeting.

## Next concrete step

Build **WP6** per `plan/specs/WP6-authored-places-manifest.md` (D1–D7 are fully decided): new
`Python/agent_runtime/places_manifest.py` + `source` column migration in `place_db.py` + loader
call in `start_simulation` + schedule validation; new `test_places_manifest.py`. Then WP7. (If the
user wants an executor session instead, hand it the spec — the executor contract is in
`plan/specs/README.md`.)

## Files touched this session

- `Python/agent_runtime/agent_manager.py` — `_ingest_wake_views` (extracted + mark_swept breadcrumb)
- `Python/agent_runtime/world_grid.py` — `image_bounds` passthrough
- `Python/web_ui/main.py` — `build_map` carries `image_bounds`
- `Python/web_ui/templates/map.html` — frame-aware placeRect, cursor world/cell readout
- `Python/scripts/agent_runtime/test_wake_place.py` — breadcrumb test
- `Python/scripts/agent_runtime/test_map_view.py` — image_bounds tests
- `plan/backlog.md` — two 2026-07-06 status banners; items #18–#21; ranked queue
- `plan/specs/WP6-authored-places-manifest.md`, `WP7-sync-world-button.md`, `README.md` — new specs
- `plan/handoffs/HANDOFF_2026-07-05_1704.md`, `LATEST.md` — prior session's docs committed
- Live (untracked) `Python/worlds/MCP_World/world_places.db` — 6 stale wake-seed rows deleted

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: build WP6 per `plan/specs/WP6-authored-places-manifest.md` (places.json manifest +
> source column + loader + schedule validation + tests), then WP7 (sync-world button). Decide
> first: executor session or build directly (open question from last session).

## Git state at handoff

```
(clean — all committed and pushed)
```

Recent commits:

```
b823e00 specs: WP6 (#15 authored places manifest + source tagging), WP7 (#21 v1 sync-world button)
dd8ea78 backlog: #18 live map camera, #19 sidewalks, #20 pacing, #21 sync-world button; queue re-ranked; stale seeds cleaned
7d942c6 map skew: image_bounds calibration + coord readout; wake sweep drops community breadcrumb
37b3afc handoff: 2026-07-05 session (wake fix x2, 9m place cells, #6c overlay)
6f7c385 backlog: #15 authored places manifest, #16 click-to-author map editor, #17 grid-first routing; #13 reframed as downloader bootstrap
```
