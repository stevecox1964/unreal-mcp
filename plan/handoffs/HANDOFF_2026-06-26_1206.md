# Handoff — Autonomous loop: #1 nav + harness + #5 social + #6 map query

**Date:** 2026-06-26 12:06 CDT
**Branch:** auto-loop/backlog (NOT main — never pushed)
**Prior handoff:** [HANDOFF_2026-06-24_2244.md](HANDOFF_2026-06-24_2244.md)

## TL;DR

Running an unattended coding loop (user: "run coding till you run out of credits",
skip anything needing PIE/editor/them). On a dedicated branch `auto-loop/backlog`,
**7 green commits** so far: place-name nav (#1), the loop harness (aggregator +
contract + green baseline), social memory (#5), and the map query surface (#6).
Offline suite is **10/10 green** via `python scripts/run_tests.py`. Next loop-safe
targets: **#2 `config_store.py`** and **#3 runner scaffolding**. Nothing pushed —
human reviews the branch before `main`.

## What was done this session

All on branch `auto-loop/backlog`, each commit green against the offline suite:

- **#1 place-name navigation** (b88766d) — `walk_to "village square"` used to idle.
  Now resolves: `PlaceDB.find_named_cell` (normalized exact + substring),
  `WorldGrid.cell_center` (inverse of `locate`), `AgentManager._resolve_place_target`,
  wired into `_execute_world_action`. Test: `test_place_resolver.py`.
- **Green baseline** (ad1f82f) — fixed 2 pre-existing offline failures: (a) regression
  from b3a9e62 that dropped grid/place from the scene-unchanged `pulse_agent` return —
  restored via `_last_grid_place` stash; (b) two stale stub bridges missing
  `set_ai_state`/`print_to_screen`.
- **Loop harness** — `scripts/run_tests.py` aggregator (bc4bf22, runs offline
  `scripts/agent_runtime/test_*.py` as subprocesses, one PASS/FAIL); `plan/autonomous_loop.md`
  run-contract (7acd742); backlog #4.1/#4.3 checked off.
- **#5 social/acquaintance memory** (f105481) — `SocialMemory` (per-agent `social.json`):
  first_met, last_seen, last_cell, meet_count, interaction_count, clamped sentiment.
  `AgentManager._record_sightings` records named perceived characters each tick
  (parallel-safe, per-agent file); acquaintances surfaced on the observation for recall.
  Test: `test_social_memory.py`. social.json gitignored.
- **#6 map query surface** (d914372) — `PlaceDB.all_named_places` + `AgentManager.known_places`
  (name + compass bearing + distance_m, nearest first); surfaced as `observation["known_places"]`
  (nearest 8). Test: `test_map_query.py`.

## In flight

_None._ All committed; tree clean; suite green. Mid-loop — more backlog items remain.

## Decisions locked in this session

- **Dedicated branch `auto-loop/backlog`, commit every green step, never push** — user
  reviews before `main` (loop guardrail).
- **Loop-safe = Python under `agent_runtime/**` + `scripts/**`, offline-test-verified.**
  Never C++/Blueprints/UMG/`.env`/PIE; the socket-based `scripts/{actors,node,blueprints}`
  tests are excluded from the aggregator (need live Unreal).
- **#6 map = context injection, not an LLM tool** (revisit if the place list grows).
- **Anonymous figures ("unknown person") never become acquaintances** — no identity.

## Open questions

- **Social interaction/sentiment wiring** (#5 leftover): say/message → `record_interaction`
  needs deciding *who* speech is directed at. API + sentiment clamp are built/tested; the
  wiring is deferred.
- **#6 manual-capture mode + lizard-brain routing** stay deferred — need the editor / a
  design decision (on-contract path-as-facts is written up in backlog #6).

## Next concrete step

Continue the loop with **#2 `agent_runtime/config_store.py`** (read/write `.env` with secrets
shown as set/unset only, preserve comments, trigger the existing reload) — pure Python, offline
testable; the settings *UI* is skipped (needs a running app). Then **#3** runner scaffolding
(factory extraction + `sim_runner.py` control API with offline tests; live-sim run skipped).
Cycle each: failing test → implement → `python scripts/run_tests.py` green → commit → check off.

## Files touched this session

- `Python/agent_runtime/place_db.py` — `find_named_cell`, `all_named_places`
- `Python/agent_runtime/world_grid.py` — `cell_center`
- `Python/agent_runtime/social_memory.py` — **new** SocialMemory store
- `Python/agent_runtime/agent_manager.py` — `_resolve_place_target` + walk_to branch,
  `_last_grid_place` regression fix, `_social`/`_record_sightings`, `known_places`,
  observation recall keys
- `Python/scripts/run_tests.py` — **new** offline aggregator
- `Python/scripts/agent_runtime/test_{place_resolver,social_memory,map_query}.py` — **new** tests
- `Python/scripts/agent_runtime/test_{world_grid,pacing_and_reset}.py` — stub fixes
- `plan/autonomous_loop.md` — **new** loop contract
- `plan/backlog.md` — #1/#4.1/#4.3/#5/#6 checked off; #6 map feature added earlier
- `.gitignore` — ignore per-run `social.json`

## How to resume

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. We're mid
> autonomous loop on branch `auto-loop/backlog` (never pushed). The next concrete step is:
> build `Python/agent_runtime/config_store.py` (#2) failing-test-first, then #3 runner
> scaffolding. Run `python scripts/run_tests.py` (must stay green) and commit each green step.
> Follow `plan/autonomous_loop.md`.

## Git state at handoff

```
(clean working tree, branch auto-loop/backlog)
```

This branch's commits (none pushed):

```
d914372 Add map query surface — known places by name (backlog #6)
f105481 Add per-agent social/acquaintance memory (backlog #5)
7acd742 Add autonomous-loop run contract (backlog #4.1); mark harness done
bc4bf22 Add offline test aggregator (backlog #4.3)
ad1f82f Report grid/place on scene-unchanged ticks; green the offline baseline
ed0e03f backlog: mark #1 resolver done; add #6 map feature
b88766d Resolve walk_to named places to world locations (backlog #1)
```
