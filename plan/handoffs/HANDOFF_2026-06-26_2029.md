# Handoff — Standalone web-driven sim live; MCP dumped; queue 1–6 done

**Date:** 2026-06-26 20:29 CDT
**Branch:** auto-loop/backlog (NOT main — never pushed)
**Prior handoff:** [HANDOFF_2026-06-26_1909.md](HANDOFF_2026-06-26_1909.md)
**Note:** one-off handoff (the user asked for it to bookend a restart). Handoffs are otherwise
retired — the backlog is the source of truth (memory `feedback-no-handoffs`).

## TL;DR

The sim is now **standalone and web-driven**: `sim_runner.py` is the engine (HTTP control API on
:8777) and the `web_ui` `/sim` cockpit (:8765) drives it — Start / Step / Stop + a live decision
log — with **no Claude/MCP in the loop**. `Python\start_sim.bat` boots both. The user **dumped the
local MCP** (`.mcp.json` → `.mcp.json.disabled`) and will restart Claude to sever the live
connection. **9 commits on `auto-loop/backlog`, 19/19 tests green, never pushed.** Next loop-safe
item is **#7 provider CRUD**, pending a small A/B design choice (below).

## What's running / how to drive it (no Claude needed)

1. Unreal → Play (PIE) — the engine needs the Unreal socket (:55557) to load agents + tick.
2. `Python\start_sim.bat` → engine (:8777) + cockpit (:8765).
3. Open **http://127.0.0.1:8765/sim** — Start / Step (single tick) / Stop, watch the live log;
   flip Ollama⇄cloud in **Settings**. `curl http://127.0.0.1:8777/status` for a raw check.

## What was done this session (9 commits, each offline-tested green)

The autonomous loop cleared queue items 1–6 + supporting fixes:
- **#1 episodic consolidation** (`EpisodicLog.consolidate`, caps overnight growth)
- **#2 state.json config/runtime split** (runtime → git-ignored `runtime.json`; no more tree churn)
- **#3 standalone runner** (`sim_runner.py` + `runner_app` control API + `RunnerClient`)
- **#4 web settings backend** (`/settings`, `config_store`, secrets masked)
- **#5 rename** NPC Builder → Unreal World Sim
- **#6 web sim cockpit** (`/sim`: drive + single-step + live log; `start_sim.bat`)
- **fixes:** runner `/events` + `recent_events`; **bugfix** runner `/status` called a non-existent
  `get_simulation_status` (real method is `get_status` — the offline stub had masked it; stubs now
  mirror real method names); friendly runner root route (was a bare 404).
- **docs/backlog:** README documents runner + settings; backlog has the cockpit/CRUD direction +
  an **MCP-deprecation** entry (Later/ideas).

## Decisions locked this session

- **Web UI is the cockpit; sim engine is standalone** — so it can run slow **local/offline models**
  with no Claude attached. Claude → plain **API calls** for coding; **MCP to be deprecated** (Epic
  ships an official Unreal MCP now) — phased, done last (backlog Later/ideas).
- **MCP dumped locally** — `.mcp.json` renamed to `.mcp.json.disabled` (gitignored; tracked
  `mcp.json` template untouched). Restore = rename back + reconnect.
- **Never push unattended** — work stays on `auto-loop/backlog` for the user to review + merge.

## Open questions

- **#7 provider CRUD — pick A or B:**
  - **(A)** Structured role selector (Decision + Vision provider dropdowns + model), over `.env`/
    `config_store`. Smaller; Claude can do solo + tested.
  - **(B)** Provider *profiles* CRUD (named profiles, create/edit/delete, assign to roles) — needs a
    new `config.json` model. Bigger.
  - Recommendation: start **(A)**, grow to **(B)** later.

## Next concrete step

After restart: a fresh session won't have the `unrealSIM` MCP tools (dumped) — that's intended;
drive the live sim via the cockpit/`curl`. To resume building, say **"run the autonomous loop"**
(reads backlog "Autonomous queue") — the next item is **#7 provider CRUD**; confirm **A vs B** first.
Otherwise point me at anything. Also pending: **review + merge `auto-loop/backlog` → main** (9 commits).

## Files of note (this session, all under Python/ unless noted)

- new: `agent_runtime/{episodic_memory consolidation, runner_app, runner_client}`,
  `sim_runner.py`, `web_ui/templates/{sim,settings}.html`, `start_sim.bat`,
  `scripts/agent_runtime/test_{runner_api,sim_controller,settings_page,agent_state}.py`
- changed: `agent_runtime/{agent.py, agent_manager.py, episodic_memory.py, place_db.py}`,
  `web_ui/{main.py, templates/base.html, templates/index.html}`, `start_npc_builder.bat`,
  `.gitignore`, `README.md`, `plan/backlog.md`
- local-only: `.mcp.json.disabled` (the MCP restore point)

## How to resume

> Resume the previous session. Read `plan/handoffs/LATEST.md`. The sim is standalone + web-driven
> (`start_sim.bat` → cockpit at :8765/sim); MCP was dumped (`.mcp.json.disabled`), so no unrealSIM
> tools — drive via the cockpit. On branch `auto-loop/backlog` (9 commits, 19/19 green, unpushed):
> review + merge to main, or say "run the autonomous loop" to build #7 provider CRUD (pick option
> A vs B first — see the handoff).

## Git state at handoff

```
On branch auto-loop/backlog (9 commits ahead of main; main is pushed/in sync with origin)
?? .mcp.json.disabled        (local MCP restore point; gitignored sibling)
```

Recent commits:

```
d34dffc Runner: friendly root route instead of a bare 404
a31999d Add web sim cockpit: drive + step + log the standalone sim (queue #6)
03de6c7 Runner: add /events; fix status to call the real manager method
be32968 docs: document the standalone runner + web settings page
2d5e67c Rename NPC Builder -> Unreal World Sim; add Settings nav link (queue #5)
```
