# Handoff — SR56 stuck twice on ground no sense could see; #101 ground sense + #102 Survey/Play built, awaiting plugin rebuild and SR57

**Date:** 2026-09-01 18:26 local
**Branch:** main
**Prior handoff:** HANDOFF_2026-09-01_1155.md

## TL;DR

SR56 (survey mission, stopped by the user at tick 94) wedged Dufus twice: on a raised slab at (5,8) and in a
carport-floor hole at (5,4). Every sense measured air and stayed silent. #101 (walkable ground as a
measurement, path-tested move orders, footing reflex) and #102 (Survey / Play sim mode, Survey default) are
**built, reviewed, merged and pushed** (`904bc79`) but **not compiled and not live-verified**. Next: the user
rebuilds the plugin with `MCPGameProject\Build.bat` (editor closed) and runs SR57 in Survey mode.

## What was done this session

- **SR56 log scan** (`Python/worlds/MCP_World/logs/sim_runner.log` 17:31–17:35 local; `agent_decisions.log`
  22:31–22:35 UTC). Findings written into `plan/backlog.md` under #101:
  - Stuck 1: (5,8) at `z=268.9` (ground elsewhere `z≈90`) — on a slab. Three move orders returned `success`,
    0 cm moved, three good cells falsely marked unreachable.
  - Stuck 2: (5,4) under the mobile-home carport; the user's editor screenshot shows a hole in the walkable
    mesh between the car and the wall. Two orders, 15 cm moved.
  - Root cause: `command_character_move_to` never tested the path; the radar measures air only.
  - Why the #86–#95 guards did not fire: the step planner runs only for `wander` / `walk_to` **with a
    direction**. Mission travel is `walk_to` **with a location** handed straight to the engine (#96 design).
    Not a bug in the guards — a gap. Recorded in the backlog (`155602d`).
- **#101 spec + build** (`094c2d9`), Sonnet agent, reviewed by Fable:
  - C++ (`UnrealMCPCharacterCommands.cpp/.h`, `UnrealMCPBridge.cpp`): radar gains per-sector `ground_cm`
    and result-level `ground_under_feet` / `nearest_ground`; `move_to` runs `FindPathToLocationSynchronously`
    first and returns `path: full|partial|none`, `path_end`, `path_end_gap_cm`, `moved` (no move issued on
    `none`); new `command_character_step_to_ground` (≤ 400 cm sweep-teleport onto nearest walkable ground).
  - **Review fix applied by Fable:** all ground projections now start at the **feet**
    (`Base - Up*HalfHeight`), not the capsule centre — the first draft's 60 cm reach from the centre would
    have read "not on ground" every tick and fired the reflex every tick.
  - Python: `unreal_bridge.radar/step_to_ground`; `agent_manager._recover_footing` (code-side reflex, seals a
    `measured` refuse patch reason `unwalkable ground`, episode, `footing_recoveries` count in
    `SIMULATION STOP`); `_survey_travel_is_wedged` reads the stashed path answer on tick 1
    (`_note_survey_travel_result`, `_abandon_survey_travel`; `partial` inside the cell → walk to `path_end`);
    `move_plan.plan_step(ground_cm=…)` caps steps for the 8 named compass directions; `_radar_text` states
    "air X m but walkable ground ends at Y m"; `_footing_text` only on a failed step; decision log gains
    `path`, `ground_under_feet`, `footing_recovery`.
- **#102 spec + build** (`158284d`, merged `40811f6`), Sonnet agent in a worktree, reviewed by Fable:
  `mode` is `survey` (default) | `play`; `live` → `survey` alias in `AgentManager._normalize_mode`; unknown →
  WARNING + survey; `explore` and `_pulse_explore` removed; play gates `_should_sweep_here`,
  `_mission_wants_tick`, wake look-around, mission start placement; play prompt fact "Survey work is paused
  today…"; `sim.html` dropdown Survey/Play with helper line; defaults changed in `runner_app.py`,
  `runner_client.py`, `web_ui/main.py`.
- **#103 spec written** (`1490ccd`): survey stand point = best open spot in the cell (candidates scored by
  walkable ground + full path + minimum radar clearance), not the geometric centre. Queued next; depends on #101.
- Backlog Status lines for #101/#102 set to BUILT / not live-verified (`904bc79`). All pushed.

## In flight

- **Plugin not compiled.** `MCPGameProject\Build.bat` has not been run since the C++ change. Agent verified
  signatures against UE 5.5 headers (`NavigationSystem.h`, `NavigationPath.h`). If it fails, fix from the
  first error in `MCPGameProject/build_results.txt`.
- **SR57 not run.** Pass mark: no APC sits > 2 ticks with `moved_cm < 150`; every abandoned cell carries
  `path` in the decision log; `footing:` WARNING lines are few or zero.

## Decisions locked in this session

- **Option A footing reflex:** the body steps itself ≤ 4 m back onto walkable ground with no model call,
  logs loud, seals the spot. User confirmed. (Option B, report-only, rejected.)
- **Prompt vocabulary:** "walkable ground", never "navmesh". Engine field names stay generic
  (`ground_cm`, `path`, `stepped`).
- **`none` path → no move issued, `success` stays true.** The path answer is the fact; Python decides.
- **Survey is the default sim mode.** `explore` is gone; `live` survives only as an alias.
- **Mission travel legs stay engine-pathed** (no step planner); #101's path test + per-tick footing reflex
  are what protect them.

## Open questions

- Pre-existing "navmesh" wording in `llm_router._sense_note` blocker text (~line 923, from #61) contradicts
  the #101 doctrine. Left untouched (surgical scope). Fix in a doctrine pass or when #99 is picked up?
- Tests referencing the removed `explore` mode (`test_explore_tick.py`, `test_frontier_blocking.py`,
  `test_runner_api.py`, `test_sim_controller.py`, `test_spool_up.py`) are now broken; speed mode says do not
  touch tests until the user says. Delete or rewrite when tests are next written.
- Dead state left behind by the explore removal (`_MAX_FRONTIER_FAILURES`, `_frontier_failures`,
  `_last_cell`) — remove alongside the test cleanup.

## Next concrete step

User closes the editor, runs `MCPGameProject\Build.bat`, reopens the editor, starts the cockpit (Mode shows
**Survey**), runs SR57; then Claude scans `logs/sim_runner.log` for `footing:` / `abandoned —` lines and
`agent_decisions.log` for `path` values and writes the SR57 verdict into `plan/backlog.md` #101.

## Files touched this session

- `plan/backlog.md` — SR56 findings, #101/#102/#103 specs, guard-bypass finding, Status lines.
- `MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/Commands/UnrealMCPCharacterCommands.cpp` —
  ground column, path-tested move_to, `HandleCommandStepToGround`, feet-origin fix.
- `MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Public/Commands/UnrealMCPCharacterCommands.h` — new handler decl.
- `MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/UnrealMCPBridge.cpp` — whitelist new command.
- `Python/agent_runtime/unreal_bridge.py` — radar docs, `step_to_ground()`.
- `Python/agent_runtime/agent_manager.py` — #101 reflex/travel/plan changes; #102 mode normalisation and gates;
  `_pulse_explore` removed.
- `Python/agent_runtime/llm_router.py` — ground clause in `_radar_text`, `_footing_text`, paused-mission fact.
- `Python/agent_runtime/move_plan.py` — `ground_cm` cap.
- `Python/agent_runtime/memory_store.py` — decision-log fields.
- `Python/agent_runtime/runner_app.py`, `runner_client.py`, `Python/web_ui/main.py` — default mode `survey`.
- `Python/web_ui/templates/sim.html` — Survey/Play dropdown + helper text.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is:
> after the user rebuilds the plugin and runs SR57, scan `logs/sim_runner.log` for `footing:` / `abandoned —`
> lines and `agent_decisions.log` for `path` values and write the SR57 verdict into `plan/backlog.md` #101.

## Git state at handoff

```
(clean)
```

Recent commits:

```
904bc79 docs: #101/#102 built, awaiting SR57 live verification
40811f6 Merge branch 'worktree-agent-ac326444031a0a061'
094c2d9 feat: #101 walkable ground is a measurement - radar ground column, path-tested move orders, footing reflex
1490ccd docs: #103 survey stand point is the best open spot in the cell, not the center
155602d docs: #101 - why the #86-#95 guards did not fire: mission travel walks bypass the step planner by design
```
