# Handoff — Direction reset + web observability (A1–A3 landed); grid/place design decisions locked; loop-safe queue drained

**Date:** 2026-07-01 11:43 local
**Branch:** auto-loop/backlog (unpushed — 10 commits ahead of the prior handoff's `317a349`)
**Prior handoff:** [HANDOFF_2026-06-28_1906.md](HANDOFF_2026-06-28_1906.md)

## TL;DR

Groom-and-build session: reset direction (wind down grid/place → web observability), locked several
director decisions, and **built + tested A1–A3** (web map view, staleness, restart-from-morning) — the
loop-safe slice of the new direction. Suite **26/26**, everything committed to `auto-loop/backlog`, nothing
pushed. The **loop-safe queue is now drained**; all remaining work needs you (PIE / editor / a design
call). Next session is a **joint B-queue** session — start with **B2: run the sim in PIE and watch cells
build out on the new `/map`** (and/or B1 child BPs).

## What was done this session

- **Env fix (baseline was silently red):** Python was upgraded to **3.14**, pulling **Starlette 1.0.0**,
  which removed the 2-arg `TemplateResponse(name, context)`. All 7 calls in `web_ui/main.py` broke
  (context dict used as template name → jinja `LRUCache` TypeError). Fixed to `TemplateResponse(request,
  name, ...)`. Restored 24/24. (commit `9f8d104`)
- **A1 · Web map view** (`a9e9630`) — `PlaceDB.map_cells()` (every named/swept cell + landmark count);
  `web_ui` `GET /map` + `GET /api/map`; `map.html` renders the bounded grid as a CSS grid colored
  **named / swept / unexplored**, **polls `/api/map` every 3s so you watch cells fill in live**, with a
  built-out % + per-cell hover. Missing DB → empty map (a GET never spawns a blank world). Map nav link
  added. Test: `test_map_view.py`.
- **A2 · Staleness / #11.3** (`f6ca7cd`) — `PlaceDB.is_stale(col,row,max_age,now=)` + a new `updated_at`
  (real UTC) column stamped on name/sweep; `map_cells(max_age_seconds=,now=)` adds a `stale` flag; the map
  shows stale cells (red ring + count, ">24h"). **Basis = real wall-clock** (the `WorldClock` resets each
  run → sim-time can't express cross-run age).
- **A3 · Restart from morning** (`b337a24`) — `AgentManager.restart_day()` (stop if running →
  `WorldClock.reset()` to morning → clear each agent's runtime state so fresh schedules regenerate;
  **preserves memories + place cells**, unlike `reset_agents`). Exposed end-to-end: runner `POST /reset_day`
  → `RunnerClient.reset_day()` → web `POST /api/sim/reset_day` → **☀ Restart day** cockpit button. Tests:
  `test_runner_api.py`, `test_sim_controller.py`, new `test_world_clock.py`.
- **Backlog groom + direction reset** (`ffb5ad2`, `7088e9b`, `ec8e494`, `f969092`, `6a75e20`) — added the
  **"▶▶ Staged plan" (A loop-safe / B work-together)**, added **#11** (grid-cell place-cell model) and
  **#6b** (APC-generated top-down map via lizard brain), retired the "maintenance APC" framing in #7.
- **Housekeeping** (`96e5729`) — gitignored the intentional `.mcp.json.disabled` so the loop preflight
  reads a clean tree.
- **Memory:** added `project_maintenance_apc_retired.md` (+ MEMORY.md index line).

## In flight

_None._ Tree clean, all work committed on `auto-loop/backlog` (unpushed). Suite 26/26, preflight green.

## Decisions locked in this session

- **Direction reset:** wind down grid/place cell design (debug + finish), then **web observability**
  (view sim progress + watch grid/place cells build out). Next things named by user: **child BPs**,
  **restart from morning**, **web-app sim viewing**. (Don't re-litigate.)
- **#10.5 = BALANCED reaction gate** — routine destination wins; only a *known friend* or *being spoken to*
  interrupts, then resume. Persona distractibility is a low-weight nudge, never an override. (Live tune = B3.)
- **"Maintenance APC" retired** — no dedicated no-personality worker role; **any APC builds a community
  place cell when it needs one**. Sweep *mechanics* (`cell_sweep.py`, `mark_swept`) stay; the `role:
  "maintenance"` gating gets collapsed (A4/#11.1). See memory `project_maintenance_apc_retired`.
- **#11.2 APC-owned place cell = a named ~3 m bounding box**, positioned by **XY offset from the grid
  cell's community place cell**; **navigation is grid-first** (route grid→grid coarse, then fine-approach
  the place cell in the target grid). User hedged "maybe too complicated" — grid-first nav is the
  load-bearing idea; the owned-cell schema is secondary/uncertain.
- **#11.3 staleness re-observation = SHELVED** ("maybe we don't need this now"). The A2 *signal* stays;
  don't build the re-observe/refresh path.
- **#6b (APC top-down map) = captured, do NOT implement yet** (design questions parked).
- **A2 staleness basis = real wall-clock** (WorldClock resets each run).
- **Push:** user is away → follow the loop convention (commit on green, **never push**), not the
  "present + directing" push stance. Work waits on the branch for review.

## Open questions

- **A5 / #11.2 owned-cells schema** — user is uncertain it's needed ("maybe too complicated") and
  soft-deferred it behind #6b. Don't build unattended; decide with the user whether grid-first nav needs
  the full owned-place-cell store or something lighter.
- **A4 / #11.1 role collapse** — the *decision* is made (retire the role), but the replacement behavior
  (any APC detours to sweep a needed uninitialized cell) is a live decide-loop change needing PIE verify;
  the only offline slice would be an unused/speculative function. Needs a live session.

## Next concrete step

Launch PIE (one editor, the user's), start the sim runner (`start_sim.bat` or `python sim_runner.py`),
open the web cockpit, and do **B2**: run the sim and **watch grid/place cells build out live on the new
`/map`** — confirm named/swept coloring, the built-out %, and A2 stale flags behave against a real run.
(Alternatively B1: create `BP_Dufus`/`BP_Maren` child Blueprints + rebind — editor + mesh choice.)

## Files touched this session

- **new:** `Python/web_ui/templates/map.html`, `Python/scripts/agent_runtime/test_map_view.py`,
  `Python/scripts/agent_runtime/test_world_clock.py`, `plan/handoffs/HANDOFF_2026-07-01_1143.md`,
  memory `project_maintenance_apc_retired.md`
- **changed (runtime):** `Python/agent_runtime/place_db.py` (map_cells/is_stale/updated_at),
  `Python/agent_runtime/world_clock.py` (reset()), `Python/agent_runtime/agent_manager.py` (restart_day),
  `Python/agent_runtime/runner_app.py` (/reset_day), `Python/agent_runtime/runner_client.py` (reset_day)
- **changed (web):** `Python/web_ui/main.py` (TemplateResponse fix, build_map + /map + /api/map +
  /api/sim/reset_day), `Python/web_ui/templates/base.html` (Map nav link),
  `Python/web_ui/templates/sim.html` (Restart day button)
- **changed (tests):** `Python/scripts/agent_runtime/test_runner_api.py`,
  `Python/scripts/agent_runtime/test_sim_controller.py`
- **changed (docs/config):** `plan/backlog.md` (big groom + staging + decisions), `.gitignore`
  (.mcp.json.disabled)
- **memory (outside repo):** `project_maintenance_apc_retired.md` + `MEMORY.md`

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is:
> launch PIE + the sim runner + web cockpit and do B2 — run the sim and watch grid/place cells build out
> live on the new `/map` (confirm named/swept coloring, built-out %, and A2 stale flags), or start B1
> (child BPs `BP_Dufus`/`BP_Maren` + rebind).

## Git state at handoff

```
(clean — all committed; .mcp.json.disabled now gitignored)
```

Recent commits:

```
6a75e20 backlog: autonomous-loop run note — loop-safe queue drained, stops here
96e5729 gitignore: ignore local .mcp.json.disabled (MCP restore point)
f969092 backlog: shelve #11.3; define #11.2 (named 3m box, XY offset, grid-first nav)
ec8e494 backlog: add #6b — APC-generated top-down map via lizard brain (chart a course)
7088e9b backlog: record A1-A3 landed; flag A4/A5 decisions for joint session
b337a24 A3: restart the sim from morning (fresh day, keep the world)
f6ca7cd A2: grid/place staleness (#11.3) on real wall-clock basis
a9e9630 A1: web map view — watch grid + place cells build out
ffb5ad2 backlog: groom + direction reset (2026-07-01)
9f8d104 web_ui: fix TemplateResponse for Starlette 1.0 (request-first signature)
```
