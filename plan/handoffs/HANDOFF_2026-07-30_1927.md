# Handoff — SR32: first working Sonnet 5 run; ThinkingBlock crash fixed; FOOTING verified live

**Date:** 2026-07-30 19:27 local
**Branch:** `main`
**Prior handoff:** [HANDOFF_2026-07-30_1828.md](HANDOFF_2026-07-30_1828.md)

## TL;DR

The survey-only Dufus refocus is **verified live and everything is committed and pushed** (`e496cca`,
tree clean). Last session's CLI crash lost nothing (all work was already pushed as `c4671e6`), but its
live run SR31 had silently failed every LLM call with `'ThinkingBlock' object has no attribute 'text'`
— Sonnet 5 puts a ThinkingBlock before the TextBlock and both Anthropic call sites read
`content[0].text`. Fixed both, then the user ran **SR32 (49 ticks, zero LLM errors): 6 new survey
composites, contiguous westward push, zero daily-life detours, and FOOTING observed working — the LLM
itself turned back from corn/lawn/interior/pitch-black views.** User's framing of the next phase:
"the LLMs are thinking; we just need to get that thinking into believable actions."

## What was done this session

- **Diagnosed the previous session's end state**: the CLI crash cost nothing — handoff written 18:28,
  work committed+pushed 18:30 (`c4671e6`). The reboot fixed the 55557 bridge; SR31 (18:35, 8 ticks)
  ran but **every LLM call failed**: `'ThinkingBlock' object has no attribute 'text'`.
- **Fixed the ThinkingBlock crash** (root cause: Sonnet 5 — Dufus's new tier-1 model — returns a
  ThinkingBlock before the TextBlock; Haiku didn't):
  - `Python/agent_runtime/llm_router.py` `_anthropic_call` — joins all `type == "text"` blocks,
    raises `ValueError` listing block types if none (fail loud).
  - `Python/agent_runtime/perception.py` `_perceive_anthropic` — same fix.
  - `Python/scripts/agent_runtime/test_vision_perceiver.py` — `_FakeContent` now carries
    `type = "text"` to match the real SDK shape.
  - Suite **55/55** after the fix.
- **User ran SR32 live** (19:08–19:17, 49 ticks, 562s, Dufus only) and this session analyzed it:
  - **Zero LLM errors** — first run where Sonnet 5 actually drove Dufus.
  - **6 new survey composites**: cells 5,5 (rev 3) / 5,6 / 4,6 / 4,5 / 3,5 / 3,6;
    `place_images` 10 → 16 rows in `world_places.db`. Contiguous westward push from Utility Pole
    Forest. Zero home/square/social detours in the whole run.
  - **FOOTING/PROGRESS WARNING verified working live** (open question carried since 07-29 is
    answered): the LLM's own decisions include "Whoops, I'm standing in a corn field — cultivated
    ground isn't for surveying feet", "Grass under my boots — manicured lawn, not proper path",
    "Pitch black scene ahead with FOOTING other — turning back", and reasoning its way out after
    clipping into a building interior ("fancy curtained stage room").
  - Survey-narration guardrail caught and dropped 2 fabricated capture claims.
- **Backlog updated** (`plan/backlog.md`): SR32 results note added under "Now"; two watch items
  recorded (footing ping-pong; bridge reconnect log noise — see Open questions).
- **Committed and pushed everything** as `e496cca` (fix + test stub + backlog + a small
  `dufus/state.json` runtime-field cleanup the sim itself wrote). Working tree is clean.

## In flight

_None._ — the tree is clean, the suite is green, and the live verification that was blocking the
previous session is done.

## Decisions locked in this session

- **Anthropic response parsing must never assume `content[0]` is text** — extract text blocks by
  type and fail loud when absent. Applies to any future Anthropic call site.
- **The footing ping-pong (if it recurs) gets louder facts, not a code blocker** — per the user's
  standing "facts not blocking" rule; e.g. "you have retreated from this spot N times".
- **Next phase framing (user's words): the LLMs are thinking — the work now is turning that thinking
  into believable actions.** Grounded, embodied behavior over new subsystems.

## Open questions

- **Footing ping-pong near the farmhouse:** SR32's last ~4 decisions (19:17) were all "standing in
  grass — turn back the way I came"; retreat kept landing him back in grass. Run stopped before we
  could see if he'd break out. Watch for it in the next run; remedy is louder facts (see Decisions).
- **Bridge reconnect noise:** ~400 `Existing connection failed: 'NoneType' object has no attribute
  'sendall'` warnings in SR32 — the socket client rebuilds the connection around every command.
  Harmless (every command succeeded) but it is most of the log volume and buries real warnings.
  Logged as a cleanup advisory in the backlog; not scheduled.
- **What does "believable actions" need next?** Candidates already in the backlog: #19 keep APCs on
  sidewalks/roads, #26 dead-end recognition, #27 navigation executive, #20 movement pacing. Not
  chosen yet — user decision.
- Carried: #50 Sonnet A/B evaluation (SR32 is the first Sonnet baseline), #49 capture-pose metadata
  (needs user go-ahead), #53 design, "Reset agents" was never pressed (SR32 ran fine with old
  memories anyway — Relevant Past Moments interference not observed, but also not looked for).

## Next concrete step

Run a longer survey expedition (same recipe: Unreal PIE → `Python\start_sim.bat` → Start) and watch
whether coverage keeps growing past the western cluster and whether the farmhouse footing ping-pong
recurs; if it does, add a retreat-count fact to the FOOTING facts in the prompt assembly rather than
any code blocker.

## Files touched this session

- `Python/agent_runtime/llm_router.py` — `_anthropic_call` extracts text blocks by type, fails loud
- `Python/agent_runtime/perception.py` — `_perceive_anthropic` same fix
- `Python/scripts/agent_runtime/test_vision_perceiver.py` — fake content block carries `type="text"`
- `Python/worlds/MCP_World/agents/dufus/state.json` — runtime dropped 4 legacy fields (sim wrote it)
- `plan/backlog.md` — SR32 results under "Now"; reconnect-noise cleanup advisory
- `plan/handoffs/HANDOFF_2026-07-30_1927.md`, `plan/handoffs/LATEST.md` — this handoff

All committed and pushed as `e496cca`.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: run a longer survey expedition (PIE → `Python\start_sim.bat` → Start), watch coverage
> growth and whether the farmhouse footing ping-pong recurs; if it recurs, add a retreat-count fact
> to the FOOTING facts, not a code blocker.

## Git state at handoff

```
(clean — nothing modified, staged, or untracked)
```

Recent commits:

```
e496cca fix: extract text blocks from Anthropic responses (ThinkingBlock crash)
c4671e6 refactor: Dufus is the surveyor — survey-only agenda, Maren parked, dead code removed
42fdd59 feat: recognition, agenda ledger, durable runner log, guardrail facts
ab60c71 feat: add deterministic APC agendas
e702dd8 docs: approve JSON APC agenda design
```
