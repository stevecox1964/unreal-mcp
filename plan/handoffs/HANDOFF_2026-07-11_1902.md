# Handoff — New architectural eyes and a project-native Codex workflow

**Date:** 2026-07-11 19:02 local
**Branch:** `auto-loop/backlog`
**Prior handoff:** [HANDOFF_2026-07-09_2032.md](HANDOFF_2026-07-09_2032.md)

## TL;DR

Codex completed a read-only architectural scan, identified two control models fighting inside navigation (semantic routed travel versus frame-by-frame directional steering), and proposed making the AI the executive while a deterministic lizard brain owns movement continuity. No product behavior changed. Five project-local Codex workflow skills and a revised autonomous-loop contract are uncommitted; the next project session should groom the backlog before approving or implementing the architectural proposal.

## What was done this session

- Scanned the repository read-only: Unreal 5.5.4 plugin, standalone Python runner, FastAPI cockpit, TCP bridge, persistent world/agent state, tests, backlog, specs, and recent handoffs.
- Identified concrete correctness risks for later backlog consideration:
  - cockpit world/agent path values are not contained before filesystem access or recursive delete;
  - manual tick/pulse calls can overlap the automatic loop while it awaits LLM work;
  - zero or negative `tick_seconds` can create a tight loop;
  - `RunnerClient` uses `httpx` without declaring it directly.
- Formed an architectural recommendation, not yet an approved implementation plan:
  - the model chooses durable semantic intentions;
  - a persistent navigation ticket tracks destination, route leg, progress, retries, arrival, and failure;
  - the lizard brain owns collision response, personal space, stuck/dead-end recovery, and movement continuity;
  - an engine-neutral embodiment interface isolates Unreal as the first adapter;
  - grids index space/exploration, place cells establish semantic arrival, and landmarks provide authored anchors;
  - a tiny headless reference world should prove the navigator works without Unreal.
- Added and validated five project-local Codex skills under `.agents/skills/`:
  - `read-handoff`
  - `create-handoff`
  - `add-backlog-item`
  - `groom-backlog`
  - `autonomous-loop`
- Rewrote `plan/autonomous_loop.md` so the durable workflow is: read handoff, create handoff, add backlog item, groom backlog. The backlog is the roadmap; handoffs are session snapshots.
- All five skill packages passed the Codex skill validator. `git diff --check` passed. Product tests were not run because only planning and skill files changed.

## In flight

- The five new `.agents/skills/*` directories, revised `plan/autonomous_loop.md`, and this handoff are uncommitted.
- The architectural proposal has not been added to or reconciled with `plan/backlog.md`; it must be groomed against existing items #17, #19, and #26 before becoming executable scope.
- This conversation is running from a different Codex workspace root. The user plans to open the existing `unreal-sim` repository as its own Codex project so project-local skills and `AGENTS.md` are discovered naturally.

## Decisions locked in this session

- **Use the existing repository as a new Codex project; do not create or copy a new code repository.** The correct root is `C:\Users\user\Documents\Unreal Projects\AAAAAA_Unreal_SIM\unreal-sim`.
- **Preserve Claude and Fable's working system.** Architectural progress should be incremental and acceptance-driven, not a rewrite.
- **Backlog and handoff have different authority.** `plan/backlog.md` owns scope and priority; handoffs record chronological session state and one resume step.
- **The user's standard workflow phrases are durable triggers:** “Read hand off,” “Create handoff,” “Add item to back log,” and “Groom backlog.”
- **Autonomous work remains guarded:** dedicated `auto-loop/*` branch, failing offline test first, full suite green, small commits, never push/merge, never claim live verification from offline evidence.

## Open questions

- Should the architectural recommendation become one new umbrella backlog item or be folded into existing #17 (grid-first routing), #19 (roads/sidewalks), and #26 (dead-end recognition)? Decide during backlog grooming.
- Which of the four correctness findings should be promoted ahead of navigation work? Tick serialization is the most directly related to reliable unattended runs, but no priority was explicitly approved.
- Should the current workflow/skill changes be committed as one project-process commit after review? They are intentionally left uncommitted.

## Relevant backlog items

- **#17 Grid-first navigation:** routed semantic travel exists, but the latest prior handoff says the travel prompt still encourages direction steering that bypasses it.
- **#19 Keep APCs on sidewalks and roads:** likely belongs inside the navigator/lizard-brain boundary rather than engine-specific patches.
- **#26 Dead-end recognition:** natural responsibility of deterministic progress/recovery state.
- **#4 Autonomous building loop:** the rewritten run contract and new skills directly update its process surface.
- The current backlog does not yet contain the proposed persistent navigation ticket, embodiment port, or headless reference-world acceptance test as approved scope.

## Next concrete step

Run **“Groom backlog”** in the correctly rooted Codex project to reconcile the architecture proposal and correctness findings with backlog items #17, #19, #26, and #4 without implementing code.

## Files touched this session

- `.agents/skills/read-handoff/SKILL.md` and `agents/openai.yaml` — load and summarize the latest project session safely.
- `.agents/skills/create-handoff/SKILL.md` and `agents/openai.yaml` — save a precise session snapshot without duplicating the roadmap.
- `.agents/skills/add-backlog-item/SKILL.md` and `agents/openai.yaml` — capture scope, deduplicate, classify, and avoid silently prioritizing it.
- `.agents/skills/groom-backlog/SKILL.md` and `agents/openai.yaml` — reconcile evidence and maintain a compact active view.
- `.agents/skills/autonomous-loop/SKILL.md` and `agents/openai.yaml` — execute approved loop-safe work test-first and hand off at the end.
- `plan/autonomous_loop.md` — replaced the contradictory historical contract with the Codex workflow and safety rules.
- `plan/handoffs/HANDOFF_2026-07-11_1902.md` — this handoff.
- `plan/handoffs/LATEST.md` — advanced to this handoff.

## How to resume

Open `C:\Users\user\Documents\Unreal Projects\AAAAAA_Unreal_SIM\unreal-sim` as the Codex project, then say:

> Read hand off.

After reviewing the resume brief, the intended next command is:

> Groom backlog.

## Git state at handoff

Before writing this handoff:

```text
## auto-loop/backlog...origin/auto-loop/backlog
 M plan/autonomous_loop.md
?? .agents/skills/add-backlog-item/
?? .agents/skills/autonomous-loop/
?? .agents/skills/create-handoff/
?? .agents/skills/groom-backlog/
?? .agents/skills/read-handoff/
```

This handoff and the `LATEST.md` update are additional uncommitted changes.

Recent commits:

```text
0524b0e handoff 2026-07-09 + backlog #26: Maren stays put; onboarding #13.1-13.3 landed
2e7e52a backlog: tick #13.3 (merged, 42/42) + note stale maren seed caveat
0648b10 #13.3: cockpit deep-reset buttons
1ec969e backlog #13.3 (spec): cockpit buttons for reset_agents/reset_places
de31a4e fix: requires-python >=3.12 (PEP-701 f-string in llm_router) + tick #13.1/#13.2
```
