# Handoff — Agent activity bubble + APC rename + cloud switch; ready to push

**Date:** 2026-06-24 22:44 CDT
**Branch:** main
**Prior handoff:** [HANDOFF_2026-06-24_1113.md](HANDOFF_2026-06-24_1113.md)

## TL;DR

Shipped four committed changes this session (place-cell DB reset, observing/thinking
activity pipeline, `MCPCharacterComponent → APCCharacterComponent` rename, runtime-JSON
gitignore) plus a started rolling backlog. Working tree is **clean and 4 commits ahead of
`origin/main` — ready to `git push`.** Next work item is **Child Blueprints for per-agent
meshes** (`BP_Dufus`/`BP_Maren`). The `.env` is switched back to **cloud** (Haiku decisions,
Gemini vision); local Ollama settings preserved beneath for later.

## What was done this session

- **`plan/backlog.md`** — new rolling backlog (add/remove items across sessions). Holds the
  three carried-over projects (named-place nav, settings page + rename, independent lifetime),
  a "Recently landed" log, a **▶ Next up: Child Blueprints** item, and a "Later/ideas"
  hybrid-provider note.
- **`reset_world_places()`** (commit f1680d4) — `PlaceDB.reset()` wipes the shared
  `world_places.db` (place_cells, place_observations, agent_visits) for a true blank world;
  complements `reset_agents` (which preserves the map). New MCP tool + manager method.
- **Agent activity display pipeline** (commit 83cc55b) — `observing`/`thinking` now push to
  each NPC's `AIState` from the **sequential** tick phases only (`_set_activity` +
  `bridge.set_ai_state`; "observing" before each observe, "thinking" before the parallel
  decide batch — never from the parallel phase, per the single-socket rule). Also fixed a real
  bug: `create_umg_widget_blueprint` sent `widget_name` but the C++ wants `name`.
- **Status bubble (in-editor, NOT in git)** — user added a **Text Render component to
  `BP_CameraNPC`**, bound to `AIState`, showing the word above each NPC's head. Verified live.
- **Rename `MCPCharacterComponent → APCCharacterComponent`** (commit 9755f4a) — class + files +
  `GetMCPComponent→GetAPCComponent` + error strings + `ClassGroup`/categories; `[CoreRedirects]`
  in `DefaultEngine.ini` keeps existing Blueprints' component intact. Verified after the user's
  C++ rebuild via `get_character_status` (returned full state, not "No component").
- **Provider switch back to cloud** — `.env` (gitignored): `LLM_PROVIDER=anthropic`
  (`claude-haiku-4-5-20251001`), `VISION_PROVIDER=gemini` (`gemini-2.5-flash-lite`); applied live
  via `reload_llm_environment` (no restart). Verified vision reads provider/model at runtime.
- **Gitignore runtime churn** (commit 4b1b928) — untracked `memory.json` + `spatial_map.json`
  under each agent (kept on disk); `state.json` stays tracked (loader requires it; holds
  blueprint_class/tier/start_location).
- **Verification runs** — local first tick was ~215s (cold qwen). Cloud run: **7 ticks / 67s
  (~9.5s/tick)**; caught Maren `observing` and Dufus `thinking` live; agents interacted (Dufus
  walked toward village square greeting Maren).

## In flight

_None._ Everything is committed; working tree is clean. The only uncommitted-by-design file is
`Python/.env` (gitignored, holds keys + the cloud switch).

## Decisions locked in this session

- **Display = Text Render on the `BP_CameraNPC` base, bound to `AIState`** — not UMG. The MCP
  UMG tooling is broken at the C++ level (`create_umg_widget_blueprint` builds a generic
  `UBlueprint` then casts to `UWidgetBlueprint` → always null; also ignores `path`/`parent`).
  TextRender needs no separate asset and no fragile graph wiring.
- **Component renamed to APC**; **module/plugin stay `UnrealMCP`** for now (bigger separate job).
- **Child Blueprints (not full copies)** for per-agent meshes — base holds shared logic + bubble.
- **`.env` back to cloud** (Haiku + Gemini); full-local stays the long-term goal.
- **Gitignore `memory.json`/`spatial_map.json`; keep `state.json` tracked** (it's required config).

## Open questions

- **C++ UMG create handler is broken** (see above). Left unfixed — we went TextRender. Worth a
  fix only if we later want UMG bubbles built via MCP. Logged here, not in backlog.
- **`state.json` still re-dirties each run** (current_goal/last_tick_time/bound_*). Accepted
  trade-off; the proper fix (split config vs runtime) is a "Later/ideas" backlog option.

## Next concrete step

`git push` the 4 commits, then start **Child Blueprints**: create `BP_Dufus` and `BP_Maren` as
**children of `BP_CameraNPC`** overriding only the skeletal mesh; then rebind agents — today
`dufus`→`BP_CameraNPC_C_1`, `maren`→`BP_CameraNPC_C_0` via each `state.json`'s
`blueprint_class`/`unreal_actor_name` (or replace the placed actors). The bubble + component live
on the base, so children inherit them.

## Files touched this session

- `plan/backlog.md` — **new** rolling backlog (+ Child-BP next item, hybrid-provider idea)
- `Python/agent_runtime/place_db.py` — `PlaceDB.reset()`
- `Python/agent_runtime/agent_manager.py` — `reset_world_places()`, `_set_activity()`, tick wiring
- `Python/agent_runtime/unreal_bridge.py` — `set_ai_state()`
- `Python/tools/simulation_tools.py` — `reset_world_places` MCP tool
- `Python/tools/umg_tools.py` — send `name` for widget create
- `MCPGameProject/.../APCCharacterComponent.{h,cpp}` — renamed from MCPCharacterComponent
- `MCPGameProject/.../UnrealMCPCharacterCommands.{h,cpp}` — APC rename
- `MCPGameProject/Config/DefaultEngine.ini` — `[CoreRedirects]`
- `Python/tools/character_tools.py`, `README.md`, `MASTER_PLAN.md`, `still_todo.md`,
  `Docs/character_system.md` — APC doc/docstring updates
- `.gitignore` — ignore per-run `memory.json`/`spatial_map.json`
- `Python/.env` — cloud providers (gitignored; not committed)
- **In-editor (not in git):** `BP_CameraNPC` gained a Text Render component bound to `AIState`

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next
> concrete step is: `git push` the 4 ready commits, then build Child Blueprints `BP_Dufus`/
> `BP_Maren` as children of `BP_CameraNPC` (mesh override only) and rebind the agents.

## Git state at handoff

```
(clean working tree)
```

Ahead of `origin/main` (to push):

```
4b1b928 Gitignore per-run agent memory.json/spatial_map.json; track backlog
9755f4a Rename MCPCharacterComponent -> APCCharacterComponent
83cc55b Push observing/thinking activity to NPC AIState for above-head display
f1680d4 Add reset_world_places to wipe shared place-cell DB
```
