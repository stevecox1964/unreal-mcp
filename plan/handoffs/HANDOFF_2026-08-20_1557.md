# Handoff — #86 adaptive step length built and proven live in SR49; the blocker is now #81's body-width raster

**Date:** 2026-08-20 15:57 local
**Branch:** main
**Prior handoff:** [HANDOFF_2026-08-20_1247.md](HANDOFF_2026-08-20_1247.md)

## TL;DR

#86 (adaptive step length + the walk plan) is **built, committed, and verified live in SR49** — Dufus
crossed **89.7 m in one order, dead on the line**, in six free bridge-only hops. The SR47 navmesh-detour
bug is gone and locomotion cost dropped ~6x on travel ticks. The run then stalled for a different
reason: **Dufus ping-ponged NE↔SW three times, 30 m each way, around a dumpster**, because #81's ray
raster is only as wide as his own body (±34 cm), so it reports `open=none` when the pavement 1.5 m
either side is clear. Next session: give the APC a real "where can I get around this" fact — the
no-rebuild route is `get_character_forward_volume`'s existing `yaw_offset_deg`.

## What was done this session

- **Specced #86 and #87** (`plan/backlog.md`, commit `524ed6d`). #86 = adaptive step length; #87 =
  analysis of "should the lizard brain be an LLM", parked behind the overnight run.
- **Built #86 v1** (`8290797`): new pure module `Python/agent_runtime/move_plan.py` (`plan_step`), plus
  `AgentManager._scan_ahead`, `_plan_move`, `_direction_yaw`. The step shrinks toward refused ground /
  no-go patches / body-box contact and grows across cells with proven good footing. `walk_to`/`wander`
  gained an optional coarse `"distance": "close|normal|far"`. The length actually taken is stated back
  to the model next tick via `last_move.plan.why`.
- **Ran SR47, killed at 5 ticks by the user** ("erratic... came back to starting point and went into
  motor home"). Three findings, all fixed in `0b842b2`:
  - **A step is a straight line; the engine walks a navmesh PATH.** Ordered north 45 m twice, Dufus
    arrived 52 m east then 48 m west — routed around a building both times.
  - **Nothing ever said he had not gone where he was sent.** Added `last_move.went` — the achieved
    heading when it is >45° off the ordered one (`DRIFT:` in the log).
  - **#81's `open_columns` named gaps that were not gaps** — the engine counts a column open if ANY of
    its three rows is clear. A sedan 17 cm into `far_right` at body height was reported as a gap.
    `_open_columns` in `agent_manager.py` now requires the whole column clear, and reports that it
    **cannot name a side** when the capsule says "does not fit" but no ray was struck. Python-side.
- **Built the walk plan** (`944ed8a`) — the real fix. `move_plan.leg_distances` cuts a grown step into
  hops of one nominal step; only the first is ordered. `AgentManager._pulse_walk` walks the rest, one
  hop per tick, **bridge only — no observe, no perceive, no model call**, in a new tick phase beside the
  survey sweep. `MAX_STEP_CM` back to 9000 (three cells).
- **Ran SR49: 22 ticks, 237 s, zero errors.** Results below.

### SR49 — what the log proves

**Working, verified live:**

- `move plan: east 90.0 m (wanted 90.0 m, grew)` → `walk plan: east 90.0 m in 6 hops of 15 m` →
  `hop 2/6` … `hop 6/6` → `walk plan ended: all hops walked`. The decision log records
  **`moved 8971.4` cm ending at y = -800.0, the exact y he started on.** Dead straight, no detour.
- **Cost:** walking ticks ran **1.66–1.89 s**; decision ticks ran **10–24 s**. A 90 m crossing of proven
  ground is one paid decision and five near-free ticks.
- **Zero `DRIFT:` lines, zero `STALLED` lines** across the whole run. SR47's failure did not recur.
- `move plan: northeast 38.0 m (wanted 45.0 m, capped by refused ground)` — the shrink half works, and
  the refusal Dufus filed himself at tick 10 is what capped it.
- `blocker: prop 352 cm ahead ('trash_bin_bigOpen6') [fits=False open=none]` — honest. In SR47 the same
  class of obstacle printed `fits=False` with all five columns "open".

**Not working — the bounce:**

Dufus spent ticks 8–22 oscillating between two points ~30 m apart at the village square:

| tick | order | outcome |
|---|---|---|
| 8 | NE 45 m, 3 hops | `walk plan ended: prop 352 cm ahead` (`trash_bin_bigOpen6`, `open=none`) |
| 10 | `refuse_cell` patch NE | "green dumpster completely blocking the path, no side open" |
| 11 | SW 78 m, 6 hops | `walk plan ended: obstacle 336 cm ahead` (`lantern8`) after hop 2, 29.6 m |
| 14 | NE 36 m, 3 hops | `walk plan ended: prop 356 cm ahead` — back at the dumpster |
| 17 | SW 78 m, 6 hops | `walk plan ended: obstacle 331 cm ahead` — back at the lantern |
| 20 | NE 38 m, 3 hops | `walk plan ended: prop 358 cm ahead` — the dumpster again |

Positions confirm a clean ping-pong: `(-456.3, -1832.4)` ↔ `(-2552.7, 264.1)`, three round trips.
His own words at tick 20: *"I've bounced here too many times"* — so #26's bounce fact reached him and he
still had nowhere to go.

**Root cause, confirmed in the C++.** `UnrealMCPCharacterCommands.cpp:346` computes the raster column
offset as `Right * (ColT * Radius)` with `Radius = 34` cm. **The scan spans exactly the body's own
width — ±34 cm.** `far_left` is 34 cm left of centre. So `open_columns` can only ever say "the gap is
inside my own shoulders", which is never useful for stepping around anything. Dufus was told
"no side is open" while clear pavement sat a metre either side, and the only move left to him was to
turn around. **The bounce is not a reasoning failure; it is a missing fact.**

**Maren did nothing all run.** 22 ticks, `moved 0.0`, 21 of them `idle(scene_unchanged)` with
`settled routine sampled; VLM sleeping`. She woke at the mobile home community believing the schedule's
claim that it is her truck post (*"Schedule says this spot counts as my truck post despite the mobile
home visuals"*), the settled-agent gate then suppressed cognition, and she never moved again. This is
#75/#71 (place identity) plus the settled gate compounding into a fully asleep APC.

## In flight

_None._ Everything built this session is committed on `main` (`524ed6d`, `8290797`, `0b842b2`,
`944ed8a`). The working tree holds only pre-existing noise: `D AGENTS.md`, `M README.md`,
`?? CLAUDEOLD.md` — none of it touched this session.

## Decisions locked in this session

- **The move plan lives in the lizard brain, not in the prompt.** The model says only *which way*; the
  distance is computed from PlaceDB and the body probe. Per the user: *"Too much rule tweaking. Make
  the lizard brain figure it out."*
- **A grown step is never handed to the engine whole.** It is cut into hops of one nominal step and
  walked one hop per tick with no model call. The engine only ever sees a distance the fixed 15 m step
  always walked correctly. This is what makes `MAX_STEP_CM = 9000` safe — not the number itself.
- **The spec's "cap at the remaining distance to goal" was deliberately not built.** The goal-bearing
  paths (routed legs, sweep centres) walk to exact coordinates and never overshoot; the paths that DO
  overshoot are direction words, whose real goal is the refusal or blocker ahead.
- **Five things end a walk plan** (each buys a full cognition tick): something ahead the body must
  reckon with, drifting >7 m off the ordered line, anyone arriving/leaving nearby, a hop making no
  ground for `_STUCK_TICKS`, and a hard 12-tick ceiling. A survey outranks a walk; an operator pulse
  ends the plan, because "pulse" must still mean "think now".
- **#87 (lizard brain as an LLM) stays analysis-only, parked behind the overnight run.** It doubles
  per-tick cost, and if a model is ever needed there the residual questions are *visual*, so it would be
  a VLM. If built, it may **shape** a move (shorten, nudge) but never **refuse** one.
- **Speed mode still applies** — no tests written or run. Everything untested is listed in the backlog's
  *Needs tests* ledger.

## Open questions

- **How to give the APC a usable "where is the way around" fact.** Two routes:
  - **(a) No rebuild — probe at angles.** `get_character_forward_volume` already accepts
    `yaw_offset_deg`. When `fits=False`, fire it at ±45° (and maybe ±90°) and report which *headings*
    the body fits along: *"you do not fit ahead; you do fit to your left."* Four extra bridge calls, and
    only on blocked ticks. Testable today.
  - **(b) Rebuild — widen the raster.** Add a `scan_width_cm` (default ~3× body radius) so
    `open_columns` means something outside the shoulders. Cleaner and one call, but it is C++ and needs
    another editor rebuild.
  - Recommendation: **(a) first** — it is buildable and testable now, and it will show whether the
    angled fact is what actually ends the bounce before committing to a rebuild.
- **What to do about Maren.** She is fully asleep: the schedule asserts she is at her post, the place
  under her feet disagrees, and the settled gate then suppresses cognition indefinitely. Is the fix in
  #71 (place names that survive being used by someone else), in the settled gate, or in #63's
  fail-loud preflight? Not decided; not attempted this session.
- **Should a walk plan abort on `fits=False` at 3.5 m?** Every SR49 plan after the first died on a prop
  within 5 m of the path. Aborting is correct while "no side is open" is the only alternative fact; it
  may be too eager once (a) or (b) lands.

## Next concrete step

Implement option (a): in `AgentManager._observe_agent`'s blocker branch
(`Python/agent_runtime/agent_manager.py`, around the `fits is not None` block at ~line 1630), when
`fits is False`, call `self._probe_ahead` variants at `yaw_offset_deg` of -45 and +45 via
`self.bridge.forward_volume(actor, distance_cm, yaw_offset_deg=...)` — the C++ handler already reads
`yaw_offset_deg` (`UnrealMCPCharacterCommands.cpp`) — and attach `observation["blocker"]["open_headings"]`
as a list of direction words the body fits along. Then render it in `llm_router._sense_note`'s
`fits is False` branch as *"your body does not fit straight ahead, but it DOES fit to your left"*,
replacing the current dead-end "no side is open" line when an angled probe succeeds.

## Files touched this session

- `Python/agent_runtime/move_plan.py` — **new**. `plan_step` (pure step sizing), `leg_distances` (hop
  splitting), the tuning constants (`NOMINAL_STEP_CM`, `MIN_STEP_CM`, `MAX_STEP_CM`, `PREFERRED_CM`,
  `STOP_SHORT_MARGIN_CM`).
- `Python/agent_runtime/agent_manager.py` — `_direction_yaw` split out of `_direction_target`;
  `_scan_ahead` (shared-map look down a heading); `_plan_move`; `_open_columns` (strict gap test);
  `_open_walk_plan` / `_has_active_walk` / `_end_walk_plan` / `_pulse_walk`; walk phase in `_tick_impl`;
  heading-drift fact in `_last_move_fact`; plan carried on the observation; new constants.
- `Python/agent_runtime/llm_router.py` — `distance` word added to the `walk_to`/`wander` action specs;
  new sense lines for the move plan, the heading drift, and the "scan could not tell which side" case.
- `plan/backlog.md` — #86 and #87 write-ups; SR47 findings; SR48/49 grading criteria; *Needs tests*
  entries; one cleanup advisory (`action["_resolved_target"]` never reaches its reader, because
  `_resolve_action_actor_refs` returns a copy).

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: implement angled body-box probes — when `fits is False`, probe `get_character_forward_volume` at
> `yaw_offset_deg` -45 and +45 and give the APC an `open_headings` fact ("you do not fit ahead, you do
> fit to your left"), because #81's raster is only as wide as the body and SR49 had Dufus bouncing 30 m
> back and forth around a dumpster with `open=none`.

## Git state at handoff

```
 D AGENTS.md
 M README.md
?? CLAUDEOLD.md
```

Recent commits:

```
944ed8a feat: #86 the walk plan - a grown step is walked hop by hop, no model call in between
0b842b2 fix: SR47 - cap grown steps at one cell, say when the walk drifted, stop naming gaps that are not gaps
8290797 feat: #86 adaptive step length - the lizard brain computes a move plan, not a constant
524ed6d spec: #86 adaptive step length (fixed 15 m step causes overshoot/oscillation), #87 lizard-brain-as-LLM analysis
fb84e65 feat: #85 shared doctrine, #81 body-box probe, #83 signal-based identity, #84 prompt payload contract (offline 64/64)
```
