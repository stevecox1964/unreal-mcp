# Handoff — the exit condition is set and Phase B is blocked; #85/#81/#83/#84 all built, offline 64/64

**Date:** 2026-08-20 12:47 local
**Branch:** main
**Prior handoff:** [HANDOFF_2026-08-19_1252.md](HANDOFF_2026-08-19_1252.md)

## TL;DR

The user set a hard exit condition for the whole navigation lane — **Dufus must run an unattended
overnight survey that fills the world with grid/place data other APCs consume, and yield a corpus
big enough to train a VLM** — and **blocked Phase B** (APCs walking around and talking) until it is
met. Four items were built against that: **#85** shared doctrine imports, **#81** the body-box
probe, **#83** identity from engine signals, **#84** the prompt payload contract. Offline suite is
**64/64**. The next session must **rebuild the UnrealMCP plugin in the editor** — #81 is C++ and
does nothing until compiled — then run SR47.

## What was done this session

- **Read SR46 and graded it.** 30 ticks, 616.8 s, two APCs, clean stop, **zero errors**.
  - **#61's reflex half verified live.** 17 `blocker:` lines, 2 reflex stops, and the point of the
    fix: **`veh_Van_6` reported at 400 / 37 / 36 cm**. Under the pre-2026-08-19 code that name
    matched no keyword, classified as `"obstacle"`, and was dropped — Dufus would have stood 36 cm
    from a van and been told nothing.
  - **The fail-loud classifier earned its keep on the first run:** exactly one unmatched actor,
    `receptionCounter_7`, logged with its raw name.
  - **Dufus: 4½ minutes trapped in cell (7,7)** bouncing between a van and a baseball field, exactly
    as the user reported watching. He refused three 9-m patches with accurate reasons and **kept
    landing back in them** — because a 9-m refusal cannot be obeyed with a **15-m step**, and a thin
    ray never says *where the gap is*. That is the argument for #81.
  - **Maren: 20 decisions moved her 0.0 cm**, then one fired and flung her ~28 m into the cornfield.
- **Confirmed the user's doctrine hypothesis by measurement.** `dufus/rules.md` was **94 lines**;
  `maren/rules.md` was **21**. `Agent.load` read one flat file per agent, no imports. **SR46
  11:45:58 is the smoking gun:** Maren decided *"time to refuse this ground"* and emitted `walk_to`,
  because nothing in her file said `refuse_cell` existed. All three refusals that run were Dufus's.
- **#85 shared APC doctrine — BUILT.** New `worlds/MCP_World/doctrine/` (`basics.md`, `ground.md`,
  `movement.md`, `obstacles.md`, `survey.md`, README). `resolve_rule_imports` in `agent.py` expands
  `@import doctrine/<file>.md` at load: literal inclusion, one level, no nesting, no path escape,
  **every failure raises**. **Maren went 21 → 100 lines** and now has refuse_cell, allow_cell,
  look-before-you-step, breadcrumbs, tried-headings and the bounce rule; she is still not told how
  to survey.
- **#81 body-box probe — BUILT (needs plugin rebuild).** New C++ `get_character_forward_volume`:
  capsule sweep on **`ECC_Pawn`** using the character's own `UCapsuleComponent`, lifted by its
  `MaxStepHeight` so a kerb is not called a wall, plus a **5×3 ray raster** returning
  `open_columns` / `open_rows` / `blocked_fraction`. `AgentManager._probe_ahead` prefers it and
  **falls back to the old ray with one loud warning per run** if the engine does not know the
  command. `fits` is `None` (never `False`) when nothing was measured.
- **#83 identity without author naming — BUILT.** `_classify_blocker` now resolves: actor **tags** →
  **is_pawn** → **physical material** → component/collision → the keyword table, last. An immovable
  `SkeletalMeshComponent` reads as **`figure`**, never `person`, so no APC greets a mannequin.
- **#84 prompt payload contract — BUILT.** New `agent_runtime/prompt_payload.py`: `ALLOWED_FIELDS`
  allow-list grouped self/place/senses/people/task, `project()`, `NESTED_STRIPS` (drops
  `blocker.actor_name|actor_class|signals` at the boundary while keeping them in the log),
  `check_clean()` which reports but never rewrites. The raw-dict dump is gone.
- **Coverage audit after the user asked whether #83 is fully contained — found a real gap.**
  `decide()` was guarded but **`orient()` (wake), `chat()` and `ask()` (planner) were not** — three
  of four prompt paths went to the model unchecked. All four now guarded.
- **Three new test files**, and both new suites were **proven falsifiable** by breaking the code in
  memory and confirming the checks fail.
- **Two real bugs found while testing:** a bare `@import` with no path was silently pasted into the
  prompt as a rule; and `test_prompt_context.py` read `rules.md` off disk, which is no longer what
  the agent receives.
- **Backlog rewritten:** new "THE EXIT CONDITION" section, Phase B blocked, items #81–#85 added and
  their Status lines updated to BUILT.

## In flight

_None._ All four items are complete, the suite is green, and the work is committed.

## Decisions locked in this session

- **THE EXIT CONDITION (user).** Done = an unattended overnight survey + reusable grid/place data
  other APCs consume + a VLM-grade corpus. SR45 being clean was **not** the finish line.
- **Phase B is BLOCKED** — #66/#67/#68 are not startable until the above is met. Do not re-litigate
  the 2026-08-12 direction reset; the user has re-decided on top of it.
- **Grade live runs on survey throughput, not on whether something social happened.** The
  2026-08-12 "grade on social" reframe is suspended.
- **Perception/locomotion is shared runtime, not Dufus code.** Other APCs never survey but walk on
  the same logic, so every fix here is a fix for every APC.
- **Doctrine vs character:** if the rule is true for any body with legs it is doctrine; if it is
  true because of who the character is it stays in `rules.md`. Character wins on conflict, and
  doctrine is resolved *before* the character's own lines so later text overrides.
- **#81 decisions taken:** capsule read from the engine (never hard-coded), `ECC_Pawn` not a custom
  channel, step-height allowance for kerbs, navmesh pairing deferred to #53.
- **#82 is absorbed into #84** as its engine-names clause.
- **Engine identity stops at the payload boundary**, but stays in the log — a bad classification
  must remain diagnosable.
- **Logs:** `sim_runner.log` is opened `mode="w"` and holds the **current run only**;
  `agent_decisions.log` is append-only and holds SR34→SR46. Default when reading is the latest run.

## Open questions

- **Does the level's art actually carry physical materials or tags?** #83's most robust path
  (path 1) is unverified — one inspection in the editor answers it. If the assets carry nothing, the
  keyword table stays load-bearing and the VLM-caption join (path 2) becomes the real fix.
- **#84 is half-done by design.** The two render paths in `decide()` were not merged into one, and
  there is no golden-prompt diff test. Merging them touches every prompt, so it was deliberately
  left until a live-run lane is not open. Still worth doing.
- **The old mis-filed survey rows** (from #55: `community:5:5` rev 4 shot from cell (5,6), named
  "Utility Pole Forest" while every revision describes a corn field) are still in the corpus and are
  now VLM training data. Purge or leave and re-survey? Carried from the last two handoffs, still
  needs a user decision.
- **Refusals smaller than the step length cannot be obeyed.** SR46 proved it. #81 gives the APC a
  named open side; whether it also needs a **short-step action** (a measured 2 m instead of a blind
  15 m) is not decided.
- **`AGENTS.md` (deleted) and `CLAUDEOLD.md` (untracked)** are still left exactly as they are, per
  the user's earlier instruction.

## Next concrete step

**Rebuild the UnrealMCP plugin in the Unreal editor**, then run SR47. `get_character_forward_volume`
is new C++ in
`MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/Commands/UnrealMCPCharacterCommands.cpp:209`
and does nothing until compiled — until then every APC silently uses the old single ray, which the
runtime announces once per run as `body-box probe (#81) unavailable`. Grep for that line first to
confirm the rebuild took.

## Files touched this session

- `Python/agent_runtime/agent.py` — `resolve_rule_imports`, `RulesImportError`, `Agent.load` now
  resolves imports against the world root (#85).
- `Python/agent_runtime/agent_manager.py` — `_probe_ahead` (volume probe + loud ray fallback);
  `_classify_blocker` rewritten to signals-first with `_TAG_CATEGORIES` / `_PHYS_MATERIAL_CATEGORIES`
  (#83); body-box facts on `observation["blocker"]`; `_volume_probe_unavailable` latch (#81).
- `Python/agent_runtime/llm_router.py` — the raw-dict dump replaced by `prompt_payload.project()`;
  `check_clean` on all four prompt paths; engine name removed from both system prompts and the
  "Nearby APCs" heading; action schemas ask for `<character name>`; the fit/gap sense lines (#81/#84).
- `Python/agent_runtime/prompt_payload.py` — **new**: `ALLOWED_FIELDS`, `NEVER_SEND`,
  `NESTED_STRIPS`, `project`, `dropped_fields`, `check_clean` (#84).
- `Python/agent_runtime/unreal_bridge.py` — `forward_volume()` (#81).
- `MCPGameProject/.../UnrealMCPCharacterCommands.cpp` — `HandleGetCharacterForwardVolume` +
  `FillHitIdentity`; new includes (#81/#83).
- `MCPGameProject/.../UnrealMCPCharacterCommands.h` — handler declaration.
- `MCPGameProject/.../UnrealMCPBridge.cpp` — command routed.
- `Python/worlds/MCP_World/doctrine/` — **new**: `README.md`, `basics.md`, `ground.md`,
  `movement.md`, `obstacles.md`, `survey.md` (#85).
- `Python/worlds/MCP_World/agents/dufus/rules.md` — 94 → imports + 5 character lines.
- `Python/worlds/MCP_World/agents/maren/rules.md` — 21 → imports + her own posture (100 resolved).
- `Python/scripts/agent_runtime/test_doctrine_imports.py` — **new** (#85).
- `Python/scripts/agent_runtime/test_body_box_probe.py` — **new** (#81/#83).
- `Python/scripts/agent_runtime/test_prompt_payload.py` — **new** (#84/#82).
- `Python/scripts/agent_runtime/test_blocker_sense.py` — updated for every-hit-is-a-fact + SR45 names.
- `Python/scripts/agent_runtime/test_prompt_context.py` — reads doctrine-resolved rules, not the raw file.
- `plan/backlog.md` — THE EXIT CONDITION, Phase B blocked, SR46 findings, items #81–#85.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: rebuild the UnrealMCP plugin in the Unreal editor so `get_character_forward_volume`
> exists, then run SR47 and check the log for `body-box probe (#81) unavailable` (should be absent),
> a `fits=false` line naming a real open side, Maren emitting `refuse_cell`, and zero `prompt leak`
> warnings.

## Git state at handoff

```
 D AGENTS.md
 M MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/Commands/UnrealMCPCharacterCommands.cpp
 M MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/UnrealMCPBridge.cpp
 M MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Public/Commands/UnrealMCPCharacterCommands.h
 M Python/agent_runtime/agent.py
 M Python/agent_runtime/agent_manager.py
 M Python/agent_runtime/llm_router.py
 M Python/agent_runtime/unreal_bridge.py
 M Python/scripts/agent_runtime/test_blocker_sense.py
 M Python/scripts/agent_runtime/test_prompt_context.py
 M Python/worlds/MCP_World/agents/dufus/rules.md
 M Python/worlds/MCP_World/agents/maren/rules.md
 M plan/backlog.md
?? CLAUDEOLD.md
?? Python/agent_runtime/prompt_payload.py
?? Python/scripts/agent_runtime/test_body_box_probe.py
?? Python/scripts/agent_runtime/test_doctrine_imports.py
?? Python/scripts/agent_runtime/test_prompt_payload.py
?? Python/worlds/MCP_World/doctrine/
```

Recent commits:

```
1dc73c5 fix: three SR44 bugs, all confirmed live in SR45 (56 ticks, 0 errors)
024b49c feat: #80 refused ground on the /map - hatched cells + dashed no-go circles, tooltip says who/why/when (offline 61/61)
5b24217 spec: #80 refused ground on the /map - hatch refused cells, circle no-go patches, say why
5919724 docs: Maren mismatch parked per user; AGENTS.md/CLAUDEOLD.md left as-is
a3b8994 docs: three cleanup advisories done (hermetic test, backlog split, save_agent fail-loud)
```
