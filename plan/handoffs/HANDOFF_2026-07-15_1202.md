# Handoff — Dufus survey-first behavior and unattended backlog queue complete

**Date:** 2026-07-15 12:02 -05:00
**Branch:** `auto-loop/2026-07-15-backlog`
**Prior handoff:** [HANDOFF_2026-07-14_2004.md](HANDOFF_2026-07-14_2004.md)

## TL;DR

Dufus now has an explicit survey-first policy: he walks to an unexplored cell's exact center,
captures N/S/E/W, then resumes his unchanged schedule. Scheduled named-place travel is authoritative
and **live-verified**: the next user run sent Dufus straight to the village instead of into the weeds.
The map shows compact 10 px blue surveyed-cell center markers and opens the durable four-view composite. The full approved
unattended queue (#27 slice, #29, #30, #20) is complete in seven reviewable commits; 47/47 offline
test scripts pass. Nothing was pushed.

## What was done this session

- Added `Agent.survey_priority`; enabled it only for Dufus and described his village-surveyor role.
- Made survey-priority APCs center in an unexplored cell, complete the survey, then resume schedule.
- Added blue surveyed-community center markers, place-image dialog metadata/descriptions, and honest
  visual-observation/label/sighting counts. Browser-verified SR21 cell `(7,5)` and its composite.
- Reduced those blue markers from 16 px to 10 px after the next live run showed they were oversized;
  centering and click behavior are preserved.
- Made named scheduled travel override ordinary `wander` and directional `walk_to` execution. The
  SR19 regression proves west-facing Dufus receives east waypoint `[600,200,90]` toward the square.
- Anchored all cockpit world/agent/map/place/replay paths beneath trusted roots, including symlink
  checks before writes and recursive delete.
- Declared `httpx>=0.25.0,<1` directly and refreshed `uv.lock`.
- Added decision timing (`observe_ms`, per-agent `llm_ms`, `act_ms`) and wake-to-walk/displacement
  milestones; replay exposes the same timing object.
- Groomed the backlog after each slice. No further no-input implementation item remains.

## In flight

No code is in flight. Product code, backlog reconciliation, and this handoff snapshot are committed;
the working tree is clean.

## Decisions locked in this session

- `survey_priority` is an explicit per-agent exception; ordinary APC scheduled travel still outranks
  opportunistic surveys.
- Survey-priority means exact cell-center arrival before N/S/E/W capture, then schedule resumption.
- A named schedule `travel` ticket owns ordinary locomotion at the execution seam, including wake
  first-actions. Recovery exceptions remain a separate later policy decision.
- Community-map blue dots represent surveyed cell centers, not physical landmark actors.
- The old “47 landmarks” display was really accumulated visual observation rows; UI labels now say
  what each metric actually counts.
- Moving `Landmark_Dufus_home` to the hotel needs convergence before inspecting the DB: **Sync world**
  does it immediately; starting a new sim run also rescans landmarks, so doing both is unnecessary.
- No pacing, schedule, persona, or speed tuning was made; instrumentation now provides evidence first.

## Open questions

- #32: transient gaze retention/dedup, unique-frame retention, and semantic place-recall query surface.
- #27: community-landmark fine-arrival geometry and bounded blocker/stuck recovery, road, and dead-end
  policy.
- #12.2: compact interaction-event schema.
- Child Blueprint mesh choices and the remaining PIE/editor acceptance bundle require the user.

## Relevant backlog items

- #20 instrumentation: done offline, 47/47.
- #27 authoritative scheduled-travel slice: done; later geometry/recovery policy waiting.
- #29 filesystem containment: done.
- #30 direct HTTP dependency: done.
- #32 visual cortex: core map/place visual memory built; retention/query choices waiting.
- #33 and #34: done; SR21 supplied the live surveyor evidence.

## Next concrete step

Reload `/map`, confirm the 10 px blue markers remain easy to select, and click one to verify its
N/S/E/W composite still opens.

## Files touched this session

- `Python/agent_runtime/{agent.py,agent_manager.py,memory_store.py,place_db.py,run_replay.py}`
- `Python/web_ui/{main.py,templates/map.html}`
- `Python/worlds/MCP_World/agents/dufus/{state.json,goals.md}`
- `Python/pyproject.toml`, `Python/uv.lock`
- `Python/scripts/agent_runtime/test_{cell_sweep,map_view,route_planner,runner_client_dependency,timing_instrumentation,web_path_containment}.py`
- `plan/backlog.md`

## How to resume

Say `Read hand off.` Then inspect this branch and use the exact live step above. For the hotel move,
pressing **Sync world** is the clearest immediate confirmation; a fresh sim start performs the same
landmark rescan automatically.

## Git state at handoff

- Branch: `auto-loop/2026-07-15-backlog`
- Commits this session:
  - `1912109` add survey-first APC policy and visual map markers
  - `bda9bda` groom backlog after SR21 survey run
  - `9012268` make scheduled named travel authoritative
  - `ce3b879` contain cockpit filesystem paths
  - `5b78ad8` declare runner http dependency
  - `61a4d94` record movement and cognition timing
  - `fd556dd` finish unattended backlog queue
  - `5e3b740` shrink surveyed map markers
- Verification: `Python\.venv\Scripts\python.exe Python\scripts\run_tests.py` → **47/47 passed**.
- Pushes: none.
- Working tree: clean after committing this handoff snapshot.
