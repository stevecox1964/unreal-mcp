# Handoff — look-before-step lane built and live-verified; three SR44 bugs fixed, awaiting SR45

**Date:** 2026-08-19 12:52 local
**Branch:** main
**Prior handoff:** [HANDOFF_2026-08-12_2010.md](HANDOFF_2026-08-12_2010.md)

## TL;DR

The perception-guided exploration lane (#77 look-before-step, #78 sub-cell no-go patches, #26
bounce fact, #79 dataset recorder, #80 map paint) is **built and live-verified in SR43/SR44** —
Dufus stayed out of the corn all run and the user saw the red-hatched cell plus no-go circles on
`/map`. SR44 exposed three bugs; **all three are fixed but uncommitted**. The next session should
run **SR45** to confirm the fixes before writing any new feature code.

## What was done this session

- **Fixed the wake `refuse_cell` bug** (`agent_manager.py` — `_apply_cell_verdict` now runs on the
  wake path, same class as SR39's `survey_here` wake bug). **Verified live SR43 12:27:02.**
- **Built #77 look-before-step:** `perception.py` reports `ground_ahead` + `path_ahead`
  (`open|dead_end|blocked`); `_note_eyes` caches every look with a known facing per compass word,
  scoped to the spot (`_EYES_VALID_CM = 300`); `_direction_lines` renders "your own eyes saw:
  grass ahead, the way ahead DEAD-ENDS".
- **Built #78 sub-cell no-go patches:** new `no_go_patches` table (point + radius, default
  `PLACE_EXTENT_CM / 2` = 4.5 m radius); `refuse_cell` / `allow_cell` accept `"scope": "spot"`;
  patches never touch the frontier, so a half-walkable cell stays a survey target.
  **Verified live SR44** — 8 patch refusals with real reasons.
- **Built #26 bounce fact:** two consecutive opposite-heading legs count a bounce per trap cell;
  at >= 2 the prompt states it with the spot-refusal escape.
- **Built #79 perception dataset recorder:** `_record_perception_pair` appends one JSON line per
  perceived image to `agents/<id>/observations/perception_log.jsonl` (caption, landmarks, footing,
  ground_ahead, path_ahead, at, cell, heading, model, sim_run, error). Wired into all four
  perceive sites: tick, wake sweep, survey sweep, legacy explore. **Answers the user's VLM
  training-corpus question** — before this, only 21 survey composites kept their text while 348
  per-tick PNGs threw theirs away.
- **Built #80 refused ground on `/map`:** `build_map` returns `refusals` + `no_go` + counts;
  refused cells paint a red diagonal hatch over any survey state, patches draw as dashed red
  circles at true radius, tooltips lead with who/why/when. **Verified live by the user.**
- **Three cleanup advisories cleared:** `test_apc_agenda_ui.py` made hermetic (dead port);
  581 lines of dated history moved to `plan/backlog_history.md`; `save_agent` now rejects
  non-numeric tier/interval/cooldown loudly with nothing written.
- **Fixed three SR44 bugs (uncommitted):** see "In flight".
- Offline suite: **61/61 green**.

## In flight

**Three SR44 fixes, written and suite-green, NOT committed** (per the user's speed-mode rule —
commit only when asked):

1. **Re-refusing the same ground** — SR44 burned 8 ticks refusing two patches over and over,
   because a patch only rendered on *step targets*, never underfoot.
   Fix: `here_no_go` observation fact (`agent_manager.py` `_observe_agent`), a sense line in
   `llm_router.py` `_sense_note` ("you are STANDING in ground already refused — do NOT refuse it
   again"), and a rules.md clause that ground is refused once.
2. **Post-survey stall** ("things sort of stopped" — SR44 ticks 29-31 idle, then the run ended).
   Fix: `self._force_next_decide` set; `_pulse_sweep` adds the agent on `sweep_done`;
   `_observe_agent` consumes it as `force_cognition = True`.
3. **Re-surveying covered ground** (user: "get rid of stale needs a survey logic or turn it off").
   Fix: `SURVEY_STALE_REFRESH = False` in `agent_manager.py`; `_survey_visual_is_current` returns
   True for any existing composite. `test_cell_sweep.py` updated to pin the new default and run
   the legacy refresh flow under the explicit toggle.

## Decisions locked in this session

- **Speed mode (user, this session):** write code, do NOT write per-item test files, do NOT
  commit per item. Flag features under the backlog's "Needs tests (speed mode)" ledger; the
  user tests live instead. Test-writing is the designated away-time catch-up work. Saved to
  memory as `feedback_speed_mode_no_tests_no_commits.md`.
- **Surveyed is surveyed, forever.** Staleness no longer re-opens a cell. #39's refresh machinery
  is kept and still tested under the toggle, for the day re-surveying becomes a deliberate act.
  `/map` still *labels* old surveys stale — display only.
- **Whole-cell vs sub-cell refusal is the APC's call.** Corn/water/fenced field = whole cell
  (the corn-field block). One bad yard/alley/doorway inside otherwise-good ground = `scope: spot`,
  so Dufus still tries the navigable half.
- **Refusals are facts, never blockers.** Nothing in code stops a step onto refused ground; the
  map and the prompt state the record. `/map` is read-only for refusals — withdrawing stays the
  APC's own `allow_cell`.
- **Maren is parked** for the next few sessions (user). Her SR44 walking/stalling is the known
  (5,5)-vs-(6,5) truck-row split from #75, not a new fault.
- **`AGENTS.md` (deleted) and `CLAUDEOLD.md` (untracked) are left exactly as they are** (user).

## Open questions

- **Commit the three SR44 fixes?** They are suite-green and uncommitted. Speed mode says commit
  only when asked, so the next session should ask before committing — or leave them in the
  working tree if the user prefers to keep testing first.
- **Does #76 (center-out exploration) still need building?** SR45 answers it: if the frontier
  facts plus refusals now grow rings on their own, #76 may need nothing. Do not build it blind.
- **The old mis-filed survey rows** (from #55: `community:5:5` rev 4 shot from cell (5,6), named
  "Utility Pole Forest" while every revision describes a corn field) are still in the corpus.
  Purge them or leave them and re-survey? Needs a user decision — and it now matters more,
  because those rows are VLM training data (#79).

## Next concrete step

Run **SR45** (no new code first) and check four things in
`Python/worlds/MCP_World/logs/sim_runner.log`: (1) no repeated "refused a 9-m patch" for the same
coordinates, (2) no idle `scene_unchanged` run right after a `sweep_done`, (3) `survey_here` only
on cells with no composite, and (4) `Python/worlds/MCP_World/agents/dufus/observations/perception_log.jsonl`
exists and grows one line per capture.

## Files touched this session

- `Python/agent_runtime/agent_manager.py` — wake cell-verdict fix; `_note_eyes` + eyes cache;
  `no_go` / `seen_ahead` on direction places; `scope: spot` verdicts; bounce counter in
  `_drop_crumb`; `here_no_go` fact; `_force_next_decide`; `SURVEY_STALE_REFRESH`;
  `_record_perception_pair` + four call sites.
- `Python/agent_runtime/place_db.py` — `no_go_patches` table; `refuse_patch` / `patches_at` /
  `clear_patches_at` / `all_patches`; `math` import; patches added to `reset()`.
- `Python/agent_runtime/llm_router.py` — `scope` in the refuse/allow action schemas; `_no_go_text`;
  `_eyes_text`; ground-ahead/path-ahead lines in `_seen_text`; bounce + `here_no_go` sense lines.
- `Python/agent_runtime/perception.py` — VLM prompt and parse for `ground_ahead` / `path_ahead`.
- `Python/web_ui/main.py` — `_form_int` fail-loud validation + both agent routes; `refusals` /
  `no_go` / counts in `build_map`.
- `Python/web_ui/templates/map.html` — refused hatch CSS, no-go circle CSS, legend entries and
  counts, two paint passes in `refresh()`.
- `Python/worlds/MCP_World/agents/dufus/rules.md` — eyes outrank the navmesh; behind buildings is
  never survey ground; patch vs whole-cell; refuse ground once, then leave.
- `Python/scripts/agent_runtime/test_look_before_step.py` (new, 35 checks),
  `test_perception_dataset.py` (new, 18 checks), `test_agent_form_validation.py` (new, 11 checks),
  `test_map_view.py` (+refused-ground test), `test_apc_agenda_ui.py` (hermetic),
  `test_cell_sweep.py` (stale-refresh policy flip).
- `plan/backlog.md` — SR42/SR44 findings, #77/#78/#79/#80 specs and BUILT flags, the ordered plan,
  "Needs tests (speed mode)" ledger, Maren parked, three advisories struck.
- `plan/backlog_history.md` (new) — 581 lines of dated banners and completed 2026-07 queues.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: run SR45 and check the log for repeated patch refusals at the same coordinates, an idle
> run right after `sweep_done`, `survey_here` on already-surveyed cells, and whether
> `perception_log.jsonl` is filling.

## Git state at handoff

```
 D AGENTS.md
 M Python/agent_runtime/agent_manager.py
 M Python/agent_runtime/llm_router.py
 M Python/scripts/agent_runtime/test_cell_sweep.py
 M Python/worlds/MCP_World/agents/dufus/rules.md
 M plan/backlog.md
?? CLAUDEOLD.md
```

Recent commits:

```
024b49c feat: #80 refused ground on the /map - hatched cells + dashed no-go circles, tooltip says who/why/when (offline 61/61)
5b24217 spec: #80 refused ground on the /map - hatch refused cells, circle no-go patches, say why
5919724 docs: Maren mismatch parked per user; AGENTS.md/CLAUDEOLD.md left as-is
a3b8994 docs: three cleanup advisories done (hermetic test, backlog split, save_agent fail-loud)
a76faaa fix: save_agent rejects non-numeric tier/interval/cooldown loudly, nothing written (offline 61/61)
```
