# Handoff — Maren wake bug fixed twice (tick-level, then spool-up SR2 fix), 9 m place cells, #6c map overlay live; #15–#17 authored-world items queued

**Date:** 2026-07-05 17:04 local
**Branch:** auto-loop/backlog (**12 commits ahead of origin — user said DO NOT PUSH**)
**Prior handoff:** [HANDOFF_2026-07-03_1230.md](HANDOFF_2026-07-03_1230.md)

## TL;DR

Debugging session with the user watching live runs. Three things landed: (1) the **Maren wake bug**
fixed — twice, because the SR2 live run exposed that the first fix fired too late; the final fix
computes the schedule verdict + seeds the place at the **true spawn transform in the spool-up,
before the orient LLM can move anyone**; (2) **place cells are now 9×9 m** around the APC
(`PLACE_EXTENT_CM` = 900); (3) **#6c built and visually confirmed by the user** — the web `/map`
draws the real top-down capture with the 30 m grid + place-cell boxes registered over it. Suite
**35/35**. Next: **PIE-verify the wake-directive fix** (Maren stays at her truck), then build
**#15 (authored places manifest)**.

## What was done this session

- **`e48f209` wake fix, round 1 (tick-level):** the sequencer's "am I already there?" was a
  name-only match vs the community cell name → on a fresh DB Maren was told to travel while
  standing at her stall. Now geometric: `AgentManager._at_scheduled_place` (community cell = same
  grid cell; owned place = inside its extent box) feeds a new `planner.step(at_place=...)` override.
  First schedule step of a run seeds an unresolvable scheduled place as the agent's own place cell
  where it stands. A scheduled **"act" tick is sweep-exempt** (partial reversal of 2026-07-03
  "sweep on entry even when acting" — Maren must not walk to the cell center at wake).
  `test_wake_place.py` (new), `test_cell_sweep.py` updated.
- **9×9 m place cells:** `place_db.py` `PLACE_EXTENT_CM = 900` (was 300); schema default, docstrings,
  tests updated; **live MCP_World DB rows widened out-of-band** (7 rows 300→900).
- **`6fe8b08` #6c map overlay:** calibration = **assume-covers-bounds** (user's call); orientation
  +X = east = right, +Y = south = down, row 0 = north (matches `place_db.py` compass convention;
  image aspect 1.527 ≈ bounds aspect 1.523). New `WorldGrid.origin()` (cell (0,0) corner —
  origin-anchored, starts outside bounds, edge cells crop). `/api/map` +
  `bounds/origin_x/origin_y/image_url` + owned `dx/dy/extent_cm`; `/images` mounted; `map.html`
  rewritten as one world-registered overlay engine (image background, translucent cells, purple
  9×9 m owned boxes, tooltips, live refresh; plain background at world aspect when no capture).
  Per-level `images/<level>.png` beats shared `world_map_view.png`. **User confirmed it looks right.**
- **`f94cc24` wake fix, round 2 (SR2 live-run diagnosis + fix):** SR2 log showed the spool-up
  orient LLM (schedule-blind) decided "walk to the truck" *before* the first schedule tick — Maren
  moved, the seed stamped the wrong spot, and she orbited town chasing a name that resolved behind
  her. Dufus thrashed between directive "home" (his 07:00–08:30 block) and persona "village
  square". Fix: `AgentManager._wake_directive` — in `_wake_agents` phase 1, **before** the orient
  call: generate the day's schedule, seed the scheduled place at the true spawn, compute the
  verdict geometrically; wake prompt gained `## Your Schedule Right Now` +
  `llm_router._wake_schedule_line` ("your position CONFIRMS you are already there / you are NOT
  there yet") — the LLM no longer guesses. Per-tick act note hardened (anti-orbit: "Do NOT walk_to
  it"). Positionless ticks no longer burn the once-per-run seed chance. Maren's SR2 mis-seeded
  truck row **deleted from the live DB** (re-seeds at true spawn next run).
- **Bindings verified — personalities NOT swapped** (user suspicion): maren→`APC_Maren_BP_C_1`,
  dufus→`APC_Dufus_BP_C_1`.
- **`6f7c385` backlog #15/#16/#17 + #13 reframe** (user: "APCs don't have a root configuration"):
  **#15** authored places manifest (`worlds/<level>/places.json` → PlaceDB at load; schedule
  validation fails loud; wake-seed demoted to fallback), **#16** click-to-author places on the
  #6c map (no-Unreal place editor), **#17** grid-first multi-leg routing (kills orbit-by-vision
  structurally), **#13** reframed as the downloader bootstrap flow tying them together.
- **Memory updated:** `project_grid_place_cell_sizes` — place cell now 9×9 m (was ~3 m).

## In flight

_None._ Tree clean, all committed, suite 35/35.

## Decisions locked in this session

- **Place cell = 9×9 m box around the APC** (`PLACE_EXTENT_CM` = 900). Grid stays 30 m.
- **#6c calibration = assume-covers-bounds** — the capture frames the world bounds exactly;
  world→pixel is linear. +X = east = right, +Y = south = down, row 0 = north.
- **Wake ground truth is computed, never guessed:** the sequencer verdict is derived geometrically
  at the true spawn in the spool-up and *stated* in the wake prompt; ordering matters (seed before
  the orient LLM can move the agent).
- **Editor placement = day-start spot** (first-wake seed convention) — an interim hack, to be
  superseded by the #15 authored manifest (authored > discovered > wake-seeded).
- **Scheduled "act" ticks are sweep-exempt** (partial reversal of 2026-07-03 sweep-on-entry) —
  the agent is AT its place doing its job; the cell is swept on a travel/idle tick. User can veto.
- **Greet cooldown already exists (#12.1, 60 min)** — but only for *named* acquaintances;
  strangers are anonymous and can be re-greeted (that gap is #12.2, not started).
- **DO NOT PUSH** this branch — user's explicit instruction at session end (12 commits local-only).

## Open questions

- **Sweep-exemption veto?** The act-tick sweep exemption reverses part of the user's own
  2026-07-03 call; flagged twice, no objection yet — confirm it survives PIE observation.
- **Dufus's "home until 08:30" block:** his own generated schedule keeps him home ~3 real minutes
  at wake (looks "stuck"). Trim his `goals.md` wake line, or accept it as persona?
- **#6c registration accuracy:** user says "visually looks good, will study more". Ground-truth
  checks suggested: Maren's spawn (-8400, 150) = cell (6,6) "motel"; a systematic shift means a
  bounds-framing offset (one-line calibration fix). Also: re-shoot the capture without the Unreal
  HUD/toolbar baked into the top edge.
- **#12.2 stranger re-greeting:** build interaction memory for anonymous encounters, or wait?

## Next concrete step

**PIE-verify the wake-directive fix (SR3):** start a run, watch Maren — expected log lines
`[maren] wake: seeded own place cell 'the vegetable truck' (9 m box)` then a wake decision that
stays put (directive "act", thought should acknowledge she's already there). Dufus should idle at
"home" until 08:30 sim time, then travel toward the village square. Scrub it afterwards in
`/replay` (SR3) and check `/map` — her truck box should sit exactly on her spawn. If she still
walks off, pull the SR3 decisions from `worlds/MCP_World/logs/agent_decisions.log` and check
whether the wake thought references the schedule verdict.

## Files touched this session

- `Python/agent_runtime/planner.py` — `step(at_place=...)` geometric override
- `Python/agent_runtime/agent_manager.py` — `_at_scheduled_place`, `_wake_directive`, wake-seed
  marker (`_wake_stepped`, cleared per run), act-tick sweep exemption, schedule directive into
  wake context
- `Python/agent_runtime/place_db.py` — `PLACE_EXTENT_CM = 900`, schema default, docstrings
- `Python/agent_runtime/llm_router.py` — `_wake_schedule_line` + `## Your Schedule Right Now` in
  the wake template; `_schedule_note` act branch anti-orbit hardening
- `Python/agent_runtime/world_grid.py` — `origin()`
- `Python/web_ui/main.py` — `/images` mount, `_map_image_url`, `build_map` overlay fields
- `Python/web_ui/templates/map.html` — rewritten: image-backed world-registered overlay
- `Python/tools/simulation_tools.py` — 9 m docstring
- `Python/scripts/agent_runtime/` — `test_wake_place.py` (new), `test_cell_sweep.py`,
  `test_owned_places.py`, `test_map_view.py`, `test_world_grid.py`, `test_prompt_context.py`
- `plan/backlog.md` — 2026-07-05 status banners; #6c BUILT; #15/#16/#17 new; #13 reframed
- Live (untracked) `Python/worlds/MCP_World/world_places.db` — extents 300→900; Maren's SR2
  mis-seed deleted
- memory: `project_grid_place_cell_sizes.md`, `MEMORY.md`

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: PIE-verify the wake-directive fix (SR3) — Maren should wake at her truck, get the
> "position CONFIRMS you are already there" verdict, and stay put; Dufus idles at home until 08:30
> then heads for the square. Then build backlog #15 (authored places manifest). Do NOT push.

## Git state at handoff

```
(clean — all committed)
```

Recent commits:

```
6f7c385 backlog: #15 authored places manifest, #16 click-to-author map editor, #17 grid-first routing; #13 reframed as downloader bootstrap
005f0cb backlog: SR2 findings + wake-directive fix noted
f94cc24 wake directive at the true spawn: seed + verdict before the LLM can move (SR2 fix)
1a02f93 backlog: 2026-07-05 status -- wake fix + 9m place cells + #6c built (suite 35/35)
6fe8b08 #6c: real top-down capture under the web /map grid + place cells
```
