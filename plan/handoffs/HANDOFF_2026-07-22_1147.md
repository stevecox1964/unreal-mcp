# Handoff — #36 JSON agenda implementation and offline tests complete

**Date:** 2026-07-22 11:47 -05:00
**Branch:** `main`
**Prior handoff:** [HANDOFF_2026-07-21_2022.md](HANDOFF_2026-07-21_2022.md)

## TL;DR

#36 now has validated per-APC authored JSON agendas, separate deterministic runtime execution and ledger
state, agenda-grounded prompt context, completion policies, and interruption/resume behavior. Sol authored
the tests and Terra ran the complete offline suite: **51/51 passed**. The user's live run confirmed Dufus
completed `morning_home`, reached the square, and activated `square_morning`; it also exposed stale legacy
`current_goal` text, which is now synchronized in code but needs a restarted-process live check. #43 now
explicitly owns a friendly expandable cockpit drill-down for APC JSON, with raw JSON secondary. All work
is uncommitted on `main`.

## What was done this session

- Added schema-versioned agenda validation, normalization, atomic writes, legacy schedule conversion,
  deterministic task transitions, chronological ledger entries, and prompt summaries.
- Integrated authored agendas and runtime execution into agent loading, resetting, inspection, decisions,
  task completion, named-place travel, waiting between blocks, day/revision rollover, and interruptions.
- Added tracked agendas for Dufus and Maren under `Python/worlds/MCP_World/agents/`.
- Kept compatibility with generated schedules and legacy duck-typed test agents.
- Added an **APCs** link to the cockpit main navigation.
- Updated `AGENTS.md`: tests are not authored or executed until requested; Sol authors requested tests and
  Luna/Terra executes them; implementation may carry a non-executable `TEST-FLAG` meanwhile.
- Authored comprehensive agenda, agent-state, prompt-context, and navigation coverage.
- Terra ran the full offline suite after compatibility fixes: **51/51 passed**.
- Inspected the user's live run: Dufus completed `morning_home` at 08:32, completed grounded travel to the
  square at 08:34, and had `square_morning` active at 09:01 with no agenda errors. No interruption occurred.
- Fixed the run's stale `current_goal` finding by synchronizing it whenever the active agenda task changes.
- Expanded backlog #43 to require labeled, expandable APC JSON summaries and bounded errors instead of a
  raw file dump.

## In flight

- The post-run `current_goal` synchronization fix has offline coverage but has not been checked in a newly
  restarted Python process.
- #36 still needs one live interruption/resume exercise proving the exact active agenda task is suspended
  and resumed. The prior user run contained no interruption.
- The Unreal sim is stopped; the user reported PIE and cockpit were left running. Runtime state may reflect
  the prior process until the Python runner/cockpit backend is restarted.
- #43's APC nav entry exists, but the expandable drill-down/editor is backlog work, not implemented here.

## Decisions locked in this session

- Authored `agenda.json` is separate from ignored `runtime.json` execution state.
- Deterministic facts own task activation, arrival/time completion, rollover, and resumption; bounded model
  confirmation applies only to the exact active task after a successful world action at its required place.
- `current_goal` mirrors the active agenda objective for legacy UI/runtime consumers.
- The cockpit should present structured APC data as readable nested summaries. Raw JSON is an advanced
  diagnostic view, never the primary user experience.
- Requested tests follow the project policy: Sol writes; Luna or Terra runs.

## Open questions

- Whether #43 uses a reusable recursive JSON viewer, dedicated agenda/ledger components, or both.
- Live behavior after process restart: does Dufus's displayed `current_goal` follow `square_morning` and does
  an interruption preserve/resume that task without backtracking?

## Relevant backlog items

- #36: implementation and offline coverage complete at 51/51; restarted live goal-sync and
  interruption/resume verification remain.
- #43: APC nav entry implemented; friendly drill-down, agenda editor, and live runtime panel remain.
- #37: dedicated direct-chat page is offline-complete; its live chat/resume verification can supply #36's
  interruption exercise.
- #6: grounded named-place route facts remain relevant to richer travel behavior.

## Next concrete step

Restart the Python runner/cockpit backend and perform the short #36 live verification: confirm Dufus's
displayed goal matches the active agenda task, then interrupt and resume that task once.

## Files touched this session

- `AGENTS.md`
- `Python/agent_runtime/agenda.py` (new)
- `Python/agent_runtime/agent.py`
- `Python/agent_runtime/agent_manager.py`
- `Python/agent_runtime/llm_router.py`
- `Python/scripts/agent_runtime/test_agenda.py` (new)
- `Python/scripts/agent_runtime/test_agent_state.py`
- `Python/scripts/agent_runtime/test_prompt_context.py`
- `Python/scripts/agent_runtime/test_sim_controller.py`
- `Python/web_ui/templates/base.html`
- `Python/worlds/MCP_World/agents/dufus/agenda.json` (new)
- `Python/worlds/MCP_World/agents/maren/agenda.json` (new)
- `plan/backlog.md`
- `plan/handoffs/HANDOFF_2026-07-22_1147.md` (new)
- `plan/handoffs/LATEST.md`

## How to resume

Say `Read hand off.` Review this handoff, restart the Python backend before live verification, and do not
rerun tests unless explicitly requested under the current `AGENTS.md` policy.

## Git state at handoff

- Branch: `main`
- HEAD: `e702dd8 docs: approve JSON APC agenda design`
- Tracking before this handoff: `main...origin/main` (no ahead/behind count reported)
- Working tree: modified and untracked files listed above; nothing staged or committed.
- Diff before adding this handoff: 9 tracked files changed, 481 insertions, 70 deletions, plus four new
  implementation/test/agenda files not included in that stat.
- No commit or push was performed. The Unreal sim was not restarted after the final goal-sync fix.
