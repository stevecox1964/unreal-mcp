# Handoff — B7 pawn-avoidance sense + WP5 route map built; live run: "the system is starting to work"

**Date:** 2026-07-01 22:00 local
**Branch:** auto-loop/backlog → merged + pushed to `main` at session end (user request)
**Prior handoff:** [HANDOFF_2026-07-01_1815.md](HANDOFF_2026-07-01_1815.md)

## TL;DR

Built and live-tested the last two open items from the prior handoff: **B7** (engine-agnostic pawn
avoidance — user's call: no RVO, the cognitive loop owns the sidestep) and **WP5/#6b** (the APC
top-down route map — user signed off all four gates in-session, choosing a **rendered image** over the
spec's text rec). Live PIE run same night: **Dufus roams all around, the web `/map` updates live**, and
the sidestep works but only *after* he's in people's faces → follow-up **B7b (personal space)** queued.
Suite **30/30**, merged to `main` and pushed. Next session: B7b, or Milestone 2 (memory stream).

## What was done this session

- **B7 built** (`8ad4073`) — `_observe_agent` now line-traces ahead **every moving tick** (500 cm,
  `_AHEAD_TRACE_CM`), not just after wedging: a *mobile* category (person/animal/vehicle,
  `_MOBILE_BLOCKERS`) becomes a `blocker` fact and **forces a re-decide** (the scene-unchanged gate
  no longer skips the LLM when a blocker is attached), so the agent can sidestep via
  `walk_to forward-left/right` before contact. Structures ahead stay silent while traveling; the
  stuck path is unchanged. Prompt: pure `_sense_note` renderer (stuck/blocker are independent facts)
  + one doctrine line ("Never walk through a person… step around… continue to your destination").
  Test: `Python/scripts/agent_runtime/test_blocker_sense.py`.
- **WP5 signed off + built** (`914ce97`) — user answered the four gates live:
  **Q1 = RENDERED IMAGE** (diverges from the spec's semantic-text rec), Q2 corridor+1 cap 15×15,
  Q3 separate renderer over shared PlaceDB, exposure = travel ticks only. New
  `Python/agent_runtime/route_map.py` (pure): `corridor()` (from-anchored truncation),
  `build_route_map()` (cell states/names/landmarks + destination bearing/distance),
  `render_map_image()` (PIL top-down PNG — north up, A=you blue, B=destination red, green/tan/gray
  states, legend strip). `AgentManager.route_map_for` resolves community name → owned place (WP4),
  writes `agents/<id>/observations/route_map.png`; `_perceive_and_decide` attaches it **only when the
  sequencer directive is travel**; `{route_map_note}` prompt section carries the facts + image legend;
  the decision call goes **multimodal** for anthropic/ollama (OpenAI stays text-only on the facts).
  Every failure degrades to "no map this tick". Test: `test_route_map.py` (incl. pixel-level render
  checks + end-to-end travel-tick injection). Rendered a sample and visually confirmed it.
- **Live PIE run (user-driven, same night):** Dufus roams freely; the A1 `/map` page updates live
  (B2 essentially ✓); B7 partial — he moves around people **after the fact** but still gets right in
  their faces first (undesirable) → **B7b** queued.
- **Docs:** WP5 spec flipped to APPROVED+BUILT with full executor notes
  (`plan/specs/WP5-apc-topdown-map.md`); backlog status banner + B7/B2/#6b items updated.
- **Merged `auto-loop/backlog` → `main` and pushed** (user: "push to main… the system is starting
  to work").

## In flight

_None._ All work committed; suite 30/30.

## Decisions locked in this session

- **B7 is solved cognitively, not in the engine** — user explicitly ruled out engine-specific fixes
  (RVO/DetourCrowd). The lizard brain reports the fact; the LLM sidesteps. Any B7b tuning must stay
  engine-agnostic-first (acceptance radius on the bridge MoveTo is the acceptable fallback).
- **WP5 Q1 = rendered image** — user's explicit pick over the recommended semantic-text map. The
  facts dict is still the substrate; the PNG is a projection of it, attached to the multimodal
  decision call. Don't re-litigate.
- **WP5 Q2/Q3/exposure** = corridor+1 cap 15×15 / separate renderer over shared PlaceDB / inject on
  travel ticks only (all per rec).
- **Push policy honored:** "push to main" = merge + push directly, no PRs (memory `feedback_push_no_prs`).

## Open questions

- **B7b approach** (queued in backlog, not designed): how to give agents personal space so the
  sidestep happens *before* face-to-face. Candidates: escalate the fact earlier (trigger the
  re-decide at a standoff threshold rather than any hit ≤500 cm — note the tick cadence ~9 s means
  he covers meters between decisions), and/or the (b) acceptance-radius on MoveTo so approaches stop
  ~1 m short. Cognitive-first per the locked decision above.
- **#10.5 friend-interrupt path still unobserved live** (carried from prior handoff — the two NPCs
  haven't met in range yet).
- **Route-map live confirmation:** the map webpage updating is confirmed; whether Dufus's travel
  ticks visibly *used* the attached route-map PNG (thought_summary referencing bearing/map) wasn't
  specifically checked in this run — worth a look next PIE session
  (`agents/dufus/observations/route_map.png` is overwritten each travel tick).

## Next concrete step

Design + build **B7b (personal space)**: in `Python/agent_runtime/agent_manager.py`, make the
forced-re-decide threshold distance-aware (e.g. only mobile blockers within ~250 cm force the
re-decide, but attach the fact from 500 cm so the LLM sees it coming), and consider passing an
acceptance radius through `unreal_bridge.execute_action`'s walk_to path — then PIE-verify Dufus
keeps ~1 m clearance. Alternatively (user's pick): start MASTER_PLAN **Milestone 2** (SQLite memory
stream + retrieval), which is loop-safe offline work.

## Files touched this session

- `Python/agent_runtime/agent_manager.py` — B7 proactive trace + forced re-decide; WP5
  `route_map_for` + travel-tick injection; `route_map` import
- `Python/agent_runtime/llm_router.py` — `_sense_note` (pure), step-around doctrine, `{sense_note}`
  rename, `{route_map_note}` section + renderer, map image passed to anthropic/ollama decide calls
- `Python/agent_runtime/route_map.py` — **new** (WP5 corridor/facts/PNG renderer)
- `Python/scripts/agent_runtime/test_blocker_sense.py` — **new** (B7)
- `Python/scripts/agent_runtime/test_route_map.py` — **new** (WP5)
- `plan/specs/WP5-apc-topdown-map.md` — APPROVED+BUILT + executor notes
- `plan/backlog.md` — status banner, B7 partial-verify + B7b, B2 mostly-✓, #6b checked off
- `plan/handoffs/HANDOFF_2026-07-01_2200.md` + `LATEST.md` — this handoff

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: design + build B7b (personal space — distance-aware re-decide threshold in agent_manager.py,
> possibly + an acceptance radius on the bridge's walk_to), or start MASTER_PLAN Milestone 2 (memory
> stream) if the user prefers.

## Git state at handoff

```
 M plan/backlog.md      (committed with the handoff before merge+push)
```

Recent commits:

```
914ce97 WP5: APC top-down route map (#6b) - rendered image on travel ticks
8ad4073 B7: engine-agnostic pawn avoidance - proactive forward path sense
00d2b54 handoff: 2026-07-01 session (architect specs + WP1-4/B5 built, #10.5 verified live)
20f7f7b backlog: B3/#10.5 verified live (gate held every tick); add B7 pawn collision
2f2aece B3 fix from live run: destination-first travel intent + de-engine Maren's truck
```
