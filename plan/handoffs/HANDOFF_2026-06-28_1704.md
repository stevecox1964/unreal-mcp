# Handoff — Haiku vision (incl. JPEG fix) + provider CRUD + MCP authoring-tools retired; cockpit Clear feed

**Date:** 2026-06-28 17:04 CDT
**Branch:** auto-loop/backlog (14 commits ahead of main — NEVER pushed)
**Prior handoff:** [HANDOFF_2026-06-26_2029.md](HANDOFF_2026-06-26_2029.md)

## TL;DR

Big session on `auto-loop/backlog`: vision now runs on **Haiku 4.5** (Gemini optional), web-UI
**provider-profiles CRUD** (`/providers`), the stale **MCP→Unreal editor-authoring tools were retired**,
the legacy **npc_builder app deleted**, and the Sim cockpit got **Clear feed + timestamps**. A live
**vision regression was found and fixed** (captures are JPEG under a `.png` name; Anthropic 400'd on the
hardcoded media type). **23/23 offline tests green, nothing pushed.** Next: live-run the sim to confirm
vision holds, then either run `/code-review` on the branch or **merge `auto-loop/backlog` → main**.

## What was done this session

- **#7.0 Haiku vision** — `VisionPerceiver` gained an `anthropic` branch (`perception.py`);
  `VISION_PROVIDER=anthropic` perceives via Haiku 4.5. `.env` flipped to anthropic; Gemini/ollama
  stay selectable. Test: `test_vision_perceiver.py`.
- **VISION REGRESSION FIX** — every tick in the 18:31 live run failed because captures are **JPEG
  bytes with a `.png` filename** and `_perceive_anthropic` hardcoded `media_type=image/png`
  (Anthropic validates it → 400; Gemini was lenient, which masked it). Added `_media_type()` magic-byte
  sniff. Verified against the exact failing frame: real caption + 9 landmarks + 2 characters.
- **#7.1/7.2/7.3 Provider profiles CRUD** — `agent_runtime/provider_profiles.py` (`config.json` of
  named `{provider,model}` profiles + decision/vision role assignment) that **compiles to the `.env`
  keys the runtime already reads** (`apply_to_env`), so `llm_router`/`perception` are untouched. Web
  routes + `/providers` page (role selectors, profile table, add/edit). User confirmed the page "looks
  good enough." Tests: `test_provider_profiles.py`, `test_providers_page.py`.
- **#8 Retired MCP→Unreal authoring tools** — deleted 8 tool modules (editor/blueprint/node/umg/
  character/camera/attachment/project, ~2300 lines) + their registrations; kept `simulation_tools.py`;
  rewrote the server `info()` prompt; fixed stale README/Docs. Verified nothing in the standalone sim
  path imported them (bridge uses raw socket commands).
- **Deleted legacy `npc_builder/`** app (superseded by `web_ui` + `/create-npc`); tidied the skill ref.
- **Sim cockpit Clear feed + timestamps** — the `/sim` feed tails `agent_decisions.log` (persists across
  runs — that was the "old vision-failed lines still showing" confusion). Added `MemoryStore.clear_recent_events`
  → `AgentManager.clear_events` → runner `POST /events/clear` → `RunnerClient.clear_events` → web
  `POST /api/sim/events/clear` → a **Clear feed** button; each line now shows local-time timestamp.
  Tests: `test_decision_feed.py`, extended `test_sim_controller.py` + `test_runner_api.py`.
- **`start_sim.bat`** now kills any prior sim engine (:8777) + cockpit (:8765) and orphaned engine
  window before relaunch (verified the netstat→taskkill parsing against live PIDs).
- **Framing + memory** — introduced **dev mode vs sim mode** (memory `feedback-dev-sim-modes`):
  dev mode = Claude operates + debugs logs with the user; sim mode = standalone web-driven sim; goal is
  Claude self-operating the web UI via browser/computer use until credits run out.

## In flight

_None._ All changes committed; tree clean except the untracked `.mcp.json.disabled` (intentional local
MCP restore point).

## Decisions locked in this session

- **Haiku for both decision + vision** — Haiku 4.5 is multimodal, so one provider/key covers both;
  Gemini kept only as a *selectable* provider (user choice), not the active default.
- **Provider profiles = option B** (named profiles), stored in `config.json`, **compiled down to `.env`**
  rather than read at runtime — keeps `llm_router`/`perception` resolution untouched.
- **MCP authoring tools go; sim tools stay** — Epic ships an official Unreal MCP for editor authoring;
  the sim is driven by the runner/web UI. C++/Blueprints untouched.
- **Dev mode vs sim mode** is the operating frame from now on (see memory).
- **#9 (SR<n> per-run tagging) is PARKED** — user said "no rename for now." Backlogged, do not implement.
- **Never push unattended** — everything stays on `auto-loop/backlog` for user review/merge.

## Open questions

- **#8 (talk to Unreal without MCP):** the Claude-driving path post-MCP — runner HTTP API (rec) vs a
  bridge HTTP shim vs Epic's official MCP. User likes Claude driving when present, but not via *our* MCP.
- **#9 SR<n>:** global vs per-world counter (rec: per-world) — only when unparked.
- **#7 deeper verify:** end-to-end "assign a profile in `/providers` → sim actually uses it" not yet spot-checked.

## Next concrete step

Re-run the sim via `Python\start_sim.bat` (Unreal in PIE) and read `worlds/MCP_World/logs/agent_decisions.log`
— confirm zero "Vision failed" lines and real sightings from the first tick. If clean, run `/code-review`
on the `auto-loop/backlog` diff (14 commits headed for main) before merging.

## Files touched this session

- **new:** `Python/agent_runtime/provider_profiles.py`, `Python/web_ui/templates/providers.html`,
  `Python/scripts/agent_runtime/{test_vision_perceiver,test_provider_profiles,test_providers_page,test_decision_feed}.py`
- **changed (runtime):** `Python/agent_runtime/{perception,provider_profiles,memory_store,agent_manager,runner_app,runner_client}.py`,
  `Python/web_ui/{main.py,templates/sim.html,templates/base.html}`, `Python/unreal_sim_server.py`,
  `Python/start_sim.bat`, `Python/.env` (gitignored: VISION_PROVIDER=anthropic)
- **changed (tests/docs):** `Python/scripts/agent_runtime/{test_sim_controller,test_runner_api}.py`,
  `README.md`, `Docs/Tools/README.md`, `.agents/skills/create-npc/SKILL.md`, `.gitignore`, `plan/backlog.md`
- **deleted:** `Python/tools/{editor,blueprint,node,umg,character,camera,attachment,project}_tools.py`,
  `Python/start_npc_builder.bat`, the whole `npc_builder/` directory
- **memory (outside repo):** `feedback_dev_sim_modes.md` + MEMORY.md index line

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is:
> re-run the sim via `Python\start_sim.bat` (Unreal in PIE) and check `worlds/MCP_World/logs/agent_decisions.log`
> for zero "Vision failed" lines; if clean, run `/code-review` on `auto-loop/backlog` before merging to main.

## Git state at handoff

```
?? .mcp.json.disabled
```

Recent commits:

```
e5027d0 Sim cockpit: Clear feed button + per-event timestamps
fad6866 Delete legacy start_npc_builder.bat
32e5184 start_sim.bat: kill prior sim engine + cockpit before relaunch
20d3e3b Backlog: mark #7.3 user-verified; park #9 (no rename for now)
53acc7a Backlog #9: Sim Run ID (SR<n>) tagging for observations + logs
```
