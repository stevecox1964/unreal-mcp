# Handoff — Maren stays put: wake fixed via authoring + clean brains; onboarding hardened (#13.1–13.3)

**Date:** 2026-07-09 20:32 local
**Branch:** auto-loop/backlog (pushed through `2e7e52a` + this handoff, per user: "push to git")
**Prior handoff:** [HANDOFF_2026-07-08_1951.md](HANDOFF_2026-07-08_1951.md)

## TL;DR

**The headline: Maren finally stays at her truck.** The chain that got there: SR7/SR8 log diagnosis
(she was never *choosing* to wander — she was lost in transit from a motel-side spawn), the fixes
(goals.md motel→home, user moved her actor + landmark to the truck, both agents' learned
memories + the place DB wiped clean), and a verified run where she wakes on post and holds.
Second thread: **#13 onboarding became the priority** (user: "paramount to making this useful to
others") and three Sonnet executors landed #13.1 (first-run banner), #13.2 (grid-gen button),
#13.3 (deep-reset cockpit buttons) — plus a real onboarding bug found by the executors themselves
(`requires-python` lied; fresh venvs on 3.11 couldn't parse `llm_router.py`). Suite **42/42**.
**Next: spec #17's prompt-side completion** — travel ticks must tell the LLM to `walk_to
target_location="<place>"` (engage the grid-first router) instead of direction-steering; that's
the root of all remaining orbiting/oscillation (Maren's old treks, Dufus's yard ping-pong).

## What was done this session

- **SR7 diagnosis (the load-bearing insight):** Maren's "wandering" was failed *travel*, not
  failed *staying*. Every SR7 tick was a `walk_to` toward the truck that never arrived — the
  decision prompt says "use walk_to with a direction relative to your facing"
  (`llm_router.py:106`), and direction-walks bypass `_execute_routed_walk`
  (`agent_manager.py:1807` vs `:1827`), so she visually steered toward a truck never in frame =
  the "greedy-by-vision orbits" failure #17's own comments predict. This *is* the carried "#17
  PIE verify": the router exists but the prompt never lets it engage.
- **Maren authoring fixed** (`6e707ce`): goals.md "retire near the motel" → "walk home and sleep
  at home" (resolves to `Landmark_maren_home`, exact match + preferred_owner). Confirmed the
  only authored motel reference; her "waking at the motel" was **perception + accumulated
  memories**, not config.
- **Agent brains + place DB wiped** (by hand; data files are gitignored): both agents'
  memory.json emptied, episodes/spatial_map/social deleted; PlaceDB.reset() cleared 12 community
  cells / 1,636 observations / 32 stale owned rows — including Dufus's rationalized
  "motel = village square" map labels (his memory literally said "woke at motel AGAIN (which IS
  the village square!)"). Landmarks re-apply at sim start, so authored truth survived.
- **User editor work:** Maren's landmark moved onto the truck, her APC actor moved beside it
  (screenshot-verified inside the 9×9 m box); `landmark_dufus_start` created then deleted after
  the "landmarks only matter if a schedule names them" explainer.
- **#13 re-reframed to the landmark era** (`93b7cab`): the git-clone → running-sim sequence is
  the product now; 6-step new-user flow documented, three gaps identified.
- **#13.1 first-run setup banner** (Sonnet executor `f88f8b9`): `config_store.setup_status()`;
  `/` + `/sim` show "First run? Add your model provider key in Settings →" when no key (ollama
  counts as ready). `GET /api/setup`. `test_first_run.py`.
- **#13.2 cockpit grid-gen** (Sonnet executor `75ae079`): `POST /api/world/grid` proxy + a
  "This level has no grid yet — Generate world grid" callout on /map with honest error text.
- **#13.3 deep-reset buttons** (Sonnet executor `0648b10`): 🧠 Reset agents + 🗺 Reset places
  beside ☀ Restart day, confirm() text states real effects. (Born from the user asking "do we
  have a webUI button for all this?" mid-hand-wipe.)
- **requires-python fixed >=3.10 → >=3.12** (`de31a4e`): both #13.1/#13.2 executor venvs failed
  at import on 3.11 (`llm_router.py:308` is PEP-701) — exactly the fresh-clone bug #13 exists to
  kill. Re-locked, smoke-verified `import web_ui.main, sim_runner`.
- **Verified live (user, SR9+ with clean DB):** Maren wakes at the truck and **stays put**; both
  agents began cell sweeps (expected on an empty DB); Dufus greeted then sat in the
  scene-unchanged idle gate (`_STATIONARY_REDECIDE_TICKS = 4` — the earlier "stuck in observing"
  was a <1-minute run ending before the 4th-tick re-decide).
- **Backlog #26 added** (uncommitted at write; rides with this handoff): dead-end recognition —
  Dufus oscillating into/out of a navmesh-legal yard; user: "just because a nav mesh says a bot
  can walk there doesn't mean they should."

## In flight

_None in code — all three executor branches merged, worktrees cleaned, suite 42/42._

## Decisions locked in this session

- **Orchestrate-by-default economics confirmed and calibrated** (user: "if sonnet can code/config
  things cheaper than you then let's do it… Architecturally I need you and future big boy models
  to run the show"): Fable = specs, diff reviews, live-log judgment, tiny one-line edits (where
  delegation costs more than doing); Sonnet executors = anything build-shaped. A vague spec is
  the expensive failure mode. Fire executors without asking once the ask is clear.
- **Moving goalposts is by design** (user, saved to memory): live runs shift scope on purpose;
  features needing more sdk/tool code get parked, not forced.
- **Wake position = actor placement; landmarks = where places are.** Two different things; the
  editor-vs-PIE world copy distinction matters (moves during Play don't persist).
- **A landmark only does something if a schedule block / walk_to names it** (hence
  landmark_dufus_start deleted).
- **The 9×9 m box is Python-side math** (`PLACE_EXTENT_CM`), not Unreal geometry — no BP
  parenting/bounding boxes, ever.
- **Maren's memory.seed.json is stale** (May world: "shop canopy", "pawn shop") — deliberately
  NOT restored during the wipe; 🧠 Reset agents will re-inject it until rewritten/deleted.

## Open questions

- **Maren "relocates a few meters on start up"** (user, SR9+) — two candidate causes, not yet
  distinguished: (a) the community-cell sweep's GOTO_CENTER step on a clean DB (but a 2026-07-05
  decision made scheduled-act ticks sweep-exempt — if she swept while on-post, that exemption may
  have a hole), or (b) the 🧠 Reset agents teleport to a recorded start transform. Read the next
  run's first ~5 log lines for `[maren]` sweep/teleport entries to tell.
- **"Are all these json files making our lives miserable?"** (user) — the hidden-state sprawl
  (memory.json / spatial_map / episodes / runtime / place DB) made this session's debugging hard
  until wiped. Possible directions: a state-inspector page, a single "wipe learned state" concept
  (now two buttons), or consolidating stores. Needs a design think, not urgent.
- **#19 sidewalks/roads design call** (carried, oldest open): navmesh costs (a) vs lizard-brain
  surface facts (b) vs route-planner weighting (c) — spec leans b+c; user hasn't chosen. #26
  dead-ends will likely fold into the same spec.
- (Carried) #12.2 interaction memory; #20 pacing instrumentation; Dufus greeting a possibly-static
  NPC that never replies (watch for a greet→freeze→re-greet loop; log will show it).

## Next concrete step

**Spec #17's prompt-side completion and fire an executor:** change the travel-tick prompt
(`llm_router.py` — the `_USER_TEMPLATE_VISION` travel path and the "To move toward anything you
see, use walk_to with a direction" doctrine at ~line 106) so a travel directive tells the LLM to
issue `walk_to {"target_location": "<the scheduled place>"}` (engaging `_execute_routed_walk`'s
grid-first legs), reserving direction-walks for the stuck/blocker escape case. Offline-testable
via the existing prompt-contract string tests (`test_prompt_context.py` pattern). This is the
structural fix for Maren's old treks AND most of Dufus's yard oscillation (#26).

## Files touched this session

- `Python/worlds/MCP_World/agents/maren/goals.md` — motel → home (sleep at Landmark_maren_home)
- `plan/backlog.md` — #13 re-reframed (landmark-era onboarding); #13.1/#13.2/#13.3 spec'd +
  ticked; #26 dead-end recognition added
- `Python/agent_runtime/config_store.py` — `setup_status()` (#13.1)
- `Python/web_ui/main.py` — `/api/setup`, `/api/world/grid`, `/api/sim/reset_agents`,
  `/api/sim/reset_places` (#13.1/2/3)
- `Python/web_ui/templates/` — `index.html`/`sim.html` setup banner + reset buttons;
  `map.html` grid-gen callout; `base.html` banner CSS
- `Python/scripts/agent_runtime/` — `test_first_run.py` (new); `test_map_view.py`,
  `test_sim_controller.py` extended
- `Python/pyproject.toml`, `Python/uv.lock` — `requires-python >=3.12` + re-lock
- Wiped (gitignored data): both agents' memory.json/episodes/spatial_map, dufus social.json,
  `worlds/MCP_World/world_places.db` contents
- `plan/handoffs/HANDOFF_2026-07-09_2032.md`, `LATEST.md` — this handoff

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete
> step is: spec #17's prompt-side completion (travel ticks direct the LLM to walk_to
> target_location so the grid-first router engages; direction-walks reserved for stuck/blocker
> escapes) and fire a Sonnet executor. Also pending: distinguish Maren's few-meter startup
> relocate (sweep GOTO_CENTER vs reset teleport) from the next run's first log lines; #19's a/b/c
> call still open.

## Git state at handoff

```
 M plan/backlog.md
(committed with this handoff, then pushed)
```

Recent commits:

```
2e7e52a backlog: tick #13.3 (merged, 42/42) + note stale maren seed caveat
0648b10 #13.3: cockpit deep-reset buttons
1ec969e backlog #13.3 (spec): cockpit buttons for reset_agents/reset_places
de31a4e fix: requires-python >=3.12 (PEP-701 f-string in llm_router) + tick #13.1/#13.2
d1904e0 merge #13.2 cockpit world-grid generation (worktree executor)
```
