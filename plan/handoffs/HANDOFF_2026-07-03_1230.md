# Handoff — grid cell reworked 4m→30m, B7b greeting standoff, #11.2 + #3/2.4 landed (all offline; PIE verify pending)

**Date:** 2026-07-03 12:30 local
**Branch:** auto-loop/backlog (9 commits ahead of origin at handoff; **pushed at session end per user**)
**Prior handoff:** [HANDOFF_2026-07-01_2200.md](HANDOFF_2026-07-01_2200.md)

## TL;DR

A big offline session on Opus 4.8: built **B7b** (personal-space stop, two mechanisms), finished
**#11.2** (owned places in prompt + web map) and **#3/2.4** (MCP tools attach to the runner), then —
from a top-down world screenshot the user shared — **reworked the grid cell size 4 m → 30 m** to fix a
grid≈place size collapse. Suite **31/31**, tree clean, everything committed + pushed. **Nothing is
verified live yet** — the next session (user doing PIE later today) should watch Dufus keep ~3 m from
people (greeting *and* passing) and watch the new 30 m grid map build out.

## What was done this session

- **B7b — personal space, two complementary mechanisms (both engine-agnostic, no C++):**
  - `3f14d5c` **reflex stop** — while moving, a mobile blocker within `_STANDOFF_CM` (300 cm) triggers
    an immediate bridge `stop` in `_observe_agent`, attaches `blocker.halted`, forces a re-decide.
  - `cdd20e9` **greeting/approach standoff** — the real fix for "walks into faces": `walk_to
    target_actor` now terminates 300 cm short by resolving the target's position and walking to a
    computed location instead (the reflex can't catch a move-to-actor that closes the whole gap within
    one ~9 s tick). If already within 300 cm, he stays put. `agent_manager._execute_world_action`.
- **`bf035eb` route map grid labels** — the #6b PNG now draws absolute col/row numbers in top/left
  gutters (same (col,row) vocabulary as the web map).
- **`548d48d` blocker classifier** — `apc` added to the "person" keywords so the user's `APC_BP` child
  blueprints still read as a person (B7/B7b keep working on them).
- **`606d476` #11.2 surfacing** — owned places now appear in `known_places`/the decision prompt
  (`(owner's place)` tag, anchor+offset position) and on the web `/map` (purple dot + tooltip + count).
  New `PlaceDB.all_owned_places()`.
- **`0f6066f` #3/2.4** — `simulation_tools.py` MCP tools are now thin `RunnerClient` attachers to the
  standalone `sim_runner`; no runner reachable = loud error, no in-process hosting. Runner API grew the
  full director surface (pause/resume/agents/goal/pulse/resets/resync/world_grid); `generate_world_grid`
  moved into `AgentManager`. New `test_sim_tools_attach.py`.
- **`35578c6` grid cell rework 4 m → 30 m** (the session's headline) — user's call from a whole-world
  top-down screenshot: a 4 m grid cell ≈ the 3 m place cell, collapsing the "district holds several
  place cells" hierarchy (and 119×78 = 9,282 cells was absurd to 360-sweep). Set `cell_size` 3000 cm;
  MCP_World now **17×12 = 204 cells**. `generate_world_grid` default → 3000 everywhere. **Fixed a latent
  bug:** `PlaceDB.reset()` didn't clear the new `owned_place_cells` table — it does now. MCP_World place
  DB was reset (rows were keyed under the old 4 m grid). Web `/map` gridlines darkened (`#8b95a1`) so the
  cells are actually visible (were pale-on-pale).
- **`fc1b304` backlog #6c** — new item: overlay grid + place cells on the real world PNG
  (`Python/web_ui/images/world_map_view.png`, now tracked). The item's substance is the
  **world↔pixel registration** problem (UE X/Y orientation + row0=north; capture is whole-world framed
  so bounds→pixel linear is a viable first cut). First consumer of the #11.2 `extent_cm`.
- **Memory:** reinstated handoffs (`feedback_no_handoffs` flipped — user: "I still like handoffs");
  added `project_grid_place_cell_sizes` (grid = 30 m district, place = 3 m spot, don't let them
  converge).

## In flight

_None._ All work committed; tree clean; suite 31/31.

## Decisions locked in this session

- **Handoffs are back ON** (user, 2026-07-03) — write HANDOFF files again *and* keep the backlog Status
  current. (Reversed the 2026-06-26 "no handoffs".)
- **Grid cell = 30 m (3000 cm); place cell = ~3 m default, buildings bigger via `extent_cm`.** Grid is a
  navigation *district* that holds several place cells; the two must stay different orders of magnitude.
  Don't let `cell_size` drift back toward place scale.
- **B7b is engine-agnostic, no C++/no RVO** — reflex stop + command-level greeting standoff, both in
  Python. The ~9 s tick cadence is *why* the command-level standoff (not the reflex alone) is needed.
- **#3 = attach, don't host** — MCP tools never host an AgentManager; the runner owns the sim lifetime +
  the single Unreal socket. No in-process fallback (that would double-own the socket).
- **#6c registration is the crux, not the drawing** — captured in the backlog item; calibration method
  is still an open decision (see below).

## Open questions

- **#6c calibration method** (decide before building): assume-covers-bounds (simplest; works only if
  every capture is a whole-world ortho) vs. a stored `world_map_view.json` extent+orientation vs. an
  in-UI 2-point click-calibration (most robust). Also: is the capture orthographic (clean linear map) or
  perspective (needs corner homography)? One shared background for web map + #6b route map, or separate?
- **#10.5 friend-interrupt path still unobserved live** (carried from prior handoffs — the two NPCs
  haven't met in range yet).

## Next concrete step

**PIE-verify the two movement fixes and the new grid** (user is doing PIE later today): start the sim in
PIE and watch that (1) Dufus stops **~3 m short** of people both when greeting and when passing (B7b —
`3f14d5c` + `cdd20e9`), and (2) the web `/map` shows the new **30 m, 17×12 grid** building out with
visible cells as agents explore (the place DB was wiped, so it starts empty). If Dufus still closes in,
check whether the LLM is using `walk_to target_actor` (standoff applies) vs `follow_character` (it does
not) and whether the target resolves to a known agent name.

## Files touched this session

- `Python/agent_runtime/agent_manager.py` — B7b reflex stop + greeting standoff; `generate_world_grid`
  moved in + default 3000; `known_places` includes owned places
- `Python/agent_runtime/llm_router.py` — `blocker.halted` sense line + doctrine; owned-place owner tag;
  route-map note mentions edge col/row labels
- `Python/agent_runtime/route_map.py` — col/row gutter labels on the PNG
- `Python/agent_runtime/place_db.py` — `all_owned_places()`; `reset()` now clears `owned_place_cells`
- `Python/agent_runtime/runner_app.py` + `runner_client.py` — full director surface; world_grid default 3000
- `Python/tools/simulation_tools.py` — rewritten as RunnerClient attachers; world_grid default 3000
- `Python/web_ui/main.py` — `/api/map` carries `owned`
- `Python/web_ui/templates/map.html` — owned-place dots + legend; darkened gridlines
- `Python/worlds/MCP_World/world_grid.json` — `cell_size` 400 → 3000 (place DB reset out-of-band)
- `Python/web_ui/images/world_map_view.png` — **new** (tracked asset for #6c)
- `Python/scripts/agent_runtime/` — `test_sim_tools_attach.py` (new); extended `test_blocker_sense.py`,
  `test_route_map.py`, `test_map_query.py`, `test_map_view.py`, `test_owned_places.py`,
  `test_prompt_context.py`, `test_runner_api.py`
- `plan/backlog.md` — status banners, #11 sizes resolved, #11.2/#3.2.4 landed, new #6c
- memory: `feedback_no_handoffs.md` (reversed), `project_grid_place_cell_sizes.md` (new), `MEMORY.md`

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: PIE-verify the two B7b movement fixes (Dufus stops ~3 m short of people when greeting AND passing)
> and watch the new 30 m / 17×12 grid map build out from empty as agents explore.

## Git state at handoff

```
(clean — all committed)
```

Recent commits:

```
fc1b304 backlog #6c: overlay grid + place cells on the real world PNG
cdd20e9 B7b: stop a standoff short when walking up to a character (greeting)
35578c6 rework grid cell size: 4m -> 30m district (grid >> place)
f50d3bb backlog: #11.2 surfacing + #3/2.4 runner attachers landed (suite 31/31)
0f6066f #3/2.4: MCP sim tools attach to the runner (attach, don't host)
```
