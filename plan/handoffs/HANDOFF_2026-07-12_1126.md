# Handoff — Clean landmark state and SR14 proves corrected intent

**Date:** 2026-07-12 11:26 local (America/Chicago)
**Branch:** `auto-loop/backlog`
**Prior handoff:** [HANDOFF_2026-07-12_1023.md](HANDOFF_2026-07-12_1023.md)

## TL;DR

The user corrected the authored vegetable-truck spelling and added
`Landmark_Community_village_square`. The live failures were then traced to stale runtime place data:
an old community `village square` at Dufus's start cell collided with the new authored square. The
world/agent runtime data was cleaned, reset behavior now also clears episodic recall, and the cockpit
decision feed now filters itself to the current sim run without deleting durable replay history.
`stop_sim.bat` was added and user-verified. SR14 was stopped cleanly at 19 ticks to protect the user's
remaining credits. Maren stayed at the vegetable truck throughout; Dufus correctly recognized that he
was not at the square and repeatedly traveled east toward it. Arrival was not reached/confirmed before
shutdown, and he reported one obstacle/stuck recovery event, so navigation robustness remains in flight.

## What was done this session

- Drove the web cockpit through the in-app browser with the user supplying PIE as visual truth.
- Diagnosed SR13: Maren behaved correctly at the truck, while Dufus treated a stale runtime
  `village square` in his start cell as authoritative and wandered locally around the barn/canopy.
- Cleaned the live world/agent runtime state and resynced authored landmarks. Verified the database
  contained only the authored community square plus three authored owned places:
  - community `village square` at map cell `(8,5)`;
  - Dufus `home` at `(10,5)`;
  - Maren `vegetable truck` at `(5,6)`;
  - Maren `home` at `(11,8)`.
- Cleared both agents' derived spatial/episodic state while preserving intended seed memory.
- Changed agent reset so it clears episodic recall as well as ordinary/spatial runtime state.
- Changed recent-decision retrieval so the cockpit automatically shows only the active sim run;
  durable `agent_decisions.log` history remains available for replay/diagnostics.
- Added `Python/stop_sim.bat`, which shuts down the runner and web UI; the user ran it and confirmed
  it works.
- Synced the clean world and ran SR14. Evidence from the run:
  - wake: Dufus identified a residential area and set the goal of reaching the village square;
  - wake and every subsequent decision: Maren identified the vegetable truck and chose `idle`;
  - Dufus moved from grid cell `-4,-1` to `-3,-1`, repeatedly chose successful eastbound
    `walk_to` actions, later reported an obstacle/stuck condition, and used `observe_heading` while
    continuing toward the square;
  - no false "I am at the village" conclusion recurred.
- Stopped SR14 through `POST /stop`: `ticks=19`, `elapsed_seconds=269.4`.
- Targeted reset/decision-feed/runner API tests passed after their changes. Immediately before the
  handoff checkpoint was committed and pushed, `python scripts/run_tests.py` passed **42/42**.

## In flight

- #27 navigation/controller work is materially improved but not accepted end-to-end: SR14 proved
  correct semantic destination selection and real eastbound progress, but not arrival at the authored
  village square.
- Dufus encountered at least one obstacle and reported being stuck before trying to route around it.
  The next live run should establish whether he eventually arrives or cycles near downtown geometry.
- The first SR14 perception tick took about a minute and temporarily made `/status` appear frozen.
  While a long tick was in progress, cockpit polling accumulated many TCP connections on port 8777.
  This is strong evidence for backlog #20 timing/instrumentation and possibly runner status isolation.
- The backlog active view predates this session's SR13/SR14 evidence. This handoff records the
  mismatch rather than hurriedly rewriting backlog priority with only 3% user credits remaining.
- All prior product/planning/skill changes and this session's changes remain uncommitted in one dirty
  worktree.

## Decisions locked in this session

- Authored landmark JSON is ground truth; stale runtime discoveries must not compete with it after a
  world reset/sync.
- Reset agents means a genuinely fresh behavioral run, including episodic recall, but durable replay
  logs are not erased.
- The cockpit's Recent decisions panel is per-run by default; historical decisions stay on disk.
- The shutdown script is the normal clean exit for both Python servers and the web page.
- Codex may drive sync/start/stop and dismiss cockpit prompts; the human runs PIE and performs visual
  acceptance while this operator workflow matures.

## Open questions

- Can Dufus complete the remaining route to the authored square, or does downtown collision geometry
  still produce a loop after the observed stuck event?
- Should runner status/control be served independently of the long tick lock so the cockpit remains
  responsive during perception sweeps?
- Should `stop_sim.bat` also close or navigate away the in-app browser tab? It currently shuts down
  the server processes; the user confirmed that behavior is useful.
- Should clean-house/reset include any additional derived files beyond episodic, spatial, runtime,
  and place rows?

## Relevant backlog items

- **#27 Navigation executive + deterministic movement controller:** live evidence improved; destination
  collision fixed, route progress proven, arrival and obstacle recovery still pending.
- **#20 Movement/decision timing instrumentation:** now has concrete SR14 evidence; the first tick
  blocked status for roughly a minute and full run cadence was much slower than the configured 2 s.
- **#29 Cockpit path containment:** still the groomed active-view `Now` item; untouched this session.
- **#30 Direct `httpx` dependency:** still queued and untouched.
- **#9 Dev mode / Codex operator:** browser-driven cockpit start/sync/stop works in supervised baby
  steps; log triage was performed directly from the run artifacts.

## Next concrete step

Run one fresh PIE acceptance pass from the clean authored database and watch Dufus until he either
arrives at `Landmark_Community_village_square` or repeats a specific blocked route segment; stop as
soon as either terminal result is captured.

## Files touched this session

- `Python/agent_runtime/episodic_memory.py` — added reset support.
- `Python/agent_runtime/agent_manager.py` — agent reset now clears episodic recall; current-run decision
  feed wiring is included among the broader uncommitted manager changes.
- `Python/agent_runtime/memory_store.py` — optional sim-run filtering for recent events.
- `Python/scripts/agent_runtime/test_pacing_and_reset.py` — episodic reset regression coverage.
- `Python/scripts/agent_runtime/test_decision_feed.py` — current-run feed regression coverage.
- `Python/stop_sim.bat` — clean server shutdown helper, user-verified.
- `Python/worlds/MCP_World/agents/{dufus,maren}/state.json` — captured start transforms remain modified.
- Runtime-only world database, memories, observations, episodes, and decision logs changed during the
  clean reset and SR14; most are ignored and should not be committed.
- All files listed in the prior handoff remain dirty; this session did not isolate or commit them.
- `plan/handoffs/HANDOFF_2026-07-12_1126.md`, `LATEST.md` — this snapshot.

## How to resume

Open this repository as the Codex project and say:

> Read hand off.

For the next supervised live check, start PIE and `Python/start_sim.bat`, then say:

> PIE and servers ready. Continue the clean Dufus route acceptance run.

## Git state at handoff

Branch: `auto-loop/backlog`, tracking `origin/auto-loop/backlog`. Latest commit remains `0524b0e`.
Nothing was staged, committed, pushed, or merged.

Before writing this handoff, `git status` showed 24 tracked files modified and untracked project-local
skill packages, `Python/stop_sim.bat`, and recent handoffs. `git diff --stat HEAD` reported 24 tracked
files changed, 856 insertions, and 195 deletions; the new handoff adds another untracked file and
updates `LATEST.md`. Preserve the dirty worktree and review changes before any commit.
