# Handoff — Architect role adopted; WP1–WP5 specs written + WP1–WP4/B5 built; #10.5 verified live in PIE

**Date:** 2026-07-01 18:15 local
**Branch:** auto-loop/backlog (unpushed — 12 commits ahead of the prior handoff's `6a75e20`)
**Prior handoff:** [HANDOFF_2026-07-01_1143.md](HANDOFF_2026-07-01_1143.md)

## TL;DR

User assigned model roles: **Fable = architect** (design-complete specs so Opus/Sonnet execute
without judgment calls), tracked in memory `feedback_fable_architect_role`. This session wrote five
executor-ready work packages under `plan/specs/` (WP1–WP5), then built four of them (WP1–WP4 + B5)
directly since the user said "chip away." Two live PIE runs verified the results: **#10.5 (the
balanced reaction gate) is DONE and confirmed working** — a real bug was found and fixed between the
runs (travel intent led with the activity, causing en-route greeting; now destination-first). Suite
**28/28**, all committed to `auto-loop/backlog`, **never pushed**. One new live-observed bug is
queued (**B7**: Dufus's pawn walks through people instead of around them — not yet worked).

## What was done this session

- **Architect pass** (`65dd549`) — wrote `plan/specs/README.md` (executor contract: preflight, order,
  per-package cycle, never push, stop conditions) + five specs, each with exact files/functions/tests
  and every design call pre-made:
  - **WP1** (`plan/specs/WP1-recall-context.md`) — architect *finding*: `acquaintances`/`known_places`/
    `recent_episodes` were computed every tick in `_perceive_and_decide` but never rendered into
    `_USER_TEMPLATE_VISION` — social recall (#5) and the map query (#6) were invisible to the LLM.
  - **WP2** (`WP2-reaction-gate.md`) — #10.5 BALANCED gate offline slice.
  - **WP3** (`WP3-sweep-capability.md`) — A4/#11.1 collapse the maintenance role into a capability any
    APC invokes.
  - **WP4** (`WP4-owned-places.md`) — A5/#11.2 owned place cells, **GATED** on user approval.
  - **WP5** (`WP5-apc-topdown-map.md`) — #6b top-down map, **design answers only, do not build**.
- **WP4 built** (`15db6e9`) — user approved same-session ("WP4 approved, build the minimal slice").
  `owned_place_cells` table (named ~3m box, XY offset from the community anchor); `PlaceDB
  .add_owned_place`/`find_owned_place`/`owned_places_in_cell`; `_resolve_place_target` falls back
  community→owned; `_record_place` stores a second name in an already-named cell as owned instead of
  dropping it. Test: `test_owned_places.py`.
- **WP1 built** (`dd13951`) — three new prompt sections (People You Know / Places You Know / Relevant
  Past Moments), pure renderers, wired into `decide()`. Test: `test_prompt_context.py`.
- **WP2 built** (`c494461`) — "What Wins Right Now" priorities block (routine wins; only a known
  person or being spoken to interrupts; resume after); exploration gated on empty schedule;
  `_schedule_note`'s travel branch states the destination as the priority. Contract-pinned by string
  tests; **live tune flagged as still needed** (turned out to be right — see below).
- **WP3 built** (`4b7cbce`) — sweep is now a capability (`_should_sweep_here`, `_sweep_step`,
  `_pulse_sweep`), gated on schedule status act/idle in an unexplored cell (never mid-travel). Deleted
  `_pulse_maintenance`, `_maintenance_tick_action`, `_nearest_unexplored_target`, `Agent.is_maintenance`.
  Rewrote `test_cell_sweep.py` to the capability model.
- **B5 built** (`e575f01`) — **the assumed C++/bridge handler for `observe_heading` was unnecessary.**
  Composed in Python from the wake look-around's existing primitives (`set_facing` + `capture_view` +
  perceive + `ingest_compass`) in new `_execute_sweep_observe`. No editor rebuild required. Offline
  test drives a full 8-heading sweep end-to-end.
- **Live PIE run #1** (~22:56–22:58Z, 14 ticks) — triaged via decision log + `world_places.db`. Found:
  Maren behaved perfectly (stayed at post, in-character). Dufus resisted 4 consecutive stranger
  sightings while traveling (the gate visibly working), **then broke** at a gas station — 3 ticks of
  greeting a stranger instead of arriving. Root cause: the travel `intent` string led with the
  activity ("It's time to greet passers-by — head to village square"), so the LLM read that as
  license to start greeting immediately. Also found Maren's `character.md` named the raw actor
  `veh_VegitableTruck2`, which leaked into her generated schedule as a target name.
- **Fix + re-verify** (`2f2aece`, `20f7f7b`) — `planner.step`'s travel intent rewritten
  destination-first ("Head to \<place\> first — your scheduled activity there is: \<activity\>. It
  starts when you arrive."); `_schedule_note` adds "Do NOT start the scheduled activity on the way";
  Maren's `character.md` now says "the vegetable truck"; cleared both agents' cached
  `daily_schedule`/`last_activity` in `runtime.json` so they regenerate clean. **Live PIE run #2**
  (~23:12–23:13Z, 8 ticks): the gate held **every tick** — "I see two unknown people ahead... but I
  gotta keep heading", "my routine says to head straight to the [square]". Backlog updated: #10.5 and
  B3 marked done+verified.
- **New live-observed bug queued as B7** (not worked this session): Dufus's pawn walks *through*
  people instead of around them (one `walk_to` returned `error` mid-approach to a sidewalk NPC).
  Diagnosed as pawn-vs-pawn steering, not a decision problem — recommended fix is RVO avoidance on
  `BP_CameraNPC`'s CharacterMovement + a MoveTo acceptance radius, not an LLM change.
- **Memory:** added `feedback_fable_architect_role.md` (+ MEMORY.md index line) — Fable defaults to
  architecting (specs in `plan/specs/`), not implementing, going forward.

## In flight

_None._ Tree clean, all work committed on `auto-loop/backlog` (unpushed). Suite 28/28.

## Decisions locked in this session

- **Model roles:** Fable = architect (writes `plan/specs/*` work packages), Opus/Sonnet = executors
  (build them without design calls). See memory `feedback_fable_architect_role`.
- **WP4/#11.2 approved** — user: "WP4 approved, build the minimal slice." Built same session (see
  above). The fuller model (surfacing owned places to the LLM/map, multi-leg grid routing) stays
  deferred to #6b.
- **#10.5 BALANCED gate — confirmed correct as designed**, but needed the destination-first intent fix
  to actually hold; that fix is now the load-bearing piece, not the priorities-block wording alone.
- **B5 needs no C++** — the backlog's "C++/editor rebuild" framing for `observe_heading` was stale;
  don't assume a bridge/C++ gap without checking what primitives already exist (the wake sweep already
  proved rotate+capture works).
- **WP5/#6b still NOT approved** — design answers are written (`plan/specs/WP5-apc-topdown-map.md`)
  but the user has not signed off; do not build.

## Open questions

- **WP5 sign-off** — 5 checkboxes at the bottom of `plan/specs/WP5-apc-topdown-map.md` (semantic text
  map vs image, corridor+1 pad cap 15, separate renderer over shared PlaceDB, injection timing, build
  order vs WP4). Needs a plain yes/no from the user, no PIE required.
- **B7 fix approach** — recommended (a) RVO avoidance checkbox on `BP_CameraNPC` + (b) MoveTo
  acceptance radius; not yet decided/built. Editor + bridge code.
- **Friend-interrupt path of #10.5 is untested** — the two NPCs haven't been in range of each other
  yet in these runs, so "a known friend interrupts, then resumes" hasn't been observed live, only the
  "no interrupt for strangers" half.

## Next concrete step

Either: (a) open the editor and PIE, add RVO avoidance to `BP_CameraNPC`'s CharacterMovement
component (one checkbox) to start on **B7**, or (b) get the user's WP5 sign-off (no PIE needed) and
build the APC top-down map per `plan/specs/WP5-apc-topdown-map.md`. Both are independent — pick
whichever the user wants to do first next session.

## Files touched this session

- **new specs:** `plan/specs/README.md`, `plan/specs/WP1-recall-context.md`,
  `plan/specs/WP2-reaction-gate.md`, `plan/specs/WP3-sweep-capability.md`,
  `plan/specs/WP4-owned-places.md`, `plan/specs/WP5-apc-topdown-map.md` (specs carry "Executor notes"
  updated post-build for WP1–WP4)
- **new tests:** `Python/scripts/agent_runtime/test_owned_places.py`,
  `Python/scripts/agent_runtime/test_prompt_context.py`
- **changed (runtime):** `Python/agent_runtime/place_db.py` (owned_place_cells + CRUD),
  `Python/agent_runtime/agent_manager.py` (`_resolve_place_target` owned fallback, `_record_place`
  owned write path, WP1 wiring N/A here — that's llm_router.py, WP3's `_should_sweep_here`/
  `_sweep_step`/`_pulse_sweep` replacing `_pulse_maintenance`/`_maintenance_tick_action`/
  `_nearest_unexplored_target`, B5's `_execute_sweep_observe`), `Python/agent_runtime/agent.py`
  (deleted `is_maintenance`), `Python/agent_runtime/llm_router.py` (WP1 renderers + template sections,
  WP2 priorities block + gated exploration + `_schedule_note` rewording, B3 fix's "Do NOT start the
  scheduled activity on the way"), `Python/agent_runtime/planner.py` (B3 fix: destination-first travel
  intent)
- **changed (tests):** `Python/scripts/agent_runtime/test_cell_sweep.py` (rewritten to the capability
  model), `Python/scripts/agent_runtime/test_pacing_and_reset.py` /
  `Python/scripts/agent_runtime/test_world_grid.py` (dropped dead `is_maintenance` stub attr),
  `Python/scripts/agent_runtime/test_planner.py` (pin destination-first intent)
- **changed (world data):** `Python/worlds/MCP_World/agents/maren/character.md` (de-engined truck
  name), `Python/worlds/MCP_World/agents/dufus/runtime.json` +
  `Python/worlds/MCP_World/agents/maren/runtime.json` (cleared cached `daily_schedule`/
  `last_activity` so they regenerate clean — **not committed**, these are per-run state)
- **changed (docs):** `plan/backlog.md` (A5/A6/A7/A4/#11.1/#10.5/B3 checked off with spec pointers;
  B5/#7 live-half marked done-no-C++; new B7 item), `plan/handoffs/HANDOFF_2026-07-01_1143.md` +
  `LATEST.md` (committed from the prior session, had been dangling)
- **memory (outside repo):** `feedback_fable_architect_role.md` + `MEMORY.md`

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: either open PIE and add RVO avoidance to `BP_CameraNPC` to start on B7 (Dufus walks through
> people), or get WP5 sign-off (`plan/specs/WP5-apc-topdown-map.md`, no PIE needed) and build the APC
> top-down map.

## Git state at handoff

```
(clean — all committed)
```

Recent commits:

```
20f7f7b backlog: B3/#10.5 verified live (gate held every tick); add B7 pawn collision
2f2aece B3 fix from live run: destination-first travel intent + de-engine Maren's truck
e575f01 B5: observe_heading — composed in Python, no C++ handler needed
4b7cbce WP3: sweep is a capability any APC invokes (#11.1 / A4)
c494461 WP2: balanced reaction gate — offline slice (#10.5)
```
