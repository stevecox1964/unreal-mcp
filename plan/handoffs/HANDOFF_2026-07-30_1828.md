# Handoff — Refocused the project: Dufus = silent surveyor, Maren parked, dead files removed; live run blocked on Unreal bridge not listening

**Date:** 2026-07-30 18:28 local
**Branch:** `main`
**Prior handoff:** [HANDOFF_2026-07-29_2145.md](HANDOFF_2026-07-29_2145.md)

## TL;DR

The user called a direction reset: months of iteration weren't converging on the main goal (an AI that
looks at a world and navigates/understands it), so this session took a holistic view (backlog + two deep
codebase explorations) and executed the refocus — **Dufus is now a silent, survey-only APC** (single
all-day expedition agenda, no speech, no hat/friends/square-reporting), **Maren is parked**
(`is_active: false`), and ~20 definitively dead files were deleted. Suite 55/55 green, all uncommitted.
The live verification run **did not happen**: Unreal's bridge port 55557 never came up ("used to work"
per the user, who was mid-diagnosis at the editor when they stopped to reboot). **Next session: get PIE +
the 55557 listener up, then run the sim and watch Dufus survey.**

## What was done this session

- **Holistic review** (user's ask: "take a holistic view of the code and achievements"): read the backlog
  active view + items #35/#36/#48–#53, ran two Explore agents (Dufus config + tick loop end-to-end;
  full subsystem inventory). Key findings that drove the plan:
  - Survey is **entirely engine-side** (`survey_priority: true` + the deterministic interrupt →
    walk → 4-heading capture → composite → PlaceDB machinery, no LLM needed). Dufus's authored files
    never mentioned surveying — what the LLM was juggling was a 12-task daily-life agenda (breakfast,
    hat hunt, 5 square social blocks), goals.md pressure ("be friends with everybody"), and a
    greet-everyone rule. That competition **was** the "won't converge" problem.
  - Only **6 of 204 grid cells (~3%)** have a current community survey; `place_images` = 10 rows.
  - `agent_manager.py` is ~4,000 lines with no seams (sense/day/interrupts all interleaved) — every
    past fix landed there as a compensator. Noted, deliberately not touched.
- **Dufus → survey-only** (backlog **#52 RESOLVED**, all edits in
  `Python/worlds/MCP_World/agents/dufus/`):
  - `agenda.json` — replaced 12 tasks with one: `survey_expedition` 07:00–22:00, empty `place`,
    `time_block_ends`. Validated via `agenda.validate_document` (agenda revision bump auto-invalidates
    the stale runtime execution ledger).
  - `goals.md` — surveyor motivation only: see unseen ground, survey every cell entered, never re-walk
    covered ground. Removed: hat, friends, report-to-square/home-base.
  - `character.md` — role re-pointed to "the village's surveyor — an accidental explorer"; kept the
    cheerful-fool voice; dropped hat backstory.
  - `rules.md` — removed greet-everyone; added "your one job is to survey"; kept JSON-only,
    no-invention, FOOTING, PROGRESS WARNING rules.
  - `tools.json` — down to `idle, wander, walk_to, observe, remember` (dropped `speak_to`,
    `inspect_object`, `follow_character`, `attack`, `flee`).
  - `state.json` untouched (`survey_priority: true`, `tier: 1`/Sonnet-5 already set).
- **Maren parked** — `agents/maren/state.json` `is_active: false`. She still spawns/binds (harmless)
  but never wakes/ticks. One flag to revive.
- **Dead-file cleanup** (user-approved): deleted `Python/scripts/{actors,blueprints,node}/` (March-2025
  MCP demos), `Python/npc_cli.py` (broken), `Python/fix_nav_mesh.py` (memory: "the wrong answer"),
  `Python/logs/` (orphan), egg-infos, `Python/unreal_mcp.log`, `.mcp.json.disabled`,
  `master_plan.html`, `still_todo.md`, all of `Docs/Tools/`, stale `.claude/worktrees/`. Fixed the
  three live references (README.md, Docs/character_system.md, run_tests.py docstring). Kept:
  explorer.py, run_replay.py, ollama_adapter.py, spatial_memory.py (functional, just quiet).
- **Fixed a real pre-existing bug from #51** (found via the suite): `_attach_run_log` opened a root
  `FileHandler` on `logs/sim_runner.log` that was never closed, holding the file open on Windows
  (failed `test_pacing_and_reset` temp-dir cleanup). `agent_manager.py` now tracks
  `self._run_log_handler` and `stop_simulation` detaches+closes it.
- **Verification (offline):** full suite **55/55**; new agenda validates; both agents smoke-load
  correctly (Dufus active, single-task agenda, narrowed actions; Maren inactive).
- **Bookkeeping:** backlog #52 marked RESOLVED with the decisions; memory
  `project_dufus_vlm_training_corpus` + MEMORY.md index updated (survey-only, Maren parked).
- **Live-run attempt (failed, diagnosed):** neither `sim_runner` nor the web UI was running when the
  user first tried ("nothing happened" — stale logs were from SR30/Jul-24). Started both from this
  session, user then relaunched via `start_sim.bat` (its port-kill replaced mine — expected). Both
  Python services healthy. **`POST /start` fails with "No agents could be bound": Unreal's 55557
  bridge listener is not up.** User: "Hummm. used to work. hold" → rebooting.

## In flight

- **The live verification run** — everything is staged for it, blocked on the Unreal bridge.
  Python side verified healthy; Unreal side (editor open → PIE → Output Log shows the 55557 listener)
  never came up this session. No C++/plugin changes were made this session or last, so a rebuild
  should not be needed; the reboot may clear it.

## Decisions locked in this session

- **Dufus is survey-only and silent** (user chose both): one all-day expedition agenda block, no
  speech tools, no social/daily-life goals. Do not re-add ordinary-life load to him. His survey
  captures are the **custom-VLM training corpus** — that's the point of the task, not just exploring
  (user's explicit framing; see #49/#53).
- **Pure roam, no home-base loop** — no periodic return-to-square blocks; maximum coverage per run.
- **Dufus-only, no generic "surveyor role" abstraction yet** — generalize when a second surveyor exists.
- **Maren stays parked until Dufus reliably does his one job.**
- **Shared prompt code untouched** — the greeting block in `_USER_TEMPLATE_VISION` is moot with no
  other active agent; surgical scope held.
- **Dead files deleted, not archived** (user approved the list).

## Open questions

- **Why is Unreal's 55557 listener down?** "Used to work." Unknown: editor not in PIE vs. plugin not
  loading vs. something the reboot fixes. First thing to check next session (Output Log at PIE start,
  filter "MCP"/"55557").
- **Does survey-only Dufus actually survey?** The whole point — unverified until a live run. Success
  criteria are in the backlog #52 note: coverage grows past 6/204, `place_images` past 10, zero
  home/square/social detours, `sim_runner.log` accounts for every tick.
- **FOOTING/PROGRESS WARNING efficacy** (carried from 2026-07-29): still never observed live.
- **"Reset agents" before the run?** Recommended once (clears Dufus's old-life memories that would
  surface as "Relevant Past Moments"); user hadn't pressed it yet. Do NOT "Reset places" — that wipes
  the survey corpus.
- **Cockpit "Unreal not connected" banner** — today the only signal is a buried bind error; #13
  hardening gap, raised again this session. Worth building soon.
- Carried: #50 Sonnet A/B evaluation, #49 capture-pose metadata (needs user go-ahead), #53 design.

## Next concrete step

Get Unreal serving the bridge (open project → PIE → Output Log shows the 55557 listener), then
`Python\start_sim.bat`, press **Reset agents** then **Start** in the `/sim` cockpit, and watch
`Python/worlds/MCP_World/logs/sim_runner.log`: Dufus should immediately be on `survey_expedition`,
survey the cell he stands in, then push outward into unsurveyed cells with no home/square detours.

## Files touched this session

Modified:
- `Python/worlds/MCP_World/agents/dufus/{agenda.json,goals.md,character.md,rules.md,tools.json}` —
  survey-only rewrite (see above)
- `Python/worlds/MCP_World/agents/maren/state.json` — `is_active: false`
- `Python/agent_runtime/agent_manager.py` — `_run_log_handler` tracked; closed/detached in
  `stop_simulation` (Windows file-lock bugfix)
- `Python/scripts/run_tests.py` — docstring no longer references deleted script dirs
- `README.md`, `Docs/character_system.md` — dangling references to deleted files removed
- `plan/backlog.md` — #52 marked RESOLVED with decisions + VLM framing
- `plan/handoffs/LATEST.md` — points at this handoff

Deleted (git rm): `Docs/Tools/` (5 files), `Python/fix_nav_mesh.py`, `Python/npc_cli.py`,
`Python/scripts/{actors,blueprints,node}/` (7 files), `master_plan.html`, `still_todo.md`.
Deleted (untracked): `Python/logs/`, `Python/unreal_mcp.log`, `Python/unreal_mcp.egg-info/`,
`Python/unreal_sim.egg-info/`, `.mcp.json.disabled`, `.claude/worktrees/`.

Memory (outside repo): `project_dufus_vlm_training_corpus.md` + `MEMORY.md` updated.

**Nothing is committed** — the user was heading to a live verify before committing; say "commit and
push" next session once the live run looks right (or commit first if preferred — suite is green).

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: get Unreal's 55557 bridge listener up (PIE + Output Log check), then start the sim via
> `Python\start_sim.bat`, press Reset agents then Start in the cockpit, and watch
> `Python/worlds/MCP_World/logs/sim_runner.log` to verify survey-only Dufus surveys outward with no
> daily-life detours.

## Git state at handoff

```
D  Docs/Tools/README.md
D  Docs/Tools/actor_tools.md
D  Docs/Tools/blueprint_tools.md
D  Docs/Tools/editor_tools.md
D  Docs/Tools/node_tools.md
 M Docs/character_system.md
 M Python/agent_runtime/agent_manager.py
D  Python/fix_nav_mesh.py
D  Python/npc_cli.py
D  Python/scripts/actors/test_cube.py
D  Python/scripts/blueprints/test_create_and_spawn_blueprints_with_different_components.py
D  Python/scripts/blueprints/test_create_and_spawn_cube_blueprint.py
D  Python/scripts/node/test_component_reference.py
D  Python/scripts/node/test_create_bird_blueprint_with_input_and_camera.py
D  Python/scripts/node/test_input_mapping.py
D  Python/scripts/node/test_physics_variables.py
 M Python/scripts/run_tests.py
 M Python/worlds/MCP_World/agents/dufus/agenda.json
 M Python/worlds/MCP_World/agents/dufus/character.md
 M Python/worlds/MCP_World/agents/dufus/goals.md
 M Python/worlds/MCP_World/agents/dufus/rules.md
 M Python/worlds/MCP_World/agents/dufus/tools.json
 M Python/worlds/MCP_World/agents/maren/state.json
 M README.md
D  master_plan.html
 M plan/backlog.md
 M plan/handoffs/LATEST.md
D  still_todo.md
?? plan/handoffs/HANDOFF_2026-07-29_2145.md
```

Recent commits:

```
42fdd59 feat: recognition, agenda ledger, durable runner log, guardrail facts
ab60c71 feat: add deterministic APC agendas
e702dd8 docs: approve JSON APC agenda design
ffa04ec docs: record no-handoff preference
72e5cda feat: move APC chat to dedicated page
```
