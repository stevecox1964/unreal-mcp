# Handoff — Survey phase closed (SR57 PASS, #103 built, SR58 scanned); Dufus is a townsperson; Play mode is the default; first Play run still pending

**Date:** 2026-09-01 20:43 local
**Branch:** main
**Prior handoff:** HANDOFF_2026-09-01_1826.md

## TL;DR

The survey era for this world is over: SR57 passed #101/#102 with zero wedges, #103 (best open stand
point) was built and seen working in SR58, and the user decided the map is done. Dufus was rewritten as
a villager who walks a schedule and talks (#105); the survey persona lives in
`agents/surveyor/` as an inactive template; **Play is the default sim mode**. SR59 (first Play run) ran
without Dufus because I saved his goals file in cp1252 — fixed and pushed. **Next: the user restarts the
runner + web app, runs Play with Dufus and Maren, and Claude scans the logs.** Everything is pushed to
`origin/main` (`58ced52`).

## What was done this session

- **SR57 scan → PASS** (`03eca10`): 127 ticks, 28 cells surveyed, zero stuck ticks, `footing_recoveries=0`,
  7 cells refused on tick 1 by the #101 path test (the SR56 carport wedge among them). Gap found and
  fixed (`710e1e1`): `interrupt_failed` decision-log events now carry `outcome: "survey abandoned — <reason>"`,
  `path`, `path_end_gap_cm` (`_abandon_survey_travel` stashes into `_survey_abandons`;
  `_terminate_active_interrupt(extra=…)`; `memory_store.record_interrupt_event` snapshot keys).
- **#104 spec** (parked): interesting-first survey — rank cells by unexplored road continuation,
  structure density, footing novelty, frontier value; nearest-first as tiebreak.
- **#103 spec'd, built by a Sonnet agent, reviewed by Fable, pushed** (`c4ff07a`, `7aa51f5`):
  C++ `get_character_radar` gains an optional `location` probe (projects to walkable ground, runs the ring
  + ground column there, adds `probe_origin`, `path`, `path_length_cm`, `path_end_gap_cm`); new pure
  `Python/agent_runtime/stand_point.py` (`candidates` / `score` / `choose`, 0.8 stay rule, vetoes);
  `agent_manager._choose_stand_point` wired into sweep creation; `place_cells.stand_x/stand_y` +
  `set_stand_point`; `/map` blue dot uses the stand point. **Review fix by Fable:** a chosen non-`here`
  point rebuilds the `CellSweep` with a 200 cm arrive tolerance (`_STAND_POINT_ARRIVE_CM`) — the
  explorer's whole-cell tolerance would otherwise have surveyed from the cell edge.
- **SR58 scan** (`4ee26a5`, findings under #103 in the backlog): stand points chosen for 4 cells, two
  surveyed 5 m off centre. Three bugs recorded, NOT fixed (user: "we are good for now"): (A) walk
  target z is the body's z re-issued each tick → drifts off walkable ground on slopes → false "no path"
  abandons; (B) no floor on minimum air (a 0.0 m spot won); (C) one walkable sliver of 17 lures the APC
  up the hill. Plus a cost note (skip ring 2 when ring 1 has zero ground). Dufus was not wedged — every
  remaining unsurveyed cell is a map-edge hill.
- **#105 Dufus → townsperson** (`395a766`): user copied `dufus/` to `surveyor/`; I set the copy to
  `agent_id: surveyor`, `is_active: false`, actor `APC_Surveyor_BP`, added a README, and deleted the
  copied runtime memory (observations 294 MB, episodes, spatial map, social, runtime, last_perception).
  Dufus: `survey_priority: false`, no `mission`, `speak_to` added / `survey_here` removed, survey
  doctrine import dropped, new character/goals/rules, eight-task agenda (home → Don's Donuts → village
  square → the vegetable truck 10–12 with Maren → Don's 12–13 with Maren → sheriff station square →
  village square → home). All eight places verified to resolve offline against `world_places.db`.
- **Play default** (`45562d1`): `runner_app.py`, `runner_client.py`, `web_ui/main.py`, `sim.html`
  dropdown, `_normalize_mode` fallback. `live` still aliases `survey`.
- **#106 spec** (`2de7e94`): run the world from the web app — roles as templates, agenda place check
  in the editor, active-APC list with memory reset, default mode on `/settings`.
- **SR59 post-mortem + fixes** (`dd9d27d`, `58ced52`): Dufus's `goals.md` was written cp1252 (em-dash
  byte 0x97) → `Agent.load` raised `UnicodeDecodeError` → runner skipped him silently in the run log.
  Re-saved as UTF-8; all three agents load. Cockpit agent list now filters `is_active === false`.
- Memory updated: `project_dufus_vlm_training_corpus.md` now records Dufus's role change and the
  surveyor template.

## In flight

- **First real Play run (SR60) not done.** SR59 had only Maren. The runner loads agents at process
  start (`_load_agents` in `AgentManager.__init__`), so the runner AND web app must be restarted before
  Dufus appears.

## Decisions locked in this session

- **Survey phase is closed for this world.** SR58's remaining cells are hills; no code for "map running
  out of places" will be written. #103's three bugs stay parked until a new world.
- **Dufus is a townsperson; the surveyor is a template folder**, not a second live APC. The survey
  CODE lives in the runtime, gated by `mission: survey` + `survey_priority`.
- **Play is the default mode.** Survey is opt-in from the dropdown.
- **Roles/templates/mode belong in the web app (#106)**, not in CLI sessions — spec'd, not built.
- Interest-first surveying (#104) is a shared body measurement; only the survey mission ranks cells
  by it. Decide the rest when picked up.

## Open questions

- Should `Agent.load` failures fail the run loudly (rule 12)? SR59 logged the error only to the runner's
  stdout, not `sim_runner.log`, and `SIMULATION START` just listed fewer agents. Not yet in the backlog.
- Dufus's `"home"` resolves to community cell (6,5) (mobile home community) rather than his owned
  `home` at (6,4) — same precedence quirk Maren has; harmless for now.
- Pre-existing "navmesh" wording in `llm_router._sense_note` (~line 923) still contradicts the #101
  vocabulary doctrine. Untouched.
- Tests referencing the removed `explore` mode are still broken (speed mode: don't touch tests).

## Next concrete step

After the user restarts the runner + web app and runs Play (SR60) with Dufus and Maren, scan
`Python/worlds/MCP_World/logs/sim_runner.log` and `agent_decisions.log` for: both agents in
`SIMULATION START`, Dufus following his agenda (walk_to resolved places, `speak_to` when Maren is in
view at the truck 10–12 and Don's 12–13), and any `PREFLIGHT` / place-resolution errors; write the SR60
verdict under #105 in `plan/backlog.md`. Then the user tests cockpit chat: ask Dufus to "go to Don's
Donuts" (`/api/sim/agents/dufus/chat/*`) and Claude scans for the resolved `walk_to`.

## Files touched this session

- `plan/backlog.md` — SR57 verdict; #103 build slice + SR58 findings; #104, #105, #106 specs; #102
  default flip note.
- `Python/agent_runtime/agent_manager.py` — #101 abandon reason/path on `interrupt_failed`;
  #103 `_choose_stand_point`, `_STAND_POINT_ARRIVE_CM`, sweep wiring, `set_stand_point` on completion;
  `_normalize_mode` default `play`.
- `Python/agent_runtime/memory_store.py` — `path`, `path_end_gap_cm` in interrupt event snapshot.
- `Python/agent_runtime/stand_point.py` — NEW pure scorer.
- `Python/agent_runtime/place_db.py` — `stand_x/stand_y` migration, `set_stand_point`, `map_cells` fields.
- `Python/agent_runtime/unreal_bridge.py` — `radar(location=…)`.
- `Python/agent_runtime/runner_app.py`, `runner_client.py`, `Python/web_ui/main.py` — default mode `play`.
- `Python/web_ui/templates/sim.html` — Play first/selected; inactive agents hidden.
- `Python/web_ui/templates/map.html` — blue survey dot at the stand point.
- `MCPGameProject/Plugins/UnrealMCP/Source/UnrealMCP/Private/Commands/UnrealMCPCharacterCommands.cpp`
  — radar `location` probe (built by the user, live in SR58).
- `Python/worlds/MCP_World/agents/dufus/` — `state.json`, `tools.json`, `character.md`, `goals.md`,
  `rules.md`, `agenda.json` rewritten (UTF-8).
- `Python/worlds/MCP_World/agents/surveyor/` — NEW template: persona files + README, inactive.
- `~/.claude/.../memory/project_dufus_vlm_training_corpus.md`, `MEMORY.md` — role change recorded.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is:
> after the user restarts the runner + web app and runs Play (SR60) with Dufus and Maren, scan
> `logs/sim_runner.log` and `agent_decisions.log` for both agents starting, Dufus following his agenda
> and speaking to Maren, and any place-resolution errors; write the SR60 verdict under #105 in
> `plan/backlog.md`; then the user tests cockpit chat "go to Don's Donuts".

## Git state at handoff

```
(clean — everything pushed to origin/main)
```

Recent commits:

```
58ced52 fix: cockpit agent list hides inactive APCs (the surveyor template folder)
dd9d27d fix: Dufus persona files re-saved as UTF-8 (cp1252 em-dash made Agent.load fail, SR59 ran without him)
2de7e94 docs: #106 run the world from the web app - roles as templates, agenda place check, active APCs, default mode
45562d1 feat: Play is the default sim mode (runner, client, web UI, mode fallback)
395a766 feat: #105 Dufus becomes a townsperson; surveyor persona kept as an inactive template folder
```
