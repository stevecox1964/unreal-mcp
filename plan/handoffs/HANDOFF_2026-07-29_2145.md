# Handoff — Diagnosed why APCs won't respect simple rules, shipped a facts-not-blocking guardrail slice, committed 3 sessions of backlog

**Date:** 2026-07-29 21:45 local
**Branch:** `main`
**Prior handoff:** [HANDOFF_2026-07-24_2251.md](HANDOFF_2026-07-24_2251.md)

## TL;DR

The user's core frustration this session: months of prompting haven't converged on simple rules ("don't
go into the corn field", "don't move farther from your goal"). Diagnosis: there was no enforcement layer
at all — `action_validator.py` only checks JSON shape, never world semantics, and no zone/distance facts
existed anywhere. Built the user's chosen fix (facts-in-the-prompt, not code-side blocking): a VLM
`footing` classification and a route `PROGRESS WARNING` delta, both rendered as explicit prompt lines,
plus matching `rules.md` rules. Also committed and pushed **three sessions of untouched work**
(recognition #44, agenda ledger #36, durable runner log #51, Sonnet 5 experiment #50, Dufus rewrite #48)
that had been sitting uncommitted since `ab60c71`. **Next session: run this live and read
`sim_runner.log` — nothing in this session was verified against a running Unreal.**

## What was done this session

- **Diagnosed the "LLM won't converge" problem** as architectural, not a prompting failure: read
  `action_validator.py` (schema-only), confirmed no zone/exclusion-zone concept exists anywhere in the
  repo (grepped `places.json`, `world_grid.json`, `world.json` — nothing), and confirmed `route_planner.py`
  computes distance/progress only for the mid-scale router, never surfaced as an explicit delta fact.
- **Presented a 3-way guardrail choice** (block silently / log-only / prompting-only). User chose
  prompting-only, explicitly rejecting code-side enforcement — see Decisions below.
- **Added backlog #52** (Dufus role simplification — strip agenda/schedule juggling down to
  explore-and-survey only) and **#53** (capture navmesh-vs-semantic disagreement as VLM training
  signal) from the user's own framing, cross-referenced against what #19/#27/#32/#49 already covered so
  nothing was duplicated.
- **Implemented the facts-not-blocking guardrail slice**:
  - `Python/agent_runtime/perception.py` — VLM schema gained `footing`
    (`pavement|road|dirt_path|grass|cultivated_field|water|other`), classified straight from the frame.
  - `Python/agent_runtime/llm_router.py` — `_seen_text` renders `FOOTING: <value>`;
    `_schedule_note` renders a `PROGRESS WARNING` line when `delta_cm > 0`.
  - `Python/agent_runtime/agent_manager.py` — `_attach_route_progress` now tracks distance-to-destination
    tick over tick on the cached route (`route["last_distance_cm"]`), stamping `delta_cm` (noise-floored
    at `_PROGRESS_NOISE_CM = 200`).
  - Both `Python/worlds/MCP_World/agents/{dufus,maren}/rules.md` gained explicit imperative lines tied to
    `FOOTING` and `PROGRESS WARNING`.
  - Tests: `test_seen_text_footing`, `test_perceive_parses_footing`, extended
    `test_schedule_route_narration` (now covers first-tick `None`, positive-delta warning, negative-delta
    no-warning) — all green, run directly (not via a suite aggregator — none was found).
- **Committed and pushed 3 sessions of accumulated work** (`42fdd59`, 31 files, +2868/-199) that the prior
  handoff had flagged as an open "whether to commit" question — user resolved it this session with a
  direct "commit, update readme, and push". One commit, not split per item (files were too interleaved
  across sessions to safely untangle).
- **Updated `README.md`** — tick table now mentions footing and the two-log-file split
  (`agent_decisions.log` vs `sim_runner.log`); new bullets for recognition (#44), authored agendas (#36),
  and the facts-not-blocking guardrail philosophy.
- **Found and excluded a secrets file**: `Python/.env.bak-presonnet` (an ungitignored `.env` backup made
  before the Sonnet switch, 3 lines containing `KEY`) was untracked and about to be swept up by a broad
  add. Staged everything explicitly by filename instead of `git add -A`, left it out, warned the user, and
  — on request — deleted it from disk this session.
- **Saved a memory** (`feedback_facts_not_blocking.md`) recording the user's explicit "louder facts, not
  code-side blocking" stance so future guardrail requests default the same way without re-asking.

## In flight

_None._ Everything attempted this session reached a tested, committed, pushed state. The working tree is
clean.

## Decisions locked in this session

- **Enforcement stays in the LLM.** Offered block-silently / log-only / prompting-only for
  no-go-zone and goal-regression violations; user picked prompting-only. Do not propose
  `action_validator.py`-level blocking again unless the user asks directly — see
  `feedback_facts_not_blocking.md`.
- **No hand-authored zone geometry.** The user explicitly does not want the corn field's bounds
  hardcoded anywhere (center+radius, landmark actor, etc.) — the long-run answer is #53's VLM training
  corpus (learn to recognize "avoid this" from Dufus's own accumulated navmesh-vs-semantic disagreements),
  not authored polygons. This session's `footing` field is the interim: VLM-classified per tick, zero
  authoring.
- **Dufus is over-scoped.** User's read: giving him ordinary daily-life tasks (breakfast, fetch hat) on
  top of survey work causes exactly the kind of state-juggling/interruption failures driving the
  "LLM won't converge" frustration. Proposed fix (not yet designed or built): strip him to
  explore-and-survey only. Backlog #52, explicitly design-gated — do not implement without the three
  open questions there answered.
- **One commit for the 3-session backlog**, not split per item — pragmatic call given how interleaved the
  changes were across `rules.md`/`character.md`/etc. (prior sessions and this session touched the same
  files). Don't try to retroactively split `42fdd59`.

## Open questions

- **Does the facts-not-blocking approach actually work?** Nothing this session was tested against a
  running Unreal. The `FOOTING`/`PROGRESS WARNING` lines exist and render correctly in offline tests, but
  whether the LLM actually changes behavior when it sees them — the entire point of the session — is
  unverified. This is the single most important open question.
- **#52 Dufus simplification** — three unanswered design questions logged in the backlog item: does he
  keep `agenda.json`/`goals.md` at all, does he keep social behaviors (greeting/speech), and does this
  generalize to a "survey-only role" any future APC could carry.
- **#53 VLM training capture** — where the navmesh-vs-semantic disagreement gets logged (new table vs.
  extending `place_images`) and what counts as "the LLM blocked it" (today `thought_summary` is free
  text, no structured signal).
- **#50 Sonnet 5 A/B** — still not evaluated; still needs a real live-run sample (SR30 produced ~2
  decisions total, no sample). Unchanged from the prior handoff.
- **#46/#47** — unchanged from the 2026-07-24 18:01 handoff, still untouched.

## Next concrete step

Start Unreal in PIE, run the sim for several minutes via `Python/start_sim.bat` with an agent positioned
near a boundary condition (off-pavement terrain, or partway through a scheduled trip), then read
`Python/worlds/MCP_World/logs/sim_runner.log` for two things at once: (1) the original #51 question —
does Maren still go quiet mid-run, is Dufus's cognition gate the cause — and (2) the new question this
session exists to answer — does a `FOOTING: cultivated_field` or `PROGRESS WARNING` line in the prompt
actually change what the LLM decides to do next, or does it get ignored like the unstructured caption
text did before.

## Files touched this session

Modified (this session's own edits, now part of commit `42fdd59`):
- `Python/agent_runtime/perception.py` — `footing` field added to the VLM schema/prompt, `_empty()`
  default, docstring example
- `Python/agent_runtime/llm_router.py` — `_seen_text` renders `FOOTING:`; `_schedule_note` renders
  `PROGRESS WARNING` when `delta_cm > 0`
- `Python/agent_runtime/agent_manager.py` — `_attach_route_progress` computes/persists `delta_cm`;
  new `_PROGRESS_NOISE_CM = 200.0` constant
- `Python/worlds/MCP_World/agents/dufus/rules.md`, `.../maren/rules.md` — new FOOTING/PROGRESS rules
- `Python/scripts/agent_runtime/test_prompt_context.py` — `test_seen_text_footing`
- `Python/scripts/agent_runtime/test_vision_perceiver.py` — `test_perceive_parses_footing` +
  footing-default assertion
- `Python/scripts/agent_runtime/test_route_planner.py` — extended `test_schedule_route_narration`
  with delta_cm/PROGRESS WARNING coverage
- `plan/backlog.md` — new **#52** (Dufus simplification), new **#53** (VLM training capture of
  navmesh-vs-semantic disagreement), status note added to **#19** (footing fact landed)
- `README.md` — tick table, log-file split, recognition/agenda/guardrail bullets

Committed this session as part of `42fdd59` but authored in prior (unhanded-off) sessions — see the
2026-07-24 22:51 handoff for what each contains:
- `Python/agent_runtime/{cell_sweep,memory_store}.py`, `recognition.py` (new)
- `Python/scripts/agent_runtime/{test_cell_sweep,test_action_errors,test_apc_agenda_ui,
  test_recognition,test_survey_grounding}.py` (four new)
- `Python/web_ui/main.py`, `templates/{agent,sim}.html`, `static/apc_drilldown.js` (new)
- `Python/worlds/MCP_World/agents/dufus/{agenda.json,character.md,goals.md,state.json,tools.json}`
- `RUNBOOK.md`, `plan/handoffs/LATEST.md`
- `plan/handoffs/HANDOFF_2026-07-24_1801.md`, `HANDOFF_2026-07-24_2251.md` (now tracked)

Deleted (not committed — was never tracked): `Python/.env.bak-presonnet`, a secrets backup found
sitting ungitignored in the working tree; removed from disk at the user's request after being flagged.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step
> is: run the sim live in PIE via `Python/start_sim.bat`, then read
> `Python/worlds/MCP_World/logs/sim_runner.log` to check both whether agents still go silent mid-run
> (#51) and whether the new `FOOTING`/`PROGRESS WARNING` prompt facts actually change LLM behavior near
> a boundary condition (the point of this session's guardrail work).

## Git state at handoff

```
(clean — nothing to show)
```

Recent commits:

```
42fdd59 feat: recognition, agenda ledger, durable runner log, guardrail facts
ab60c71 feat: add deterministic APC agendas
e702dd8 docs: approve JSON APC agenda design
ffa04ec docs: record no-handoff preference
72e5cda feat: move APC chat to dedicated page
```
